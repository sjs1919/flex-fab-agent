# 待办清单：多模态运维手册知识库

- 需求规格：`docs/02-specs/需求规格-多模态运维手册知识库-v1-20260907.md`
- 设计文档：`docs/02-specs/2026-09-07-多模态运维手册知识库-design.md`
- 实施计划：`docs/04-plans/2026-09-07-多模态运维手册知识库.md`
- 日期：2026-09-07
- 状态：✅ 需求定稿 + 设计定稿 + 实施计划就绪，**待开始 T0 spike**（需 1 份真实大扫描件样本）

---

## 阶段 0：需求确认（✅ 已完成，2026-09-07 全部闭环）

- [x] 图片处理策略 —— OCR 打底 → 无文字/低置信自动多模态图注 → 界面人工标注重生成
- [x] C1 手册规模 —— **数十份，含大扫描件** → 异步架构锁定，`manual_import_job` 必做
- [x] C2 导入方式 —— **界面上传 + 目录扫描都做**，共用同一套异步解析
- [x] C3 手册权限 —— **分 public/confidential 两档 + 修复只读工具 token 落差 bug**（`DOC_PERMISSION` 改数据驱动）
- [x] C4 版本管理 —— **多版本并存可切**：`device_model` 分组 + `version` + `is_active`，检索只命中激活版
- [x] C5 库分离 —— **手册库与合同库分离**：新建 `kb_manuals` collection + `search_manual` 工具
- [x] C6 多模态 LLM provider —— **不锁供应商**，新增 `VISION_PROVIDERS` 主备表 + `call_vision()` 回落，与现有文本 provider 同构靠参数切换
- [x] C7 溯源粒度 —— **页码 + 章节都存**，PDF 按页、Word 按标题自适应切分
- [x] C8 调试台 tab —— **一个 tab「手册库」** 内含三块，问答复用 `/ask` + `_run_agent_round` 链路

## 阶段 1：技术选型与验证（**执行主线见实施计划 T0–T9**；以下为能力分组的详细清单，随主线推进勾选）

- [ ] PDF 解析库选型（文本层 + 内嵌图 + 扫描页判定），验证离线可用
- [ ] DOCX 解析库选型（正文 + 内嵌图 + 标题层级）
- [ ] 本地 OCR 选型，验证中文识别效果与离线加载（参考 `core/hf_utils.py` 离线优先模式）
- [ ] **多模态 provider 抽象层**（对齐 `config.py:116` / `core/llm_client.py:123-131` 同构）：
  - [ ] `config.py` 新增 `VISION_PROVIDERS` + `PRIMARY_VISION_PROVIDER` + `available_vision_providers()`
  - [ ] 新建 `core/vision_client.py` 的 `call_vision()`：遍历回落 + 连接池 + 缓存
  - [ ] 图片入参统一 OpenAI 兼容 `image_url` + base64 data URI
  - [ ] 缓存键 = 图片 hash + 提示词版本；L3 重生成**显式跳过缓存**
  - [ ] 全 provider 不可用时降级：`caption_source=none` + `review_status=flagged`，不中断解析
  - [ ] 单测 monkeypatch `VISION_PROVIDERS`（照 `core/test_llm_client.py:68`），不发真实调用
  - [ ] 预置候选条目（火山标准端点 / 海外 vision / 本地 VLM 末位兜底），实施期按额度定主用
- [ ] 用一份**真实大扫描件**做端到端 spike，实测解析耗时量级，据此定并发上限与阶段进度粒度
- [x] 出设计文档 → `docs/02-specs/2026-09-07-多模态运维手册知识库-design.md`（D1–D8 决策定稿）
- [x] 出实施计划 → `docs/04-plans/2026-09-07-多模态运维手册知识库.md`（T0–T9，最小闭环早跑通）
- [ ] **T0 解析库型号 spike**（需 1 份真实大扫描件样本）
- [ ] **T1 权限修复 D1**（graph role 注入 + AskRequest.role + search_manual 全角色）
- [ ] **T2 schema v6 三表 + config 手册项 + 迁移**
- [ ] **T3 core/vision_client.py + VISION_PROVIDERS（mock）**
- [ ] **T4 文本手册检索最小闭环**（manual_index/manual_retriever/chunker 文本版/search_manual）
- [ ] **T5 解析 + 图片三级**（loaders/ocr/captioner/chunker 完整/asset 落库）
- [ ] **T6 worker + /manuals API + 静态图访问**
- [ ] **T7 前端**（manual.ts/ManualView/tab manuals/路由）
- [ ] **T8 操作日志动作映射 + smoke 页面**
- [ ] **T9 验收清单 + 全量门禁 + 容器部署验证**

## 阶段 2：数据层

- [ ] `manual_doc` 表 DDL（含 `file_hash` 去重 + `access` 权限 + `device_model/version/is_active` + `device_model+version` 唯一约束）
- [ ] `manual_asset` 表 DDL（含 `caption_source` / `vector_id`）
- [ ] `manual_import_job` 表 DDL（**必做**：stage / stage_progress / retry_count / failed_stage）
- [ ] `schema/migrate.py`：`CURRENT_VERSION` 5 → 6，注册 `_MIGRATIONS[6]` up/down
- [ ] `schema/schema.sql` 同步建表语句

## 阶段 3：解析与灌库层

- [ ] `rag/loaders/` 按格式拆分（pdf / docx / image）
- [ ] `rag/ocr.py`（L1，含置信度输出）
- [ ] `rag/captioner.py`（L2 自动兜底 + L3 重生成，共用一套调用）
- [ ] 阈值配置接入 `/config` 白名单（`api.py:185`）
- [ ] 结构感知切分（按页 / 按标题），metadata 带 doc_id / 页码 / 章节 / asset_id / access / is_active（设计 D3）
- [ ] `rag/manual_index.py` 增量 upsert（按 doc_id + seq 幂等）+ `delete_doc(where doc_id)` + `invalidate()`
- [x] ~~改造 `rag/knowledge_base.py:82` count>0 复用~~ —— **设计收敛 D2：合同语料静态保持现状不改**，增量灌库全部在新模块 `manual_index.py` 做
- [x] ~~`rag/retriever.py:135` _rag_state 加 invalidate~~ —— **设计收敛 D2**：合同 `_rag_state` 不动；手册检索态独立 `_manual_state` + `invalidate()`

## 阶段 4：检索与 Agent 工具层

- [ ] 新建 `kb_manuals` collection（与 `kb_contracts_delay` 分离）
- [ ] 注册 `search_manual` 工具（`tools/registry.py`，沿用 `:258-271` 懒导入模式）
- [ ] 工具 description 写明手册场景关键词，避免与合同工具混淆召回
- [ ] 检索返回带溯源（手册名 + 页码/章节 + 图片 URL）
- [ ] `search_manual` 默认只命中 `is_active=1` 手册（激活版过滤，同型号多版本不打架）
- [ ] **修复只读工具 token 落差（设计 D1，选方案一）**：graph 读工具按 `state["role"]` 签 token（`single_agent_graph.py:247-254`）；`run_single_agent` 增 role 并写入 state
- [ ] `_TOOLS_NEED_TOKEN` 加 `search_manual`（`tools/registry.py:25`）；`auth/token_exchange.py:35-41` RBAC 加白名单且**全角色放行**（guard 层不做 confidential 判定，过滤在 RAG 层）
- [ ] ~~`knowledge_base.py:30` DOC_PERMISSION 改数据驱动~~ —— **设计收敛 D3**：合同 DOC_PERMISSION 保留（仅 1 条）；手册权限在灌库时冗余进 chunk metadata `access`，检索读 metadata 过滤不建映射表
- [ ] `search_manual` 检索过滤：metadata access（角色）+ is_active（激活版）双条件，融合后 rerank 前（与 `search_knowledge_base` 同位置谓词）

## 阶段 5：后端 API

- [ ] `AskRequest.role`（默认 admin）+ `_run_agent_round(query, thread_id, role)` 透传（设计 D1，`api.py:141/467`）
- [ ] 手册列表 / 详情查询端点（新 GET 路由必须插在 `api.py:682` SPA catch-all **之前**，现设计在 :675 静态块前）
- [ ] 上传端点（multipart，类型白名单 + 单文件大小上限，`require_admin`）
- [ ] 目录扫描导入端点（支持子目录、目录名推断分类、按 `file_hash` 幂等跳过并报告跳过数）
- [ ] 两条路径汇聚到同一「建任务入队」入口——**不写两套解析逻辑**
- [ ] 后台解析 worker（串行/限流队列，单图失败降级不拖垮整份）
- [ ] 解析进度查询端点（分阶段 stage + stage_progress）
- [ ] 失败重试端点（从 `failed_stage` 续跑，不整份重解析）
- [ ] 启动时清理残留 `running` 僵尸任务（容器重启场景）
- [ ] 图片资产列表 / 人工标注 / 重生成图注端点（写操作走 `require_admin`，`api.py:274`）
- [ ] 手册权限编辑端点（public/confidential，导入时 + 后续改）
- [ ] 原图静态访问路径（不与 SPA catch-all 冲突）
- [ ] 「设为当前版本」端点（同型号历史版激活切换，写操作 `require_admin`）
- [ ] 删除单版本手册端点（含该版向量下线，不影响其他版本）
- [ ] `observability/operation_log.py:43` 加中文动作名映射

## 阶段 6：前端（调试台）

- [ ] `web/vite.config.ts:16-24` proxy 白名单加新前缀（否则 5173 联调 404）
- [ ] 新建 `web/src/api/manual.ts`（含 multipart 上传，现有封装全是 JSON）
- [ ] 新建手册库视图组件（列表 / 解析详情 / 图片资产 / 问答试验）
- [ ] 手册列表展示权限标记（公开/机密 tag）+ 权限下拉编辑
- [ ] 手册列表按型号分组展示版本，标「当前/历史」+ 「设为当前版本」操作
- [ ] 导入进度展示：分阶段进度条 + 轮询（解析中不阻塞界面，可离开再回来）
- [ ] 失败手册展示错误原因 + 重试按钮
- [ ] 图片人工标注交互（标记识别不准 → 编辑说明 / 重新生成图注 → 确认入库）
- [ ] 挂载：`web/src/views/PortalView.vue:30-55` 加 tab + `web/src/router/index.ts:13-23` 加深链
- [ ] `npm run build`（vue-tsc + vite build）通过

## 阶段 7：测试与验收

- [ ] 后端单测（见需求规格 10.1，含 `test_rag_permission` 权限回归）
- [ ] 手动验收（见需求规格 10.2，含权限项）
- [ ] `flex_fab_agent/smoke_test.py:510` 页面清单加新页
- [ ] `python run_all_tests.py` 全过
- [ ] schema v6 迁移 up/down 均可执行
- [ ] 容器重启后手册与向量库仍在（`RUNTIME_DIR` 挂卷，`docker-compose.yml:14`）
- [ ] （可选）扩展 `eval/ragas_regression.py` 加手册问答质量门禁
