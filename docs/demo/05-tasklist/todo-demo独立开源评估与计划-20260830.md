# demo 独立开源项目（评估与计划）待办

> 日期：2026-08-30 · 优先级：P2 · 状态：📋 待评估
> 决策：用户考虑将 demo 独立成单独项目（代码 + docs），放开源库，作为简历上的作品。**项目名 flex-fab-agent，开源地址 https://github.com/sjs1919/flex-fab-agent.git**；**明确要求迁移时保留 git 历史记录**（2026-08-30 用户追加）。当前为评估与计划阶段，未执行任何拆分动作。

## 背景

用户 2026-08-30 询问「把 demo 独立容易吗」（仅询问，已答复评估：分代码仓库 / 文档体系 / 部署三层次，代码自包含、主要成本在文档链接清理与 git 历史）。随后决定写独立开源计划：代码 + docs 打包独立，放开源库，作简历作品。

## 任务

- [ ] 写评估与计划文档 `docs/demo/04-plans/2026-08-30-demo独立开源计划.md`
- [ ] 确认 git 历史迁移方案：`git filter-repo`（推荐）按路径抽取，一次指定 `demo/` + `web/` + `docs/demo/` + Dockerfile + docker-compose.yml + requirements 等全部路径，保留这些路径的完整历史；**会改写 commit hash**；历史中的敏感信息（.env / credentials）需 `--replace-text` 或人工清理
- [ ] 计划经用户确认后登记执行拆分的 todo

## 验收 checklist

1. 计划明确：目标、范围（哪些文件进独立仓）、git 历史策略、docs 组织、开源准备、简历素材、风险
2. 拆分的执行步骤可操作（含验证命令）

## 关联

- workspace 先例：retrosys 独立拆分（commit `4ab7744`，`chore(workspace): 将 retrosys 拆分为独立仓库，从父仓库追踪中移除`）
- 简历触发规则：涉及简历作品，落地到产出文件后需同步简历相关文档
