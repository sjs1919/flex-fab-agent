# 资源聚合页延后项 backlog

> 日期：2026-08-27 · 优先级：P2 · 状态：📋 延后（非阻塞合并，最终审查裁定可延后）

## 事项

资源聚合页 + 人员模块最终整分支审查（APPROVE，无 Critical）后，延后 Minor 登记，便于后续排期。

## 延后项

| # | 项 | 位置 | 说明 |
|---|----|------|------|
| 1 | **PUT 人员状态同值幂等误报 404**（已修 2026-08-28） | `demo/api.py` `/resources/personnel/{id}/status` | ✅ 已修：`WHERE status<>%s` 幂等 + 存在性区分（同状态成功，不存在才 404） |
| 2 | batches/preprocess 空断言 | `demo/test_resources.py` | 已缓解：任务 5 时两表有 166 条数据，id 倒序断言实际已覆盖 |
| 3 | load_* `SELECT *` 隐式主键序（已修 2026-08-28） | `demo/tools/data.py` | ✅ 已修：load_batches/load_preprocess_tasks 加 `ORDER BY id` 显式排序 |
| 4 | 404 消息断言加固（已修 2026-08-28） | `demo/test_resources.py` | ✅ 已修：断言 `message` 含「未知资源类目」 |
| 5 | `_CATEGORIES` 未纳 personnel（已修 2026-08-28） | `demo/test_resources.py` | ✅ 已修：personnel 入 `_CATEGORIES` |
| 6 | leave 测试 seed 隐式依赖（已修 2026-08-28） | `demo/simulator/test_engine_events.py` | ✅ 已修：fixture 调 `seed_personnel` 幂等确保 personnel 存在 |
| 7 | 单行 if/else 风格（已修 2026-08-28） | `web/src/views/ResourcesView.vue` | ✅ 已修：`if (res.ok) ...; else ...` 改块状（位置实为前端 ResourcesView，非 demo/tools） |
| 8 | **test_e2e_m3 批次完成依赖干净库**（已修 2026-08-28） | `demo/simulator/test_e2e_m3.py` | ✅ 已修：fixture 造产能内 5 订单 + teardown 恢复共享库（全量已全绿） |
| 9 | **demo 数据规模 vs 设备产能匹配**（已修 2026-08-28） | `seed.py` / `solver` | ✅ 已修：seed 订单 40→20（solve FEASIBLE，批次有 start_time）；test_e2e_m3/order_tools/data_mysql/test_seed 已适配 |
| 10 | **test_runner_ticks 挂起**（已修 2026-08-28） | `demo/simulator/test_runner.py` | ✅ 已修：deadline 放宽 60s + mock KPI 快照（tick 只验证推进/scene_version，KPI 由 test_run_tick_writes_kpi_snapshot 覆盖） |

## 状态

- ✅ 已全部修复（2026-08-28）：#1-#7 均已完成并验证（test_resources/engine_events 13 用例 + 前端 build 通过）；#8-#10 已完成
