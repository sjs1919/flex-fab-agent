# 资源聚合页延后项 backlog

> 日期：2026-08-27 · 优先级：P2 · 状态：📋 延后（非阻塞合并，最终审查裁定可延后）

## 事项

资源聚合页 + 人员模块最终整分支审查（APPROVE，无 Critical）后，延后 Minor 登记，便于后续排期。

## 延后项

| # | 项 | 位置 | 说明 |
|---|----|------|------|
| 1 | **PUT 人员状态同值幂等误报 404**（优先） | `demo/api.py` `/resources/personnel/{id}/status` | 目标已是同状态时 `rowcount=0` → 误报 404「人员不存在」；应区分「无变化成功」与「不存在」 |
| 2 | batches/preprocess 空断言 | `demo/test_resources.py` | 已缓解：任务 5 时两表有 166 条数据，id 倒序断言实际已覆盖 |
| 3 | load_* `SELECT *` 隐式主键序 | `demo/tools/data.py` | 依赖 InnoDB 主键升序，测试守护；CSV 兜底路径排序无保证 |
| 4 | 404 消息断言加固 | `demo/test_resources.py` | 未断言 `message:"未知资源类目"`，可防 catch-all 回归 |
| 5 | `_CATEGORIES` 未纳 personnel | `demo/test_resources.py` | 注释「6 类」过时，personnel 未入循环（有独立测试） |
| 6 | leave 测试 seed 隐式依赖 | `demo/simulator/test_engine_events.py` | 依赖 seed 后 personnel 表存在 |
| 7 | 单行 if/else 风格 | `demo/tools` | 风格统一 |

## 状态

- 全部延后，非阻塞；#1（PUT 幂等 404）建议优先修复
