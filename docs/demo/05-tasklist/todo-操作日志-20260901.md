# 验收清单：操作日志统一落库与查询界面

- 需求规格：`docs/demo/02-specs/需求规格-操作日志-v1-20260901.md`
- 实施计划：`docs/demo/04-plans/2026-09-01-操作日志统一落库与查询界面.md`
- 日期：2026-09-01
- 状态：进行中

## 后端单测（test_operation_log.py）

- [ ] `record_operation` 写入成功、detail 正确序列化
- [ ] `query_operations` 分页：total 正确、page 越界返回空页
- [ ] 各筛选维度：category / start-end 区间 / keyword（action、summary、trace_id 三种命中）
- [ ] `classify_request` 分流：debug/manual 归类正确、静态资源与 /health、/logs、/debug/admin-token 不落
- [ ] 现有测试不破坏：test_auto_scheduler.py、simulator/、test_api_sim.py 全过

## 手动验收（四类落库）

- [ ] 自动调度触发排产 → `auto` 类出现「自动排产触发」
- [ ] 自动审批通过 → `auto` 类出现「自动审批」
- [ ] 模拟器 tick 有事件 → `simulator` 类出现「模拟器tick 推进1h」
- [ ] Web 控制台手动排产/审批/改配置 → `manual` 类出现对应动作
- [ ] 调试台 rerun/judge → `debug` 类出现对应动作
- [ ] 四类「跳过型」自动日志未落库
- [ ] `/logs` 响应含 trace_id、sim_time 字段，排序 real_time DESC

## 前端验收

- [ ] 新 tab「操作日志」在 Web 控制台出现，默认进看板
- [ ] 表格列：现实时间 / 类别 tag / 动作 / 状态 tag / 摘要 / 模拟时间 / trace_id
- [ ] 类别筛选（四类 + 全部）
- [ ] 时间范围筛选（daterange）
- [ ] 关键字搜索（动作/摘要/trace_id 命中）
- [ ] 分页：翻页、页大小切换（10/20/50/100）
- [ ] 空态展示正常
- [ ] `npm run build`（vue-tsc 类型检查 + vite build）通过
- [ ] `npm run build` 通过

## 部署验证

- [ ] schema v5 迁移应用成功（CURRENT_VERSION=5）
- [ ] `python run_all_tests.py` 全过
- [ ] 容器重启后 `/logs` 端点可访问、四类日志随运行增长
