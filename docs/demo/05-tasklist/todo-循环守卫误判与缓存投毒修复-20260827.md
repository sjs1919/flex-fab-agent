# 循环守卫误判与缓存投毒修复 待办

> 日期：2026-08-27 · 优先级：P1 · 状态：✅ 已完成（2026-08-27 复测通过，真实查询自然语言答案 + 工具调用合理）

## 事项

问题「有没有订单已经打印完成？」产出垃圾答案（judge 相关度 0 / faithfulness 0），trace `5ac4a01cd16246e1` 显示答案 100% 来自语义缓存命中。根因链：

1. **循环守卫误判**（`demo/graph/single_agent_graph.py` evaluate_results）：用 `Counter` 全局计数同一工具出现 ≥3 次即判循环，把「query_orders 带不同 status 参数的合理探索」误判成循环。
2. **产出原始 dump**：误判时 `final_answer` 直接拼 `"基于已收集数据的汇总（检测到重复检索，停止继续查询）：\n" + 全部工具结果`，泄漏内部诊断消息、答非所问。
3. **垃圾写入语义缓存**（`demo/agents/single_agent.py` 缓存写入门禁）：只拦 DSML 标记文本与优雅兜底，未拦循环守卫 dump。
4. **命中返回**：同类问题再问 → 缓存命中跳过整图执行，垃圾原样返回。

## 状态

- [x] **任务 1**（graph）：筛选空结果识别为终态答案 —— query_orders 带筛选条件且干净返回「未找到匹配的订单。」→ 直接 ready_for_answer
- [x] **任务 2**（graph）：循环守卫改为「同工具 + 同参数连续 ≥3 次」判定；产出改为面向用户前缀 `LOOP_GUARD_FALLBACK_PREFIX`（去掉「检测到重复检索」内部诊断）
- [x] **任务 3**（agents）：缓存写入门禁追加拦截 `LOOP_GUARD_FALLBACK_PREFIX` 前缀答案
- [x] **任务 4**：全量测试验证（新增回归测试 4 条全绿；全量套件存量环境性失败已基线对照证明非本次引入）

**复测结果（2026-08-27，用户要求全体验收）：**
- ✅ 不再命中垃圾缓存：final_answer 为新生成自然语言（"由于当前没有正在生产或即将完成的订单…"），非缓存垃圾
- ✅ final_answer 不含「检测到重复检索」/「基于已收集数据的汇总」
- ✅ tool_results 2 次 query_orders（status=即将完成/生产中，空结果即终态，未 3 次误判循环）
- ⏭️ judge 相关度（eval 设计限制：订单非 RAG 上下文，不强制）

## 待办（环境就绪后端到端复测）

> 用户选择「等环境就绪后端到端复测」（2026-08-27）。前置：MySQL（WSL demo_scheduling 库）可达 + `.env` LLM key 就位。

**复测步骤：**
1. **清污染缓存**：重测前删 `demo/data/cache_db/`（或调 `clear_state_entries()`）——修复前的垃圾条目仍在 Chroma 持久化里，不清理会继续命中。
2. **跑一次真实查询**：`python -c "from demo.agents.single_agent import run_single_agent; r=run_single_agent('有没有订单已经打印完成？'); print(r['final_answer']); print(r['tool_results'])"`（cwd=demo）
3. **验收标准**：
   - 不再命中垃圾缓存（trace 有 `query_orders` + `generate_answer` span，非 1 个 `cache:lookup` hit）
   - `final_answer` 为自然语言「没有已打印完成的订单」类，不含「检测到重复检索」/「基于已收集数据的汇总」
   - `tool_results` 仅 1 次 `query_orders`（空结果即终态，不再 3 次误判循环）
   - judge 答案相关度 >0（`has_context` 仍 false：订单非 RAG 上下文，judge 只认 `search_knowledge_base`——eval 设计限制，非本修复范围）

## 关联

- 实现计划：`04-plans/2026-08-27-循环守卫误判与缓存投毒修复.md`
- 根因证据：`demo/data/cases.jsonl:36`（同问题 2026-08-24 已产出相同垃圾，judge 0/0）
- 既有测试：`demo/graph/test_single_agent_graph.py`、`demo/agents/test_single_agent.py`
