# 汇总步标记文本防护（方案 B）待办

> 日期：2026-08-26 · 优先级：P1 · 状态：✅ 已完成（2026-08-27，commit a5f278f）

## 事项

demo/graph 汇总步（generate_answer）模型偶发输出 DSML 标记文本（`<|tool_calls|>` / `||DSML||`），
导致用户只看到兜底道歉。已有重试纠错（`_SUMMARY_NUDGE` + 重试分支，另一 agent 改动）；
**方案 B** 为纵深防御——正常汇总 prompt 显式禁止标记语法，降低触发率（防患未然）。

## 状态

- [x] **代码追加**（2026-08-26）：`demo/graph/single_agent_graph.py` 的 `generate_answer` `summary_lines` 追加一行
  `禁止输出 <|tool_calls|>/||DSML|| 等工具调用标记语法——本轮为最终汇总，不使用工具，仅用中文输出综合结论。`
- [x] **测试验证**（2026-08-27）：`pytest demo/graph/test_single_agent_graph.py` → 15 passed（方案 B + 另一 agent 重试改动全过）
- [x] **commit**（2026-08-27）：`a5f278f` `fix(demo): 汇总步标记文本防护——重试纠错(_SUMMARY_NUDGE) + 主动禁止DSML语法(方案B)`（含另一 agent 的 `_SUMMARY_NUDGE` + 重试分支）

## 关联

- `demo/graph/single_agent_graph.py:346`（方案 B 插入处）
- 另一 agent 改动：`_SUMMARY_NUDGE`（L85-86）+ `generate_answer` 重试分支（L363）
