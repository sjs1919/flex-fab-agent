# 自动排产调度器 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 新增自动排产调度器——容器内后台线程随模拟器 tick 周期自动排产 + 版本级 FIFO 自动审批（保留最近 top20 待审核），模拟器事件触发即时重排兜底，Agent 语义触发与人工审批保持不变。实现「自动排版 → 自动关联设备 → 自动审核通过 → 模拟器推进」完整闭环。

**架构：** `AutoScheduler` 后台线程（`demo/scheduler/auto_scheduler.py`）监控 `sim_clock.tick_count`，每 `AUTO_SCHEDULE_TICK_INTERVAL`（默认 3）tick 触发一轮 `run_scheduling`（求解器自动分配 machine_id）+ FIFO 审批（保留 `AUTO_APPROVE_TOP_N`=20 个最新待审核版本，更早的有批次版本自动 `approve_schedule`，approver=system）；模拟器 tick 检测到重排信号（设备故障/新插单/延期告警）→ `request_rerun()` 即时重排兜底；`threading.Lock` 保证同一时刻仅一个排产在跑。配置走 env，`GET /scheduler/status` 查看运行态。

**技术栈：** FastAPI + pymysql + pytest(TestClient)；模拟器 A 时钟 + 事件引擎。

**关联现状：**
- `demo/tools/scheduler_tools.py`：`run_scheduling(triggered_by)` 求解 + 落库（版本+批次，`machine_id` 求解器自动分配）；`approve_schedule(version_id, action, note, approver)` 单事务更新状态
- `demo/simulator/runner.py`：模拟器心跳线程（`tick_count`），tick 推进批次/设备状态（按 `batches.machine_id` 关联设备）
- `demo/simulator/engine.py`：`check_hard_infeasibility`（延期告警）、`_fire_machine_failure`/`_fire_new_order`（事件）
- `demo/config.py`：`SIM_TICK_SECONDS` 等 env 配置模式
- `demo/api.py`：`/sim/start|stop|status`、`/schedule/approve`（R-7 admin）

---

## 文件结构

| 文件 | 动作 | 职责 |
|------|------|------|
| `demo/scheduler/auto_scheduler.py` | 创建 | `AutoScheduler` 类：后台线程 + FIFO 审批 + 事件重排信号 + 状态 |
| `demo/config.py` | 修改 | `AUTO_SCHEDULE_ENABLED`/`AUTO_SCHEDULE_TICK_INTERVAL`/`AUTO_APPROVE_TOP_N` |
| `demo/simulator/runner.py` | 修改 | tick 尾检测重排信号 → 通知 AutoScheduler 即时重排（兜底） |
| `demo/api.py` | 修改 | `GET /scheduler/status`；应用启动接线 AutoScheduler |
| `demo/test_auto_scheduler.py` | 创建 | AutoScheduler 单测（FIFO/节流/锁/事件） |
| `demo/test_api_scheduler.py` | 创建 | `/scheduler/status` 端点测试 |

---

## 任务 1：AutoScheduler 核心（配置 + FIFO 审批 + 排产节流）

**文件：**
- 创建：`demo/scheduler/auto_scheduler.py`
- 修改：`demo/config.py`
- 测试：`demo/test_auto_scheduler.py`

- [ ] **步骤 1：config 加配置项**

`demo/config.py` 追加：
```python
AUTO_SCHEDULE_ENABLED = os.getenv("AUTO_SCHEDULE_ENABLED", "on")      # off 关闭自动排产
AUTO_SCHEDULE_TICK_INTERVAL = int(os.getenv("AUTO_SCHEDULE_TICK_INTERVAL", "3"))  # 每 N tick 排一轮
AUTO_APPROVE_TOP_N = int(os.getenv("AUTO_APPROVE_TOP_N", "20"))        # 保留最近 N 个待审核版本，更早自动通过
```

- [ ] **步骤 2：编写失败测试**

`demo/test_auto_scheduler.py`：
```python
"""AutoScheduler 单测：FIFO 审批 / tick 节流 / 并发锁 / 事件重排信号。"""
import threading
from demo.scheduler.auto_scheduler import AutoScheduler
from demo.tools.data import get_connection


def _seed_versions(count: int) -> list[int]:
    """造 count 个待审核版本（有批次），返回 id 升序。"""
    with get_connection() as conn, conn.cursor() as cur:
        ids = []
        for i in range(count):
            cur.execute("INSERT INTO schedule_versions (created_at, triggered_by, status) "
                        "VALUES (NOW(), 'test', '待审核')")
            vid = cur.lastrowid
            cur.execute("INSERT INTO batches (id, schedule_version_id, process, model_type, "
                        "source, status, approval_status) "
                        "VALUES (%s, %s, 'SLA', '450', '整批', '前道', '待审核')",
                        (f"T{vid}", vid))
            ids.append(vid)
        conn.commit()
    return ids


def test_fifo_approve_keeps_top_n():
    """保留最近 top_n 待审核版本，更早的有批次版本自动通过。"""
    s = AutoScheduler(interval_ticks=3, approve_top_n=2)
    ids = _seed_versions(5)  # id 升序，ids[-2:] 为最新
    s._fifo_approve()
    with get_connection() as conn, conn.cursor() as cur:
        cur.execute("SELECT id, status FROM schedule_versions WHERE id IN (%s,%s,%s,%s,%s) "
                    "ORDER BY id", tuple(ids))
        rows = dict(cur.fetchall())
    for vid in ids[:-2]:
        assert rows[vid] == "已审核", f"最早版本 {vid} 应自动通过"
    for vid in ids[-2:]:
        assert rows[vid] == "待审核", f"最近版本 {vid} 应保留待审"


def test_run_once_locked_when_busy():
    """并发：同一时刻仅一个 run_once 执行（锁生效）。"""
    s = AutoScheduler(interval_ticks=3, approve_top_n=2)
    entered = threading.Event()
    release = threading.Event()

    def _fake_run(trigger):
        entered.set()
        release.wait(timeout=3)
        s._runs += 1
        return "ok"

    s._auto_schedule = _fake_run
    t = threading.Thread(target=lambda: s.run_once("tick"))
    t.start()
    entered.wait(timeout=3)
    # 第二线程尝试 run_once -> 应被锁阻塞（不执行）
    s.run_once("tick")
    release.set()
    t.join(timeout=5)
    assert s._runs == 1, f"并发应只执行一次，实际 {s._runs}"


def test_auto_schedule_disabled():
    """AUTO_SCHEDULE_ENABLED=off 时 run_once 不排产。"""
    s = AutoScheduler(interval_ticks=3, approve_top_n=2, enabled=False)
    assert s.run_once("tick") is False
```

- [ ] **步骤 3：运行测试确认失败**

运行：`wsl -e bash -lc 'cd /mnt/c/workspace/projects/agent-training && .venv/bin/python -m pytest demo/test_auto_scheduler.py -v'`
预期：`ImportError: cannot import name 'AutoScheduler'`

- [ ] **步骤 4：实现 AutoScheduler**

`demo/scheduler/auto_scheduler.py`：
```python
"""自动排产调度器（2026-08-27）——容器内后台线程，随模拟器 tick 周期排产 + FIFO 审批。

三层触发：
  1. cron 周期（本模块主循环）：每 AUTO_SCHEDULE_TICK_INTERVAL tick 跑 run_scheduling + FIFO 审批
  2. 模拟器事件（runner tick 尾 request_rerun）：设备故障/插单/延期告警 → 即时重排兜底
  3. Agent 语义（run_scheduling 工具，已有）：用户提问触发，保持不动
人工审批保持：FIFO 只自动通过「早于最近 top_n」的待审核版本，top_n 内留给人审。

并发：_lock 保证同一时刻仅一个 run_scheduling 在跑（CP-SAT 不可并发）。
"""
from __future__ import annotations

import logging
import threading
import time

from ..config import (
    AUTO_APPROVE_TOP_N, AUTO_SCHEDULE_ENABLED, AUTO_SCHEDULE_TICK_INTERVAL, SIM_TICK_SECONDS,
)

logger = logging.getLogger(__name__)


class AutoScheduler:
    def __init__(self, interval_ticks: int | None = None, approve_top_n: int | None = None,
                 enabled: bool | None = None):
        self.interval_ticks = interval_ticks if interval_ticks is not None else AUTO_SCHEDULE_TICK_INTERVAL
        self.approve_top_n = approve_top_n if approve_top_n is not None else AUTO_APPROVE_TOP_N
        self.enabled = enabled if enabled is not None else AUTO_SCHEDULE_ENABLED.lower() != "off"
        self._lock = threading.Lock()          # 排产互斥（与 agent/事件并发）
        self._signal = threading.Event()       # 事件即时重排信号
        self._stop = threading.Event()
        self._thread: threading.Thread | None = None
        self._last_sim_time: str | None = None   # sim_clock 无 tick_count 列，以 current_sim_time 变化计 tick
        self._tick_changes = 0
        self._runs = 0
        self._last_version: int | None = None
        self._last_trigger: str | None = None

    # ---- 生命周期 ----

    def start(self) -> None:
        if not self.enabled or self._thread and self._thread.is_alive():
            return
        self._thread = threading.Thread(target=self._loop, name="auto-scheduler",
                                        daemon=True)
        self._thread.start()
        logger.info("自动排产调度启动: interval=%d tick, top_n=%d",
                    self.interval_ticks, self.approve_top_n)

    def stop(self) -> None:
        self._stop.set()
        self._signal.set()
        if self._thread:
            self._thread.join(timeout=5)

    # ---- 主循环（随模拟器 tick 节流） ----

    def _loop(self) -> None:
        while not self._stop.is_set():
            # 事件即时重排优先；否则按 tick 节流
            if self._signal.is_set():
                self._signal.clear()
                self._run_locked("event")
            else:
                sim_time = self._read_sim_time()
                if sim_time is not None and sim_time != self._last_sim_time:
                    if self._last_sim_time is None:
                        self._last_sim_time = sim_time
                    else:
                        self._tick_changes += 1
                        self._last_sim_time = sim_time
                        if self._tick_changes >= self.interval_ticks:
                            self._tick_changes = 0
                            self._run_locked("tick")
            self._stop.wait(SIM_TICK_SECONDS / 4 or 1)

    def _read_sim_time(self) -> str | None:
        """读 sim_clock.current_sim_time（tick 变化依据；sim_clock 无 tick_count 列）。"""
        try:
            from demo.tools.data import get_connection
            with get_connection() as conn, conn.cursor() as cur:
                cur.execute("SELECT current_sim_time FROM sim_clock WHERE id=1")
                row = cur.fetchone()
                return str(row[0]) if row else None
        except Exception as e:
            logger.warning("读 sim_clock 失败: %s", e)
            return None

    # ---- 单轮：排产 + FIFO 审批（带锁） ----

    def run_once(self, trigger: str = "tick") -> bool:
        """供测试/手动调用的单轮入口；enabled=False 时不动作。"""
        if not self.enabled:
            return False
        return self._run_locked(trigger)

    def _run_locked(self, trigger: str) -> bool:
        if not self._lock.acquire(blocking=False):
            return False  # 已有排产在跑，跳过本轮
        try:
            self._auto_schedule(trigger)
            self._fifo_approve()
            self._runs += 1
            self._last_trigger = trigger
            return True
        finally:
            self._lock.release()

    def _auto_schedule(self, trigger: str) -> None:
        """跑一轮排产（求解器自动分配 machine_id），落库版本。"""
        from demo.tools.scheduler_tools import run_scheduling
        try:
            result = run_scheduling(triggered_by=f"auto:{trigger}")
            import re
            m = re.search(r"版本 (\d+)", result)
            if m:
                self._last_version = int(m.group(1))
        except Exception as e:
            logger.error("自动排产失败(%s): %s", trigger, e)

    def _fifo_approve(self) -> None:
        """版本级 FIFO：保留最近 top_n 待审核版本，更早的有批次版本自动通过（approver=system）。"""
        from demo.tools.data import get_connection
        from demo.tools.scheduler_tools import approve_schedule
        try:
            with get_connection() as conn, conn.cursor() as cur:
                cur.execute(
                    "SELECT v.id FROM schedule_versions v "
                    "WHERE v.status='待审核' "
                    "AND EXISTS (SELECT 1 FROM batches b WHERE b.schedule_version_id=v.id) "
                    "ORDER BY v.id")
                ids = [r[0] for r in cur.fetchall()]
            if len(ids) <= self.approve_top_n:
                return
            for vid in ids[:-self.approve_top_n]:
                approve_schedule(vid, "通过", note="自动审批(FIFO)", approver="system")
        except Exception as e:
            logger.error("FIFO 自动审批失败: %s", e)

    # ---- 事件即时重排（兜底） ----

    def request_rerun(self) -> None:
        """模拟器事件（设备故障/插单/延期告警）触发即时重排信号。"""
        self._signal.set()

    # ---- 状态 ----

    def status(self) -> dict:
        return {
            "enabled": self.enabled,
            "interval_ticks": self.interval_ticks,
            "approve_top_n": self.approve_top_n,
            "running": bool(self._thread and self._thread.is_alive()),
            "runs": self._runs,
            "last_version": self._last_version,
            "last_trigger": self._last_trigger,
        }
```

- [ ] **步骤 5：跑测试验证通过**

运行：`.venv/bin/python -m pytest demo/test_auto_scheduler.py -v`
预期：3 个测试全 PASS（FIFO 保留 topN / 并发锁 / disabled）。**注意**：`test_fifo_approve_keeps_top_n` 用真 MySQL（对齐 test_schedule_approve），测试末尾把造数版本清理或保留（demo 库可接受）。

- [ ] **步骤 6：Commit**

```bash
git add demo/scheduler/auto_scheduler.py demo/config.py demo/test_auto_scheduler.py
git commit -m "feat(demo): 自动排产调度器核心——随 tick 周期排产 + FIFO 自动审批(保留topN) + 并发锁"
```

---

## 任务 2：模拟器事件触发即时重排（兜底）

**文件：**
- 修改：`demo/simulator/runner.py`
- 测试：`demo/test_auto_scheduler.py`（追加）

- [ ] **步骤 1：失败测试**

`demo/test_auto_scheduler.py` 追加：
```python
def test_request_rerun_sets_signal():
    """事件信号触发：request_rerun 后 _signal 置位，run_once 处理并清除。"""
    s = AutoScheduler(interval_ticks=3, approve_top_n=2)
    s._auto_schedule = lambda trigger: None   # 不真跑求解
    s._fifo_approve = lambda: None
    s.request_rerun()
    assert s._signal.is_set()
    # 模拟循环处理一次信号
    s._signal.clear()
    assert not s._signal.is_set()
```

- [ ] **步骤 2：实现 runner 事件信号**

`demo/simulator/runner.py` 的 `run_tick` 尾部，检测本次 tick 是否发生「需重排」事件（machine_failure / new_order / 延期告警），有则通知 AutoScheduler：

```python
# 事件兜底重排（2026-08-27）：本 tick 发生设备故障/新插单/延期告警 → 即时重排信号
from ..scheduler.auto_scheduler import get_scheduler  # 或注入单例
if self._need_reschedule(cur, sim_time):
    get_scheduler().request_rerun()
```

辅助函数（同文件）：
```python
def _need_reschedule(self, cur, sim_time) -> bool:
    """本 tick 是否发生需重排的事件：设备故障 fired / 新插单 / 延期告警。"""
    cur.execute(
        "SELECT event_type, COUNT(*) FROM sim_events "
        "WHERE status='fired' AND sim_time=%s "
        "AND event_type IN ('machine_failure','new_order') GROUP BY event_type", (sim_time,))
    rows = cur.fetchall()
    if rows:
        return True
    # 延期告警（硬性不可行）
    if check_hard_infeasibility(cur, sim_time):
        return True
    return False
```
（`check_hard_infeasibility` 已在 engine.py 提供；`get_scheduler` 为模块级单例访问器，见任务 3 接线）

- [ ] **步骤 3：跑测试**

运行：`.venv/bin/python -m pytest demo/test_auto_scheduler.py -q`
预期：4 个测试全 PASS

- [ ] **步骤 4：Commit**

```bash
git add demo/simulator/runner.py demo/test_auto_scheduler.py
git commit -m "feat(demo): 模拟器事件触发即时重排（设备故障/插单/延期告警兜底）"
```

---

## 任务 3：启动接线 + 状态端点

**文件：**
- 修改：`demo/scheduler/auto_scheduler.py`（模块级单例 `get_scheduler`）
- 修改：`demo/api.py`（`/scheduler/status` + 启动接线）
- 测试：`demo/test_api_scheduler.py`

- [ ] **步骤 1：模块级单例**

`demo/scheduler/auto_scheduler.py` 末尾：
```python
_scheduler_instance: AutoScheduler | None = None


def get_scheduler() -> AutoScheduler:
    """进程级单例（供 runner / api / 测试获取）。"""
    global _scheduler_instance
    if _scheduler_instance is None:
        _scheduler_instance = AutoScheduler()
    return _scheduler_instance
```

- [ ] **步骤 2：失败测试**

`demo/test_api_scheduler.py`：
```python
"""GET /scheduler/status 端点测试。"""
from fastapi.testclient import TestClient
from demo.api import app


def test_scheduler_status_returns_state():
    """status 端点返回调度器运行态（enabled/interval/runs 等）。"""
    client = TestClient(app)
    r = client.get("/scheduler/status")
    assert r.status_code == 200
    data = r.json()
    assert "enabled" in data and "interval_ticks" in data
    assert data["approve_top_n"] >= 1
```

- [ ] **步骤 3：实现端点 + 启动接线**

`demo/api.py`：
- 导入：`from .scheduler.auto_scheduler import get_scheduler`
- 启动接线：FastAPI 启动事件里 `get_scheduler().start()`（受 `AUTO_SCHEDULE_ENABLED` 控制）：

```python
@app.on_event("startup")
def _start_auto_scheduler():
    """自动排产调度随应用启动（enabled=off 时 no-op）。"""
    get_scheduler().start()
```

- 状态端点：

```python
@app.get("/scheduler/status")
def scheduler_status() -> dict:
    """自动排产调度器运行态（enabled/interval/topN/runs/上次版本/触发原因）。"""
    return get_scheduler().status()
```

- [ ] **步骤 4：跑测试 + compileall**

运行：`.venv/bin/python -m pytest demo/test_api_scheduler.py demo/test_auto_scheduler.py -q`（全 PASS）+ `compileall`

- [ ] **步骤 5：Commit**

```bash
git add demo/scheduler/auto_scheduler.py demo/api.py demo/test_api_scheduler.py
git commit -m "feat(demo): AutoScheduler 启动接线 + /scheduler/status 状态端点"
```

---

## 任务 4：端到端验收测试

**验收标准**（来自设计 spec + 用户需求）：
- A1 自动排产循环：模拟器运行数 tick 后，自动生成新版本（`/schedule/versions` 数量增长），`machine_id` 已自动分配（批次非空）
- A2 FIFO 审批：待审核版本 > top20 时，更早的自动变「已审核」（approver=system）；最近 top20 保持「待审核」
- A3 事件重排：设备故障事件后触发即时重排（`/scheduler/status` last_trigger=event 或版本新增）
- A4 Agent 语义 / 人工审批不受影响：`run_scheduling` 工具、`POST /schedule/approve` 仍可用
- A5 配置开关：`AUTO_SCHEDULE_ENABLED=off` 时调度器不排产

- [ ] **步骤 1：单元回归**

```bash
.venv/bin/python -m pytest demo/test_auto_scheduler.py demo/test_api_scheduler.py demo/test_schedule_approve.py demo/test_resources.py -q
```
预期：全 PASS

- [ ] **步骤 2：容器重建 + 观察自动排产循环**

```bash
docker compose build && docker compose up -d
# 观察：模拟器运行数 tick 后版本增长 + 自动审批
curl -s http://localhost:8000/scheduler/status | python3 -m json.tool
curl -s http://localhost:8000/schedule/versions | python3 -c "import sys,json; print(len(json.load(sys.stdin)['versions']),'个版本')"
```

- [ ] **步骤 3：FIFO 审批验证**

```bash
# 待审核版本数
curl -s http://localhost:8000/schedule/versions | python3 -c "import sys,json; v=json.load(sys.stdin)['versions']; print('待审核:', sum(1 for x in v if x['status']=='待审核'))"
# 自动通过（top20 外）生效：较早版本 status=已审核 且 approvals.approver=system
```

- [ ] **步骤 4：事件重排验证**

容器日志观察 `auto:event` 触发（或 `/scheduler/status` last_trigger 变化）；设备故障事件后版本新增

- [ ] **步骤 5：Agent/人工不受影响 + 开关验证**

调试台提问触发排产正常；审批 tab 通过版本正常；`.env` 设 `AUTO_SCHEDULE_ENABLED=off` 重建后 `/scheduler/status` enabled=false 且不自动排产

---

## 自检对照

- 规格覆盖：A1（自动排产循环）任务 1 · A2（FIFO）任务 1 · A3（事件重排）任务 2 · A4（Agent/人工不动）贯穿 · A5（开关）任务 1/4
- 占位符扫描：所有步骤含实际代码/命令
- 并发安全：`_lock`（排产互斥）+ `_signal`（事件信号）+ 线程 daemon
- 类型一致：`get_scheduler()` 单例 ↔ runner/api 引用；`run_once(trigger)` ↔ 测试
- 端到端验收：任务 4 为最后一个任务，逐条列出 A1-A5 验收标准 + 验证命令
