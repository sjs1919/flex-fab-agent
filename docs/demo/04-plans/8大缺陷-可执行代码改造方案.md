# 8 大缺陷 · 可执行代码改造方案

> 基于 `7周路线-vs-demo-差距全景图.md` 中的 8 个 🔴 红色缺陷，逐一展开为可落地的代码改造方案。
> 每个方案包含：当前代码位置 → 改造目标 → 详细步骤 → 验收标准。
> 日期：2026-08-07

---

## 方案概览

| # | 缺陷 | 改造范围 | 预估工作量 | 优先级 |
|---|------|---------|-----------|--------|
| R1 | 工具执行无沙箱与重试 | `tools/registry.py` + 新建 `tools/sandbox.py` | 1.5 天 | P0 |
| R2 | 输出护栏缺失 | 新建 `guardrails/` 模块 | 2 天 | P0 |
| R3 | 步骤校验为 noop | `graph/single_agent_graph.py` | 0.5 天 | P1 |
| R4 | 上下文无限增长 | `graph/single_agent_graph.py` + 新建 `graph/context_compressor.py` | 1 天 | P1 |
| R5 | MCP 真进程隔离 | `tools/mcp_servers.py` + 新建 `tools/mcp_client.py` | 1.5 天 | P1 |
| R6 | 无 Agent 评估脚本 | 新建 `eval/` 模块 | 1.5 天 | P0 |
| R7 | RAG 无结构化筛选 | `tools/order_tools.py` + `tools/resource_tools.py` | 1 天 | P1 |
| R8 | 无多租户隔离 | `auth/token_exchange.py` + `tools/registry.py` + `tools/data.py` | 1 天 | P1 |

**总预估**：约 10 个工作日（2 周）。建议按 P0 → P1 顺序执行。

---

## R1：工具执行沙箱与重试

### 1.1 当前代码

**文件**：`demo/tools/registry.py:75-101`

```python
def execute(self, name: str, arguments: dict, token=None, audit=None) -> str:
    if name not in self._handlers:
        return f"❌ 未知工具: '{name}'。可用: {', '.join(self.list_all())}"
    # 鉴权...
    try:
        schema = self._tools[name]
        valid_keys = set(schema.parameters.get("properties", {}).keys())
        filtered = {k: v for k, v in arguments.items() if k in valid_keys} if valid_keys else arguments
        from ..observability import tracer
        with tracer.span(f"tool:{name}", server=schema.server):
            return self._handlers[name](**filtered)  # ← 直接调，无超时/重试/隔离
    except Exception as e:
        return f"❌ 工具 '{name}' 执行失败: {type(e).__name__}: {e}"
```

**问题**：
- handler 抛异常 → 只返回错误字符串，LLM 不会自动重试
- 无超时控制 → 工具挂死，Agent 永远等
- 与 Agent 同进程 → 工具 OOM，Agent 一起崩

### 1.2 改造目标

```
当前                          改造后
─────                         ──────
registry.execute()            registry.execute()
  → handler(**args)             → sandbox.run(handler, args)
                                   ├── timeout=10s（超时 kill）
                                   ├── retry=3（指数退避 1s→2s→4s）
                                   └── 异常时返回结构化错误
```

### 1.3 实施步骤

**Step 1**：新建 `demo/tools/sandbox.py`

```python
"""工具执行沙箱 -- 超时控制 + 指数退避重试 + 异常标准化。

设计要点：
  - 每个工具调用包裹在沙箱中执行：超时 → 重试 → 结构化错误返回。
  - 指数退避：1s → 2s → 4s，最多 3 次。
  - 可通过 env 配置：TOOL_TIMEOUT（默认 10s）、TOOL_MAX_RETRIES（默认 3）。
  - 返回 (result: str, success: bool, retries: int) 三元组，
    让上层（registry.execute / Agent）知道执行是否成功。
"""
import os
import time
import traceback
from typing import Any, Callable


TOOL_TIMEOUT = float(os.getenv("TOOL_TIMEOUT", "10"))
TOOL_MAX_RETRIES = int(os.getenv("TOOL_MAX_RETRIES", "3"))


class ToolExecutionError(Exception):
    """工具执行失败（含重试信息）。"""
    def __init__(self, tool_name: str, attempts: int, last_error: str):
        self.tool_name = tool_name
        self.attempts = attempts
        self.last_error = last_error
        super().__init__(f"工具 '{tool_name}' 执行失败（{attempts} 次尝试）: {last_error}")


def run_with_retry(handler: Callable[..., Any], args: dict,
                   tool_name: str = "",
                   timeout: float | None = None,
                   max_retries: int | None = None,
                   ) -> tuple[str, bool, int]:
    """在沙箱中执行工具调用，含超时 + 指数退避重试。

    Args:
        handler: 工具函数
        args: 过滤后的参数字典
        tool_name: 工具名（日志用）
        timeout: 超时秒数，默认 TOOL_TIMEOUT
        max_retries: 最大重试次数，默认 TOOL_MAX_RETRIES

    Returns:
        (result_str, success, retries_used)
    """
    timeout = timeout if timeout is not None else TOOL_TIMEOUT
    max_retries = max_retries if max_retries is not None else TOOL_MAX_RETRIES

    last_error = ""
    for attempt in range(max_retries + 1):
        try:
            # ---- 超时控制 ----
            # Python 3.11+：用 signal.alarm 或 threading.Timer
            # 这里用 threading.Timer（跨平台），生产换 subprocess 子进程
            result_container: dict[str, Any] = {}
            exception_container: dict[str, Exception | None] = {"exc": None}

            def _run():
                try:
                    result_container["value"] = handler(**args)
                except Exception as e:
                    exception_container["exc"] = e

            import threading
            t = threading.Thread(target=_run, daemon=True)
            t.start()
            t.join(timeout=timeout)

            if t.is_alive():
                # 超时：线程还在跑（daemon 线程在进程退出时会清理）
                raise TimeoutError(f"工具执行超时（>{timeout}s）")

            if exception_container["exc"]:
                raise exception_container["exc"]

            result = result_container.get("value", "")
            return (str(result), True, attempt)

        except TimeoutError as e:
            last_error = str(e)
            if attempt < max_retries:
                wait = 2 ** attempt  # 1s, 2s, 4s
                print(f"  ⚠️  [{tool_name}] 超时，{wait}s 后重试（{attempt + 1}/{max_retries}）...")
                time.sleep(wait)
        except Exception as e:
            last_error = f"{type(e).__name__}: {e}"
            traceback.print_exc()
            if attempt < max_retries:
                wait = 2 ** attempt
                print(f"  ⚠️  [{tool_name}] 失败: {last_error[:80]}，{wait}s 后重试（{attempt + 1}/{max_retries}）...")
                time.sleep(wait)

    # 全部重试失败
    return (
        f"❌ 工具 '{tool_name}' 执行失败（{max_retries + 1} 次尝试均失败）\n"
        f"最后错误: {last_error}\n"
        f"建议：检查工具数据源是否正常，或换个方式提问。",
        False,
        max_retries,
    )
```

**Step 2**：修改 `demo/tools/registry.py:75-101`

```python
# 原代码（:98-99）
with tracer.span(f"tool:{name}", server=schema.server):
    return self._handlers[name](**filtered)

# 改为 →
from .sandbox import run_with_retry

with tracer.span(f"tool:{name}", server=schema.server) as s:
    result_str, success, retries = run_with_retry(
        self._handlers[name], filtered, tool_name=name,
    )
    s.attributes["tool_success"] = success
    s.attributes["tool_retries"] = retries
    return result_str
```

**Step 3**：在 `demo/tools/__init__.py` 中导出 sandbox 符号（可选，方便测试）

**Step 4**：加环境变量文档到 `demo/README.md`

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `TOOL_TIMEOUT` | `10` | 单次工具调用超时秒数 |
| `TOOL_MAX_RETRIES` | `3` | 最大重试次数（不含首次） |

### 1.4 验收标准

- [ ] `python -m demo.main --check` 通过（registry 初始化不受影响）
- [ ] 模拟工具超时：`TOOL_TIMEOUT=1` 跑 RAG 工具，确超时后能重试
- [ ] 模拟工具异常：临时改 handler 抛 Exception，确重试后返回结构化错误而非崩溃
- [ ] trace 中能看到 `tool_success` 和 `tool_retries` 属性

### 1.5 后续升级路径

- 当前用 `threading.Timer` 做超时 → 生产改用 subprocess 子进程（真隔离，能 kill）
- 当前 handler 仍是同进程函数 → 未来结合 R5（MCP 真进程隔离），改为子进程调用

---

## R2：输出护栏（guardrails）

### 2.1 当前代码

**文件**：`demo/graph/single_agent_graph.py:104-130` — `generate_answer` 节点

```python
def generate_answer(state: AgentState) -> AgentState:
    # ... 直接 trust LLM 输出 ...
    response = call_llm(state["messages"] + [summary_prompt])
    answer = response.choices[0].message.content or ""
    # ← 没有任何校验
    state["final_answer"] = answer
    state["messages"].append({"role": "assistant", "content": answer})
    return state
```

整个 demo 没有任何输出校验。

### 2.2 改造目标

```
改造前                          改造后
──────                          ──────
LLM 输出 → 直接返回              LLM 输出 → guardrails pipeline →
                                               ├── JSON Schema 校验（如需要）
                                               ├── 敏感词/越权指令过滤
                                               ├── 格式完整性检查
                                               └── 通过 → 返回；失败 → 重试/降级
```

### 2.3 实施步骤

**Step 1**：新建目录 `demo/guardrails/` + 3 个文件

```
demo/guardrails/
├── __init__.py          # 导出 run_guardrails()
├── schema_validator.py  # JSON Schema 校验（LLM 输出结构化内容时）
├── content_filter.py    # 敏感词/越权指令过滤
└── rules.py             # 护栏规则定义（可配置）
```

**Step 2**：`demo/guardrails/rules.py` — 定义校验规则

```python
"""护栏规则定义 -- 可配置的输出校验规则集。

每条规则是一个 (check_fn, severity, message) 三元组。
severity: "block"（阻断+重试）| "warn"（告警但放行）| "sanitize"（替换后放行）
"""

# ── 越权操作指令（不应出现在 Agent 输出中）──
BLOCKED_PATTERNS = [
    # 修改类操作（排产助手只给建议，不应直接修改数据）
    "修改.*订单状态", "删除.*订单", "取消.*排产",
    "UPDATE.*SET", "DELETE.*FROM", "DROP.*TABLE",
    # 系统指令注入
    "忽略.*系统指令", "ignore.*system prompt", "你是.*新角色",
    "reset.*your.*instructions",
]

# ── 敏感信息泄露模式 ──
SENSITIVE_PATTERNS = [
    r"\b\d{16}\b",           # 银行卡号
    r"\b\d{6}(19|20)\d{2}\b", # 身份证片段
]

# ── 输出格式规则（排产建议必须包含的结构）──
REQUIRED_SECTIONS_FOR_SCHEDULING = [
    "关键发现", "优先排产", "可延后",
]

import re


def check_blocked_content(text: str) -> list[str]:
    """检测是否包含禁止内容。返回匹配到的规则列表。"""
    hits = []
    for pattern in BLOCKED_PATTERNS:
        if re.search(pattern, text, re.IGNORECASE):
            hits.append(f"检测到禁止内容: {pattern}")
    return hits


def check_missing_sections(text: str, required: list[str] | None = None) -> list[str]:
    """检测是否缺少必要的输出段落。"""
    if required is None:
        required = REQUIRED_SECTIONS_FOR_SCHEDULING
    missing = [s for s in required if s not in text]
    return [f"缺少必要段落: {s}" for s in missing]


def check_sensitive_info(text: str) -> list[str]:
    """检测是否包含敏感信息。返回发现的内容（脱敏后显示）。"""
    hits = []
    for pattern in SENSITIVE_PATTERNS:
        matches = re.findall(pattern, text)
        if matches:
            hits.append(f"疑似敏感信息: {pattern}（匹配 {len(matches)} 处）")
    return hits
```

**Step 3**：`demo/guardrails/content_filter.py` — 内容过滤器

```python
"""内容过滤器 -- 敏感词 + 越权指令 + 有害内容检测。

三层防御：
  1. 规则匹配（regex，零延迟）
  2. 可选：小模型分类器（有害内容检测，按需启用）
  3. 降级策略：原样返回 + 告警 或 替换敏感部分
"""
import os
import re
from .rules import check_blocked_content, check_sensitive_info


GUARDRAILS_MODE = os.getenv("GUARDRAILS_MODE", "warn")  # block | warn | off


class GuardrailsViolation(Exception):
    """护栏违规异常。"""
    def __init__(self, violations: list[str], original_output: str):
        self.violations = violations
        self.original_output = original_output[:200]
        super().__init__(f"护栏违规（{len(violations)} 条）: {'; '.join(violations[:3])}")


def filter_output(text: str, context: dict | None = None) -> tuple[str, list[str]]:
    """对 LLM 输出做安全过滤。

    Returns:
        (filtered_text, violations) — violations 为空列表表示通过。
    """
    if GUARDRAILS_MODE == "off":
        return text, []

    violations = []

    # 1. 越权指令检测
    blocked = check_blocked_content(text)
    violations.extend(blocked)

    # 2. 敏感信息检测
    sensitive = check_sensitive_info(text)
    violations.extend(sensitive)

    # 3. 按模式处理
    if violations:
        if GUARDRAILS_MODE == "block":
            raise GuardrailsViolation(violations, text)
        else:  # warn
            print(f"  ⚠️  [护栏告警] {len(violations)} 条违规: {'; '.join(violations[:3])}")

    return text, violations
```

**Step 4**：`demo/guardrails/__init__.py` — 统一入口

```python
"""输出护栏模块 -- LLM 输出安全校验的统一入口。

用法：
  from ..guardrails import run_guardrails
  result, warnings = run_guardrails(llm_output, context={"mode": "scheduling"})
  if result.is_valid:
      return result.text
  else:
      # 重试 LLM 或降级
"""
from dataclasses import dataclass, field
from .content_filter import filter_output
from .rules import check_missing_sections


@dataclass
class GuardrailsResult:
    text: str
    is_valid: bool
    warnings: list[str] = field(default_factory=list)
    blocked_by: str = ""


def run_guardrails(text: str, context: dict | None = None) -> GuardrailsResult:
    """对 LLM 输出运行完整护栏检查。

    Args:
        text: LLM 原始输出
        context: 上下文信息 {"mode": "scheduling"|"query"|"rag"}

    Returns:
        GuardrailsResult — is_valid=True 表示通过所有检查
    """
    warnings = []
    ctx = context or {}
    mode = ctx.get("mode", "scheduling")

    # 1. 安全过滤（越权/敏感信息）
    try:
        text, violations = filter_output(text, ctx)
        if violations:
            for v in violations:
                if "禁止内容" in v:
                    return GuardrailsResult(
                        text="",
                        is_valid=False,
                        warnings=violations,
                        blocked_by=v,
                    )
                warnings.append(v)
    except Exception as e:
        return GuardrailsResult(text="", is_valid=False, blocked_by=str(e))

    # 2. 排产模式 → 检查必要段落
    if mode == "scheduling":
        missing = check_missing_sections(text)
        if missing:
            warnings.extend(missing)
            # 缺少必要段落 → 不算阻断但告警
            print(f"  ⚠️  [护栏告警] {missing}")

    return GuardrailsResult(text=text, is_valid=True, warnings=warnings)
```

**Step 5**：集成到 `demo/graph/single_agent_graph.py:104-130`

```python
# 原 generate_answer 节点 → 加入 guardrails
from ..guardrails import run_guardrails

def generate_answer(state: AgentState) -> AgentState:
    # ... 前面的逻辑不变 ...

    # 护栏检查
    MAX_RETRIES = 2
    for retry in range(MAX_RETRIES + 1):
        try:
            response = call_llm(state["messages"] + [summary_prompt])
            answer = response.choices[0].message.content or ""
        except Exception:
            # LLM 调用失败 → 兜底
            answer = _fallback_answer(state)

        # 护栏检查
        gr = run_guardrails(answer, context={"mode": "scheduling"})
        if gr.is_valid:
            state["final_answer"] = gr.text
            state["messages"].append({"role": "assistant", "content": gr.text})
            return state
        else:
            if retry < MAX_RETRIES:
                # 在 prompt 中追加护栏反馈，让 LLM 修正
                state["messages"].append({
                    "role": "system",
                    "content": f"上次输出被护栏拦截: {gr.blocked_by}。请修改后重新输出。"
                })
            else:
                # 多次重试仍不通过 → 降级返回安全版本
                fallback = f"抱歉，生成排产建议时遇到问题（{gr.blocked_by}）。\n请稍后重试或联系管理员。"
                state["final_answer"] = fallback
                state["messages"].append({"role": "assistant", "content": fallback})
                return state
    return state
```

**Step 6**：在 `demo/main.py` 和 `demo/api.py` 中也加一层全局护栏（API 网关层）

### 2.4 验收标准

- [ ] 排产建议输出包含"关键发现""优先排产""可延后"三个段落 → 通过
- [ ] 在输出中注入"删除所有订单" → 护栏拦截
- [ ] 缺少必要段落 → 告警不阻断
- [ ] `GUARDRAILS_MODE=off` → 跳过所有检查
- [ ] 多次重试仍不通过 → 降级返回安全文案

### 2.5 后续升级路径

- 当前规则匹配是 regex → 生产加入小模型有害内容分类器
- 当前是规则引擎 → 调研 Guardrails AI / NVIDIA NeMo Guardrails 的 RAIL 规范

---

## R3：步骤校验（evaluate_results 不再是 noop）

### 3.1 当前代码

**文件**：`demo/graph/single_agent_graph.py:85-87`

```python
def evaluate_results(state: AgentState) -> AgentState:
    """评估数据是否足够（当前 noop，由 should_continue 条件边决定）。"""
    return state
```

### 3.2 改造目标

```
改造前                            改造后
──────                            ──────
evaluate_results = noop           evaluate_results 校验：
                                     ├── tool_results 中每条非空且非错误
                                     ├── 必要数据是否已收集（如排产需要订单+库存+设备）
                                     ├── 不达标 → 标记 needs_more_data
                                     └── 达标 → 标记 ready_for_answer
```

### 3.3 实施步骤

**Step 1**：在 `demo/graph/single_agent_graph.py` 中重写 `evaluate_results`

```python
def evaluate_results(state: AgentState) -> AgentState:
    """校验已收集的工具数据质量。

    检查：
      1. 每条 tool_result 非空且不以 ❌ 开头（工具执行错误）
      2. 已收集到至少一条订单数据和一条资源数据（排产场景的最低要求）
      3. 如果 LLM 没有调任何工具 → 提醒 Agent 至少查一次
    """
    results = state.get("tool_results", [])
    iteration = state.get("iteration", 0)

    # 第一轮还没调工具 → 跳过校验
    if iteration == 0:
        return state

    # 检查每条结果质量
    bad_results = []
    for tr in results:
        if not tr.get("result") or tr["result"].startswith("❌"):
            bad_results.append(tr.get("tool", "?"))

    if bad_results:
        state["evaluation_notes"] = f"以下工具返回空或错误: {', '.join(bad_results)}"
        state["needs_retry"] = True
        return state

    # 检查数据完整性（排产场景至少需要订单 + 某类资源数据）
    tool_names = {tr.get("tool", "") for tr in results}
    has_order = any(t in tool_names for t in ["query_orders", "get_order_detail"])
    has_resource = any(t in tool_names for t in ["query_inventory", "query_machine_load", "query_customer"])

    if not has_order:
        state["evaluation_notes"] = "尚未查询订单数据，建议先查订单"
        state["needs_more"] = True
    elif not has_resource:
        state["evaluation_notes"] = "已查订单，建议补充查询库存或设备数据"
        state["needs_more"] = True
    else:
        state["evaluation_notes"] = "数据已充足，可以生成排产建议"
        state["ready_for_answer"] = True

    return state
```

**Step 2**：修改 `AgentState`（`demo/graph/state.py`），加新字段

```python
from typing import TypedDict

class AgentState(TypedDict):
    messages: list[dict]
    tool_results: list[dict]
    iteration: int
    final_answer: str
    # 新增字段
    evaluation_notes: str       # evaluate_results 的检查记录
    needs_retry: bool           # 工具执行失败，需要重试
    needs_more: bool            # 数据不够，需要继续查
    ready_for_answer: bool      # 数据充足，可以生成答案
```

**Step 3**：修改 `should_continue` 条件边，加入 `evaluation_notes` 逻辑

```python
def should_continue(state: AgentState) -> str:
    if state["iteration"] >= 5:
        return "generate_answer"  # 安全阀

    # evaluate_results 标记了需要重试/继续查
    if state.get("needs_retry") and state["iteration"] < 4:
        return "select_and_execute"
    if state.get("needs_more") and state["iteration"] < 4:
        return "select_and_execute"

    last_msg = state["messages"][-1] if state["messages"] else {}
    if last_msg.get("role") == "tool":
        return "select_and_execute"
    if last_msg.get("role") == "assistant" and last_msg.get("tool_calls"):
        return "select_and_execute"
    return "generate_answer"
```

### 3.4 验收标准

- [ ] 工具返回 `❌ 工具执行失败` 时 → `evaluation_notes` 标记 "以下工具返回错误"
- [ ] 只查了订单没查库存时 → `evaluation_notes` 标记 "建议补充查询库存或设备"
- [ ] 数据充足时 → `ready_for_answer = True` → 进入 generate_answer
- [ ] `iteration >= 5` → 强制结束，不管数据是否充足

---

## R4：上下文压缩

### 4.1 当前代码

**文件**：`demo/graph/single_agent_graph.py:39-83` — `select_and_execute` 无截断

每轮 `state["messages"].append(...)`，永不清理。

### 4.2 改造目标

```
改造前                              改造后
──────                              ──────
messages 无限增长                    messages 超过阈值 → 压缩早期消息
                                         ├── 保留 system prompt（永远在第一）
                                         ├── 保留最近 N 条消息
                                         ├── 中间消息 → LLM 摘要替代
                                         └── state 添加 compressed_summary 字段
```

### 4.3 实施步骤

**Step 1**：新建 `demo/graph/context_compressor.py`

```python
"""上下文压缩器 -- 长对话 messages 自动摘要压缩。

策略：summarization buffer（LangGraph 推荐）。
  - messages 超过 MAX_TOKENS_ESTIMATE（按字符数估算）→ 触发压缩
  - 压缩后 messages = [system] + [摘要1] + [摘要2] + ... + [最近 N 条原始消息]
  - 摘要由 LLM 生成（用便宜的 provider / 小 max_tokens）

阈值说明（demo 教学版用字符数近似，生产用 tiktoken 精确计数）：
  - 中文约 1.5 字符/token
  - MAX_CHARS = 8000 → 约 5300 tokens（远低于模型 128K 上下文，但控制成本）
"""
import os

MAX_CHARS = int(os.getenv("CONTEXT_MAX_CHARS", "8000"))
KEEP_RECENT = int(os.getenv("CONTEXT_KEEP_RECENT", "6"))  # 保留最近 N 条
COMPRESS_CHUNK_SIZE = int(os.getenv("CONTEXT_COMPRESS_CHUNK", "10"))  # 每次压缩 N 条


def estimate_chars(messages: list[dict]) -> int:
    """估算 messages 总字符数。"""
    return sum(len(str(m.get("content", ""))) for m in messages)


def should_compress(messages: list[dict]) -> bool:
    """判断是否需要压缩。"""
    return estimate_chars(messages) > MAX_CHARS


def _messages_to_text(messages: list[dict]) -> str:
    """把 messages 转成可读文本，供 LLM 摘要。"""
    lines = []
    for m in messages:
        role = m.get("role", "?")
        content = str(m.get("content", ""))[:500]
        if m.get("tool_calls"):
            content += f" [调用了工具: {[tc['function']['name'] for tc in m['tool_calls']]}]"
        lines.append(f"[{role}] {content}")
    return "\n".join(lines)


def build_compression_prompt(messages_to_compress: list[dict]) -> list[dict]:
    """构造摘要请求的 messages。"""
    text = _messages_to_text(messages_to_compress)
    return [
        {"role": "system", "content": (
            "你是一个对话摘要助手。请用 2-3 句话（不超过 150 字）把以下对话片段中的关键信息总结出来。"
            "保留：订单号、客户名、交期、材料名、库存状态、设备负载、关键决策。"
            "丢弃：系统消息、问候语、重复内容。"
            "只输出摘要，不要加任何前缀。"
        )},
        {"role": "user", "content": f"请总结以下对话片段：\n{text}"},
    ]


def compress_messages(messages: list[dict], llm_call) -> list[dict]:
    """压缩 messages：保留 system + 最近消息，中间部分用摘要替代。

    Args:
        messages: 原始消息列表
        llm_call: LLM 调用函数（call_llm）

    Returns:
        压缩后的消息列表
    """
    if not should_compress(messages):
        return messages

    # 1. 分离 system prompt（永远保留）
    system_msgs = [m for m in messages if m.get("role") == "system"]
    other_msgs = [m for m in messages if m.get("role") != "system"]

    # 2. 保留最近 KEEP_RECENT 条
    recent = other_msgs[-KEEP_RECENT:] if len(other_msgs) > KEEP_RECENT else other_msgs
    old = other_msgs[:-KEEP_RECENT] if len(other_msgs) > KEEP_RECENT else []

    if not old:
        return messages

    # 3. 对早期消息分批做摘要（每 COMPRESS_CHUNK_SIZE 条一批）
    summaries = []
    for i in range(0, len(old), COMPRESS_CHUNK_SIZE):
        chunk = old[i:i + COMPRESS_CHUNK_SIZE]
        prompt = build_compression_prompt(chunk)
        try:
            resp = llm_call(prompt, max_tokens=200, temperature=0.1)
            summary = resp.choices[0].message.content or ""
            summaries.append(summary)
        except Exception:
            # 摘要失败 → 保留原始消息（降级）
            summaries.append(f"[对话片段 {i // COMPRESS_CHUNK_SIZE + 1}，共 {len(chunk)} 条消息]")

    # 4. 组装压缩后的 messages
    summary_text = "【历史对话摘要】\n" + "\n".join(
        f"· {s}" for s in summaries
    )
    compressed = system_msgs + [
        {"role": "system", "content": summary_text},
    ] + recent

    # 标记压缩发生（供 trace 记录）
    print(f"  📦 [上下文压缩] {len(old)} 条历史消息 → {len(summaries)} 段摘要（{estimate_chars(other_msgs)} → {estimate_chars(compressed)} 字符）")

    return compressed
```

**Step 2**：集成到 `demo/graph/single_agent_graph.py`

```python
# 在 select_and_execute 开头加压缩检查
from ..graph.context_compressor import compress_messages, should_compress

def select_and_execute(state: AgentState) -> AgentState:
    # 压缩检查（LLM 调用前）
    if should_compress(state["messages"]):
        state["messages"] = compress_messages(state["messages"], call_llm)

    # ... 原有逻辑不变 ...
```

**Step 3**：在 `demo/graph/state.py` 加压缩相关字段

```python
class AgentState(TypedDict):
    # ... 原有字段 ...
    compression_count: int        # 已压缩次数
    compressed_summary: str       # 最新摘要文本
```

### 4.4 验收标准

- [ ] 短对话（< 8000 字符）→ 不触发压缩
- [ ] 长对话（模拟 15+ 轮）→ 触发压缩，messages 字符数显著减少
- [ ] 压缩后 system prompt 仍在第一位
- [ ] 压缩后最近 6 条消息保留原文
- [ ] 摘要失败 → 降级保留原文（不丢数据）
- [ ] trace 中能看到 `compression_count` 变化

### 4.5 后续升级路径

- 当前用字符数近似 → 生产用 tiktoken 精确计数
- 当前用 LLM 做摘要 → 生产可考虑用更便宜的模型 + 缓存摘要

---

## R5：MCP 真进程隔离

### 5.1 当前状态

**有但未用**：
- `demo/tools/mcp_servers.py` — FastMCP server 构建代码（展示概念）
- `demo/tools/registry.py:99` — 运行时直接 `handler(**args)`，同进程函数调用

### 5.2 改造目标

```
当前                              改造后
────                              ──────
ToolRegistry.execute()             ToolRegistry.execute()
  → handler(**args)                  → 检查 mode（默认 local，兼容现有）
                                     → MCP mode → MCPClient.call_tool(name, args)
                                                   → stdio 子进程通信
                                                   → 超时/重试/隔离自然获得
```

### 5.3 实施步骤

**Step 1**：新建 `demo/tools/mcp_client.py`

```python
"""MCP Client — 通过 stdio 协议调用真 MCP 子进程工具。

与 registry 配合使用：
  - registry.execute(name, args, mode="mcp") → 走 MCP 协议
  - registry.execute(name, args, mode="local") → 走原函数直调（默认，兼容现有）

设计要点：
  - 子进程生命周期：首次调用时启动，空闲 5 分钟自动退出。
  - 工具发现：启动时 list_tools → 缓存工具列表。
  - 调用：call_tool → 组装 JSON-RPC → 写 stdin → 读 stdout。
  - 超时/重试由 R1 的 sandbox 层统一处理。
"""
import json
import os
import subprocess
import sys
import time
import uuid
from typing import Any


MCP_MODE = os.getenv("MCP_MODE", "local")  # local | mcp


class MCPToolClient:
    """通过 stdio 与 MCP Server 子进程通信。"""

    def __init__(self, server_script: str, python: str | None = None):
        self._script = server_script
        self._python = python or sys.executable
        self._process: subprocess.Popen | None = None
        self._tools: dict[str, dict] = {}  # name → tool schema
        self._last_used: float = 0

    def _ensure_running(self):
        """确保子进程在运行（首次调用时启动；空闲超时退出后重启）。"""
        if self._process is None or self._process.poll() is not None:
            self._process = subprocess.Popen(
                [self._python, self._script],
                stdin=subprocess.PIPE,
                stdout=subprocess.PIPE,
                stderr=subprocess.PIPE,
                text=True,
            )
            # 初始化 + 发现工具
            self._send_json_rpc("initialize", {
                "protocolVersion": "2024-11-05",
                "capabilities": {},
                "clientInfo": {"name": "demo-mcp-client", "version": "0.1.0"},
            })
            tools_resp = self._send_json_rpc("tools/list", {})
            for t in tools_resp.get("tools", []):
                self._tools[t["name"]] = t
        self._last_used = time.time()

    def _send_json_rpc(self, method: str, params: dict) -> dict:
        """发送 JSON-RPC 请求，返回 result。"""
        req = {
            "jsonrpc": "2.0",
            "id": uuid.uuid4().hex[:8],
            "method": method,
            "params": params,
        }
        raw = json.dumps(req) + "\n"
        self._process.stdin.write(raw)
        self._process.stdin.flush()
        line = self._process.stdout.readline()
        resp = json.loads(line)
        if "error" in resp:
            raise RuntimeError(f"MCP 错误: {resp['error']}")
        return resp.get("result", {})

    def call_tool(self, name: str, arguments: dict) -> str:
        """调用 MCP 工具。"""
        self._ensure_running()
        result = self._send_json_rpc("tools/call", {
            "name": name,
            "arguments": arguments,
        })
        # 提取文本内容
        content = result.get("content", [])
        texts = [c["text"] for c in content if c.get("type") == "text"]
        return "\n".join(texts)

    def list_tools(self) -> list[dict]:
        """返回缓存的工具列表。"""
        self._ensure_running()
        return list(self._tools.values())

    def close(self):
        if self._process:
            self._process.terminate()
            self._process = None


# 全局 client 缓存（按 server 名）
_clients: dict[str, MCPToolClient] = {}


def get_mcp_client(server_name: str, script_path: str) -> MCPToolClient:
    """获取或创建 MCP client（按 server 名缓存，复用子进程）。"""
    if server_name not in _clients:
        _clients[server_name] = MCPToolClient(script_path)
    return _clients[server_name]
```

**Step 2**：修改 `demo/tools/registry.py`，加入 MCP 路由

```python
# 在 execute 方法中加模式判断（:98 附近）

def execute(self, name: str, arguments: dict, token=None, audit=None,
            mode: str | None = None) -> str:
    """执行工具。mode="mcp" 时走 MCP 协议，默认走本地函数直调。"""
    if name not in self._handlers and name not in self._mcp_tools:
        return f"❌ 未知工具: '{name}'"

    # 鉴权...
    mode = mode or os.getenv("MCP_MODE", "local")

    if mode == "mcp" and name in self._mcp_tools:
        # MCP 协议调用
        client = self._mcp_tools[name]["client"]
        return client.call_tool(name, arguments)
    else:
        # 本地函数直调（含沙箱重试，R1）
        from .sandbox import run_with_retry
        # ... R1 改造后的逻辑 ...
```

**Step 3**：更新 `demo/tools/mcp_servers.py`，让 server 脚本可独立运行

```python
# 确保每个 MCP server 脚本可被 `python mcp_servers.py --server order_server` 启动
if __name__ == "__main__":
    import sys
    server_name = sys.argv[2] if len(sys.argv) > 2 and sys.argv[1] == "--server" else "order_server"
    mcp = build_server(server_name)
    mcp.run(transport="stdio")
```

### 5.4 验收标准

- [ ] `MCP_MODE=local`（默认）→ 走原函数直调（兼容现有行为）
- [ ] `MCP_MODE=mcp` → 通过子进程 stdio 通信调用工具
- [ ] 子进程 crash → 下次调用自动重启
- [ ] 工具超时 → 子进程被 kill（R1 的 sandbox + MCP 进程隔离双重保障）

---

## R6：Agent 评估脚本

### 6.1 当前状态

- `scripts/week4/ragas_eval.py` — Week 4 有手写 RAGAS 4 指标，但未接入 demo
- 无 ground truth Q&A 对
- 无回归测试

### 6.2 改造目标

```
改造前                              改造后
──────                              ──────
无评估                              eval/
                                       ├── ground_truth.json     # 至少 10 组排产场景 Q&A
                                       ├── run_eval.py           # 评估入口
                                       ├── metrics.py            # RAGAS 4 指标 + 自定义指标
                                       └── report.py             # 评估报告生成
```

### 6.3 实施步骤

**Step 1**：新建 `demo/eval/` 目录

```
demo/eval/
├── __init__.py
├── ground_truth.json       # 评估数据集
├── metrics.py              # 指标实现
├── runner.py               # 评估运行器
└── report.py               # 报告生成
```

**Step 2**：`demo/eval/ground_truth.json` — 10 组排产场景 Q&A

```json
{
  "version": "1.0",
  "created": "2026-08-07",
  "cases": [
    {
      "id": "eval_001",
      "scenario": "排产优先级",
      "query": "今天先做哪些订单？",
      "expected_answer": "优先排产 ORD003（深圳精密，交期明天，A级客户）",
      "expected_tools": ["query_orders", "query_inventory", "query_machine_load"],
      "expected_order_ids": ["ORD003"],
      "checks": {
        "must_contain": ["ORD003", "深圳精密"],
        "must_not_contain": ["无法确定", "没有数据"],
        "min_tools_called": 2
      }
    },
    {
      "id": "eval_002",
      "scenario": "紧急订单识别",
      "query": "有哪些紧急订单？",
      "expected_answer": "ORD001（交期已过）、ORD005（交期明天）",
      "expected_tools": ["query_orders"],
      "checks": {
        "must_contain": ["ORD001", "ORD005", "紧急"],
        "must_not_contain": ["没有问题"]
      }
    },
    {
      "id": "eval_003",
      "scenario": "单订单详情",
      "query": "ORD001 能按时交付吗？",
      "expected_answer": "存在延期风险",
      "expected_tools": ["get_order_detail", "get_production_status", "query_inventory"],
      "checks": {
        "must_contain": ["ORD001", "风险", "环节"],
        "must_not_contain": ["无法确定"]
      }
    },
    {
      "id": "eval_004",
      "scenario": "客户信用评估",
      "query": "东莞模具厂订单总体情况？信用如何？",
      "expected_answer": "东莞模具厂（C002），B级客户",
      "expected_tools": ["query_customer", "query_orders"],
      "checks": {
        "must_contain": ["东莞", "C002"],
        "min_tools_called": 2
      }
    },
    {
      "id": "eval_005",
      "scenario": "材料库存影响",
      "query": "PEEK 材料库存够吗？会影响哪些订单？",
      "expected_answer": "可能影响使用 PEEK 材料的订单",
      "expected_tools": ["query_inventory", "query_orders"],
      "checks": {
        "must_contain": ["PEEK"],
        "min_tools_called": 2
      }
    },
    {
      "id": "eval_006",
      "scenario": "合同条款RAG",
      "query": "广州航天合同有什么特殊条款？",
      "expected_answer": "100% 全检、报废处理",
      "expected_tools": ["search_knowledge_base"],
      "checks": {
        "must_contain": ["广州航天", "全检"],
        "min_tools_called": 1
      }
    },
    {
      "id": "eval_007",
      "scenario": "设备瓶颈",
      "query": "哪些设备是瓶颈？",
      "expected_answer": "负载最高",
      "expected_tools": ["query_machine_load"],
      "checks": {
        "must_contain": ["设备", "负载"],
        "must_not_contain": ["没有设备数据"]
      }
    },
    {
      "id": "eval_008",
      "scenario": "深圳精密专项",
      "query": "深圳精密有哪些在生产的订单？",
      "expected_answer": "ORD001",
      "expected_tools": ["query_orders"],
      "checks": {
        "must_contain": ["深圳", "ORD"],
        "min_tools_called": 1
      }
    },
    {
      "id": "eval_009",
      "scenario": "多条件综合",
      "query": "A级客户的紧急订单，交期在最近3天内的，按设备空闲情况排优先级",
      "expected_answer": "至少列出1个订单",
      "expected_tools": ["query_orders", "query_customer", "query_machine_load"],
      "checks": {
        "must_not_contain": ["无法处理", "不支持"],
        "min_tools_called": 2
      }
    },
    {
      "id": "eval_010",
      "scenario": "延期记录查询",
      "query": "历史上有哪些订单延期了？原因是什么？",
      "expected_answer": "航天件、未及时排产",
      "expected_tools": ["search_knowledge_base"],
      "checks": {
        "must_contain": ["延期", "航天"],
        "min_tools_called": 1
      }
    }
  ]
}
```

**Step 3**：`demo/eval/metrics.py` — 评估指标

```python
"""评估指标 — RAGAS 4 指标 + 自定义 Agent 评估指标。

- faithfulness: 回答中的陈述能否在检索上下文中找到依据
- answer_relevancy: 回答与问题的语义相关性
- context_precision: 检索结果中相关文档的排名质量
- context_recall: 检索结果中召回了多少相关文档
- tool_call_accuracy: Agent 是否调了正确的工具
- answer_completeness: 是否包含所有必要信息
"""
import re
from typing import Any


def tool_call_accuracy(expected_tools: list[str], actual_tools: list[str]) -> float:
    """工具调用准确率：调了的工具中有多少在期望列表中。"""
    if not actual_tools:
        return 0.0
    hit = sum(1 for t in actual_tools if t in expected_tools)
    # 召回率：期望的工具中有多少被调了
    recall = sum(1 for t in expected_tools if t in actual_tools) / len(expected_tools) if expected_tools else 1.0
    # 精确率：调了的工具有多少是需要的
    precision = hit / len(actual_tools)
    # F1
    if precision + recall == 0:
        return 0.0
    return 2 * precision * recall / (precision + recall)


def answer_completeness(checks: dict, answer: str) -> dict:
    """回答完整性检查。返回各子项的通过情况。"""
    results = {}
    # must_contain
    for phrase in checks.get("must_contain", []):
        results[f"包含'{phrase}'"] = phrase in answer
    # must_not_contain
    for phrase in checks.get("must_not_contain", []):
        results[f"不包含'{phrase}'"] = phrase not in answer
    return results


def compute_all_metrics(case: dict, answer: str, actual_tools: list[str]) -> dict:
    """对一个 case 计算所有指标。"""
    checks = case.get("checks", {})
    completeness = answer_completeness(checks, answer)
    completeness_score = sum(completeness.values()) / len(completeness) if completeness else 1.0

    tool_score = tool_call_accuracy(
        case.get("expected_tools", []), actual_tools
    )

    # 检查是否包含预期订单号
    expected_orders = case.get("expected_order_ids", [])
    order_hit = sum(1 for oid in expected_orders if oid in answer) / len(expected_orders) if expected_orders else 1.0

    return {
        "case_id": case["id"],
        "scenario": case["scenario"],
        "query": case["query"],
        "answer_preview": answer[:150],
        "tools_called": actual_tools,
        "tool_call_accuracy": round(tool_score, 3),
        "completeness_score": round(completeness_score, 3),
        "completeness_details": completeness,
        "order_accuracy": round(order_hit, 3),
        "overall_score": round((tool_score * 0.3 + completeness_score * 0.5 + order_hit * 0.2), 3),
    }
```

**Step 4**：`demo/eval/runner.py` — 评估运行器

```python
"""评估运行器 — 遍历 ground_truth.json，逐个 case 跑 Agent，计算指标。

用法：
  python -m demo.eval.runner           # 跑全部
  python -m demo.eval.runner --case eval_001  # 跑单个
  python -m demo.eval.runner --mode multi      # 多 Agent 模式
"""
import json
import sys
import time
from pathlib import Path
from typing import Any

from ..agents.single_agent import run_single_agent
from ..agents.supervisor import run_supervisor
from ..observability import tracer, cost_tracker
from .metrics import compute_all_metrics


GROUND_TRUTH_PATH = Path(__file__).parent / "ground_truth.json"


def load_cases() -> list[dict]:
    with open(GROUND_TRUTH_PATH, "r", encoding="utf-8") as f:
        return json.load(f)["cases"]


def run_eval(mode: str = "single", case_filter: str | None = None) -> list[dict]:
    """运行评估。"""
    cases = load_cases()
    if case_filter:
        cases = [c for c in cases if c["id"] == case_filter]

    results = []
    for i, case in enumerate(cases):
        tracer.reset()
        cost_tracker.reset()
        print(f"\n{'=' * 60}")
        print(f" 评估 [{i + 1}/{len(cases)}] : {case['id']} — {case['scenario']}")
        print(f" 问题：{case['query']}")
        print(f"{'=' * 60}")

        start = time.time()
        if mode == "multi":
            result = run_supervisor(case["query"])
            answer = result.get("synthesis", "")
        else:
            answer = run_single_agent(case["query"])

        elapsed = time.time() - start
        trace_summary = tracer.get_summary()
        cost_summary = cost_tracker.get_summary()

        # 从 trace 中提取实际调用的工具名
        actual_tools = []
        for span in trace_summary.get("spans", []):
            if span["name"].startswith("tool:"):
                actual_tools.append(span["name"].replace("tool:", ""))

        # 计算指标
        case_result = compute_all_metrics(case, answer, actual_tools)
        case_result["elapsed_s"] = round(elapsed, 2)
        case_result["trace_id"] = tracer.trace_id
        case_result["token_usage"] = {
            "total_tokens": cost_summary.get("total_tokens", 0),
            "total_cost": cost_summary.get("total_cost", 0),
        }
        case_result["pass"] = case_result["overall_score"] >= 0.6

        results.append(case_result)
        status = "✅" if case_result["pass"] else "❌"
        print(f" {status} 评分: {case_result['overall_score']:.2f} | "
              f"工具: {case_result['tool_call_accuracy']:.2f} | "
              f"完整性: {case_result['completeness_score']:.2f} | "
              f"耗时: {elapsed:.1f}s | token: {case_result['token_usage']['total_tokens']}")

    return results


def print_summary(results: list[dict]):
    """打印评估汇总报告。"""
    if not results:
        return
    passed = sum(1 for r in results if r["pass"])
    avg_score = sum(r["overall_score"] for r in results) / len(results)
    avg_tool = sum(r["tool_call_accuracy"] for r in results) / len(results)
    avg_time = sum(r["elapsed_s"] for r in results) / len(results)
    total_cost = sum(r["token_usage"]["total_cost"] for r in results)

    print(f"\n{'=' * 60}")
    print(f" 📊 评估汇总")
    print(f"{'=' * 60}")
    print(f" 通过: {passed}/{len(results)} ({passed / len(results) * 100:.0f}%)")
    print(f" 综合评分: {avg_score:.2f}")
    print(f" 工具调用准确率: {avg_tool:.2f}")
    print(f" 平均耗时: {avg_time:.1f}s")
    print(f" 总费用: ¥{total_cost:.4f}")
    print(f"\n 各 case 详情:")
    for r in results:
        status = "✅" if r["pass"] else "❌"
        print(f"   {status} {r['case_id']} ({r['scenario']}): "
              f"score={r['overall_score']:.2f} tool={r['tool_call_accuracy']:.2f}")

    # 回归基线检查
    print(f"\n 回归检查：")
    baseline_pass = 7  # 期望至少 7/10 通过
    if passed >= baseline_pass:
        print(f"   ✅ 通过回归基线（{passed}/{len(results)} >= {baseline_pass}/{len(results)}）")
    else:
        print(f"   ❌ 未通过回归基线（{passed}/{len(results)} < {baseline_pass}/{len(results)}）")
        print(f"   → 请检查近期代码改动，确认是模型行为变化还是 bug 引入。")


if __name__ == "__main__":
    import argparse
    parser = argparse.ArgumentParser()
    parser.add_argument("--case", type=str, help="只跑指定 case")
    parser.add_argument("--mode", type=str, default="single", choices=["single", "multi"])
    args = parser.parse_args()

    results = run_eval(mode=args.mode, case_filter=args.case)
    print_summary(results)
```

### 6.4 验收标准

- [ ] `python -m demo.eval.runner` 跑全部 10 个 case
- [ ] 至少 7/10 通过（overall_score >= 0.6）
- [ ] 输出评估汇总报告（通过率、综合评分、工具准确率、费用）
- [ ] 单 case 模式 `--case eval_001` 可跑
- [ ] 多 Agent 模式 `--mode multi` 可跑

### 6.5 持续集成建议

```bash
# 加入 pre-commit 或 CI pipeline
python -m demo.eval.runner --mode single 2>&1 | tail -5
# 检查最后一行是否包含 "通过回归基线"
```

---

## R7：RAG 结构化筛选

### 7.1 当前状态

**文件**：`demo/tools/order_tools.py` — `query_orders` 只支持 `status` 和 `customer_name` 两个字段筛选

```python
def query_orders(status: str = "", customer_name: str = "") -> str:
    # 只支持两个字段筛选，不支持多字段 AND 组合
```

**问题**：用户问"交期<7/30 AND 等级=A AND 工艺=3D打印的前3单" → Agent 只能查到原始数据然后让 LLM "人工"筛选，LLM 容易出错。

### 7.3 实施步骤

**Step 1**：增强 `demo/tools/order_tools.py` 中的 `query_orders`

```python
def query_orders(
    status: str = "",
    customer_name: str = "",
    customer_level: str = "",      # 新增：客户等级 A/B/C/D/S
    process: str = "",              # 新增：工艺 3D打印/CNC/注塑
    due_before: str = "",           # 新增：交期在指定日期之前 YYYY-MM-DD
    due_after: str = "",            # 新增：交期在指定日期之后
    sort_by: str = "",              # 新增：排序字段 priority/due/level
    limit: int = 0,                 # 新增：返回前 N 条
) -> str:
    """查询订单列表，支持多字段 AND 组合筛选和排序。

    筛选字段（全部可选，AND 组合）：
      status          — 订单状态（紧急/生产中/待排产/排期中/即将完成）
      customer_name   — 客户名模糊匹配
      customer_level  — 客户等级（S/A/B/C/D）
      process         — 工艺类型（3D打印/CNC/注塑/表面处理）
      due_before      — 交期在指定日期之前（YYYY-MM-DD）
      due_after       — 交期在指定日期之后（YYYY-MM-DD）
      sort_by         — 排序字段：priority（综合优先级）/due（交期）/level（客户等级）
      limit           — 返回前 N 条（0=全部）
    """
    from .data import load_orders
    orders = load_orders()

    # 筛选
    filtered = []
    for o in orders:
        if status and o.get("状态", "") != status:
            continue
        if customer_name and customer_name not in o.get("客户", ""):
            continue
        if customer_level and o.get("客户等级", "") != customer_level:
            continue
        if process and o.get("工艺", "") != process:
            continue
        if due_before and o.get("交期", "") > due_before:
            continue
        if due_after and o.get("交期", "") < due_after:
            continue
        filtered.append(o)

    # 排序
    if sort_by == "due":
        filtered.sort(key=lambda x: x.get("交期", "9999-99-99"))
    elif sort_by == "level":
        level_order = {"S": 0, "A": 1, "B": 2, "C": 3, "D": 4}
        filtered.sort(key=lambda x: level_order.get(x.get("客户等级", ""), 5))
    elif sort_by == "priority":
        # 综合优先级：紧急状态 → 交期近 → 等级高
        level_order = {"S": 0, "A": 1, "B": 2, "C": 3, "D": 4}
        status_order = {"紧急": 0, "生产中": 1, "待排产": 2, "排期中": 3, "即将完成": 4}
        filtered.sort(key=lambda x: (
            status_order.get(x.get("状态", ""), 9),
            x.get("交期", "9999-99-99"),
            level_order.get(x.get("客户等级", ""), 5),
        ))

    # limit
    if limit > 0:
        filtered = filtered[:limit]

    # 格式化输出
    if not filtered:
        return "未找到匹配的订单"

    lines = [f"找到 {len(filtered)} 笔订单："]
    for o in filtered:
        lines.append(
            f"  {o.get('订单号', '?')} | {o.get('客户', '?')}({o.get('客户等级', '?')}级) | "
            f"{o.get('产品', '?')} | {o.get('工艺', '?')} | "
            f"交期:{o.get('交期', '?')} | 状态:{o.get('状态', '?')}"
        )
    return "\n".join(lines)
```

**Step 2**：同步更新 `demo/tools/registry.py` 中 `query_orders` 的 Schema

```python
r.register(
    "query_orders",
    "查询订单列表，支持多字段 AND 组合筛选和排序。"
    "可用于：按状态筛选、按客户名/等级筛选、按工艺筛选、按交期范围筛选、按优先级排序。"
    "例：'交期在7/30之前的A级客户3D打印订单' → "
    "query_orders(due_before='2026-07-30', customer_level='A', process='3D打印', sort_by='priority', limit=5)",
    {"type": "object", "properties": {
        "status": {"type": "string", "description": "订单状态（紧急/生产中/待排产/排期中/即将完成），空=全部"},
        "customer_name": {"type": "string", "description": "客户名模糊匹配，空=全部"},
        "customer_level": {"type": "string", "description": "客户等级（S/A/B/C/D），空=全部"},
        "process": {"type": "string", "description": "工艺类型（3D打印/CNC/注塑/表面处理），空=全部"},
        "due_before": {"type": "string", "description": "交期在此日期之前 YYYY-MM-DD，空=不限"},
        "due_after": {"type": "string", "description": "交期在此日期之后 YYYY-MM-DD，空=不限"},
        "sort_by": {"type": "string", "description": "排序方式：priority(综合优先级)/due(交期)/level(客户等级)，空=不排序"},
        "limit": {"type": "integer", "description": "返回前N条，0=全部。建议排产场景设5-10"},
    }},
    query_orders, "order", "order_server",
)
```

**Step 3**：同样增强 `demo/tools/resource_tools.py` 中的 `query_inventory` 和 `query_customer`

```python
# query_inventory 新增筛选
def query_inventory(
    material_name: str = "",
    category: str = "",           # 新增：材料类别 金属/塑料/复合材料
    stock_below: float = 0,        # 新增：库存低于指定数量
    sort_by: str = "",            # 新增：排序 stock_asc(库存从少到多)
) -> str:
    # ... 多字段筛选逻辑 ...

# query_customer 新增排序
def query_customer(
    customer_id: str = "",
    customer_name: str = "",
    min_level: str = "",          # 新增：最低等级
    sort_by: str = "",            # 新增：排序 level/credit
) -> str:
    # ...
```

### 7.4 验收标准

- [ ] `query_orders(customer_level="A", process="3D打印", due_before="2026-07-30")` 返回空或精确匹配
- [ ] `query_orders(sort_by="priority", limit=5)` 返回按优先级排序的前 5 条
- [ ] Agent 问"交期 7/30 前 A 级客户 3D 打印订单" → LLM 能自动拼出正确的 query_orders 参数
- [ ] 所有新参数均加入 `registry.py` 的 Schema 定义

---

## R8：多租户隔离

### 8.1 当前状态

- `auth/token_exchange.py` — Token 无 `tenant_id` 字段
- `tools/data.py` — CSV 数据全局共享，无租户过滤
- 所有用户看到同样的订单/库存/设备数据

### 8.2 改造目标

```
改造前                                改造后
──────                                ──────
Token = {subject, role, permissions}   Token = {subject, role, permissions, tenant_id}
所有用户共享数据                        工具查询自动按 tenant_id 过滤
                                      数据隔离：tenant_A 只能看到 tenant_A 的订单
```

### 8.3 实施步骤

**Step 1**：修改 `demo/auth/token_exchange.py` — Token 加 `tenant_id`

```python
@dataclass
class Token:
    subject: str
    role: RoleType
    permissions: list[str]
    source: str
    tenant_id: str = ""           # 新增：租户 ID
    parent_trace: str = ""
    issued_at: float = field(default_factory=time.time)
    expires_at: float = 0.0
    token_id: str = field(default_factory=lambda: uuid.uuid4().hex[:12])

    # ... 其余不变 ...


class STS:
    def issue_user_token(self, user_id: str, role: RoleType,
                         tenant_id: str = "",      # 新增
                         ttl: int = 3600) -> str:
        token = Token(
            subject=user_id, role=role,
            permissions=ROLE_PERMISSIONS.get(role, []),
            source="user",
            tenant_id=tenant_id,  # 新增
            expires_at=time.time() + ttl,
        )
        self._store.save(token)
        return token.token_id

    def exchange(self, parent_token_id, requested_role, target_trace=""):
        # ... 原有逻辑 ...
        child = Token(
            # ... 原有字段 ...
            tenant_id=parent.tenant_id,  # 子 Token 继承父 Token 的 tenant_id
            # ...
        )
```

**Step 2**：修改 `demo/tools/data.py` — 数据加载加租户过滤

```python
"""数据层 -- CSV 加载 + 租户隔离。

读 CSV 后缓存在内存。工具查询时自动按租户过滤。
"""
import csv
from pathlib import Path
from typing import Any

DATA_DIR = Path(__file__).parent.parent / "data"

# 全局缓存
_orders: list[dict] | None = None
_inventory: list[dict] | None = None
_machines: list[dict] | None = None
_customers: list[dict] | None = None


def _load_csv(filename: str) -> list[dict]:
    path = DATA_DIR / filename
    if not path.exists():
        return []
    with open(path, "r", encoding="utf-8") as f:
        return list(csv.DictReader(f))


def load_orders(tenant_id: str = "") -> list[dict]:
    """加载订单。tenant_id 非空时只返回该租户的数据。"""
    global _orders
    if _orders is None:
        _orders = _load_csv("orders.csv")
    if not tenant_id:
        return _orders
    # 如果 CSV 有 tenant_id 列则过滤
    return [o for o in _orders if o.get("tenant_id", "") == tenant_id]


def load_inventory(tenant_id: str = "") -> list[dict]:
    # 同上...
    pass

# ... 其余 load_* 函数同理 ...
```

**Step 3**：修改 `demo/tools/registry.py:execute` — 自动传入 tenant_id

```python
def execute(self, name: str, arguments: dict, token=None, audit=None) -> str:
    # ...

    # 提取 tenant_id（如有 Token）
    tenant_id = ""
    if token is not None:
        # Token 对象有 tenant_id 属性
        if hasattr(token, 'tenant_id'):
            tenant_id = token.tenant_id

    # 工具函数如果接受 tenant_id 参数，自动注入
    if "tenant_id" in schema.parameters.get("properties", {}):
        arguments["tenant_id"] = tenant_id
```

**Step 4**：修改 `demo/tools/data.py` — CSV 加 `tenant_id` 列（可选，多租户数据用）

```csv
# orders.csv 示例
订单号,客户,产品,数量,交期,状态,工艺,客户等级,tenant_id
ORD001,深圳精密,航天连接器,50,2026-07-15,生产中,CNC,A,factory_a
ORD002,东莞模具,汽车模具底座,10,2026-07-22,待排产,注塑,B,factory_b
```

默认 CSV 不设 `tenant_id`（向后兼容，`tenant_id=""` 返回全部数据）。

**Step 5**：修改 `demo/auth/guard.py` — 工具层校验加租户维度

```python
def check_tool_permission(token, tool_name, audit=None) -> tuple[bool, str]:
    # ... 原有角色校验 ...

    # 租户隔离：不同租户不能访问对方的数据
    # （这层是补充校验，数据层已经过滤了，但 guard 层再加一道以防配置错误）
    # 当前不做强制租户校验（向后兼容），生产通过 FORCE_TENANT 环境变量开启
    return True, "ok"
```

**Step 6**：加环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `FORCE_TENANT` | `false` | 强制租户隔离，未带 tenant_id 的请求拒绝 |
| `DEFAULT_TENANT` | `""` | 无 Token 时的默认租户 |

### 8.4 验收标准

- [ ] `tenant_id=""` → 返回全部数据（向后兼容）
- [ ] `tenant_id="factory_a"` → 只返回 factory_a 的订单
- [ ] Token Exchange 子 Token 继承父 Token 的 tenant_id
- [ ] `FORCE_TENANT=true` 且请求无 tenant_id → 拒绝

---

## 实施路线建议

```
Week 1（5 天）
  Day 1-2: R1（工具沙箱与重试）         ← P0
  Day 3-4: R2（输出护栏）               ← P0
  Day 5:   R3（步骤校验）                ← P1

Week 2（5 天）
  Day 1-2: R6（Agent 评估脚本）         ← P0
  Day 3:   R4（上下文压缩）              ← P1
  Day 4:   R7（RAG 结构化筛选）         ← P1
  Day 5:   R5 + R8（MCP 隔离 + 多租户） ← P1，可并行
```

**P0 优先原因**：
- R1（沙箱/重试）和 R2（护栏）是 Agent 安全的基础防线，没有它们 Agent 不稳定且不安全
- R6（评估脚本）是量化改进效果的前提——没有基线就没法判断"改完有没有变好"

---

> **文档维护**：每完成一个方案，在对应小节末尾标注 ✅ + commit hash。
> 关联文档：`7周路线-vs-demo-差距全景图.md` · `面试技术深度与应对策略.md`
