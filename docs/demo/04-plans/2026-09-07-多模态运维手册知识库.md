# 多模态运维手册知识库 实施计划

> **面向 AI 代理的工作者：** 本计划按任务顺序执行，每任务含实现 + 测试 + 验证。测试/构建在 **WSL** 下执行（`wsl -d Ubuntu bash -c '...'`，路径 `/mnt/e/workspace/codebuddy_workspace/projects/flex-fab-agent`，下文命令省略前缀）。**禁止自动 git**（项目红线）：所有改动完成后由用户统一发起提交。**开发分支**：用户已要求创建 dev 分支 + 功能分支，git 操作待用户发起。
>
> 实现细节引用：**设计文档 `docs/demo/02-specs/2026-09-07-多模态运维手册知识库-design.md`**（D1–D8 + 完整 DDL）与**需求规格 `docs/demo/02-specs/需求规格-多模态运维手册知识库-v1-20260907.md`**。本文只写「每步做什么、验证什么、照哪个模式写」，不重复贴完整代码。

**目标：** PDF/Word/图片手册入库（图片三级策略：OCR→自动图注→人工标注），调试台「手册库」单 tab 管理，`search_manual` 工具供 Agent 检索并带可定位溯源；修复「/ask 检不到 confidential」既有 bug。

**执行顺序原则：** 最小闭环早跑通、不依赖项并行。T1/T2/T4（权限 + schema + 文本检索闭环）**不依赖解析库型号**，与 T0 spike 互不阻塞；T5（解析/图片）依赖 T0 定型号。

**规格依据：** `需求规格-多模态运维手册知识库-v1-20260907.md`（§10 测试框架）
**设计依据：** `2026-09-07-多模态运维手册知识库-design.md`

---

## 依赖总览

```
T0 spike ───────────────┐（定库型号/耗时真数）
T1 权限修复(D1) ────────┤
T2 schema v6 + config ──┤
T4 文本检索闭环(index/retriever/search_manual) ←─ T1+T2 已就绪即可，无需 T0
                        ├──► T5 解析+图片三级(loaders/ocr/caption/chunker)
                        ├──► T6 worker+导入API(scan/upload/job) → T7 前端
T8 操作日志+冒烟 ───────┴──► T9 全量测试+部署
```

---

## 任务总表

| # | 任务 | 依赖 | 独立可合入 |
|---|------|------|-----------|
| T0 | 解析库型号 spike（验证性） | 需用户供 1 份真实大扫描件 | ✅ 独立验证，产出一份结论 |
| T1 | 权限修复 D1：graph role 注入 + AskRequest.role | 无 | ✅ 独立（bug 修复本体） |
| T2 | config 手册项 + schema v6 三表 + 迁移 | 无 | ✅ 独立 |
| T3 | `core/vision_client.py` + VISION_PROVIDERS（mock 测） | 无 | ✅ 独立（不依赖真 key） |
| T4 | `manual_index`/`manual_retriever`/`chunker` 文本版 + `search_manual` | T1 T2 | 文本手册检索最小闭环 |
| T5 | `loaders/{pdf,docx,image}` + `ocr` + `captioner` + asset 落库 | T0 | 图片三级处理 |
| T6 | `manual_import_worker` + `/manuals/*` API + 静态图访问 | T2 T4 T5 | 导入异步编排 |
| T7 | 前端 manual.ts + ManualView + tab「手册库」+ 路由 | T6 | 调试台手册功能 |
| T8 | 操作日志动作映射 + smoke 页面清单 | T6 | 治理收尾 |
| T9 | 验收清单 + `run_all_tests` + 前端 build + 容器部署验证 | 全部 | 门禁 |

---

## T0：解析库型号 spike（先跑，验证性）

**目标：** 敲定 PyMuPDF / python-docx / RapidOCR 型号、离线可装、实测一份真实大扫描件的解析耗时量级 → 定 T5/T6 的超时与进度粒度。**需用户提供 1 份真实扫描件样本**（放到临时目录，不入库）。

- [ ] **步骤 1：requirements 追加候选依赖**，WSL 内 pip 安装验证离线可用（无网络时先下载 wheel 或走代理）
  ```bash
  pip install pymupdf python-docx rapidocr-onnxruntime 2>&1 | tail -3
  python -c "import fitz, docx; print('pdf/docx ok')"
  python -c "from rapidocr_onnxruntime import RapidOCR; print('rapidocr ok')"
  ```
- [ ] **步骤 2：小脚本抽测样本**：文本层页数、扫描页判定阈值、单页 OCR 平均耗时、单页内嵌图提取
- [ ] **步骤 3：产出结论**：写一段 spike 记录到 `tmp/`（不入库）——库型号定稿、`MANUAL_OCR_MIN_CHARS`/`MANUAL_OCR_CONF_THRESHOLD` 建议值、单 worker 下百页扫描件 OCR 预计耗时（用于 T6 前端进度说明与 axios 超时）。

**验证：** 上述 3 条 python 断言通过；spike 记录含实测耗时。

---

## T1：权限修复（设计 D1，独立可合入）

**Bug 本体：** `graph/single_agent_graph.py:247-254` 只给写工具签 admin token，只读 `search_knowledge_base` 恒 `token=None` → `rag/retriever.py:158 _allowed_sources(None)` 仅 public → `/ask` 检不到 confidential。

**文件：**
- 修改 `flex_fab_agent/api.py`（`AskRequest` :141 加 role；`_run_agent_round` :467 透传）
- 修改 `flex_fab_agent/agents/single_agent.py`（`run_single_agent` 增 role → state）
- 修改 `flex_fab_agent/graph/single_agent_graph.py`（工具执行段 :247-254 读工具签 role token）
- 修改 `flex_fab_agent/tools/registry.py`（`_TOOLS_NEED_TOKEN` :25 加 `search_manual`）
- 修改 `flex_fab_agent/auth/token_exchange.py`（RBAC 白名单 :35-41 加 `search_manual`，全角色）
- 新建 `flex_fab_agent/test_rag_permission_graph.py`（T3 测试）

- [ ] **步骤 1：AskRequest.role + _run_agent_round 透传**。`role: str = "admin"`（写死在 request model，校验 4 个合法值用 `Literal` 或字段校验）。`_run_agent_round(query, thread_id=None, role="admin")` → 传 `run_single_agent(..., role=role)`。
- [ ] **步骤 2：graph 读工具按 state["role"] 签 token**。在 `:247` 的 `if schema and not schema.read_only:` 分支后补：
  ```python
  elif schema and state.get("role") and name in _TOOLS_NEED_TOKEN:
      from ..auth.token_exchange import STS
      token = STS().get_token(STS().issue_user_token("agent-graph", state["role"]))
  ```
  保持写工具分支不变（admin）。
- [ ] **步骤 3：registry._TOOLS_NEED_TOKEN 加 `search_manual`**（连同下一步注册一起，见 T4）；token_exchange 白名单加 `search_manual`，admin/reviewer/operator/viewer **全角色放行**（guard 不做 confidential 判定，RAG 层过滤）。
- [ ] **步骤 4：测试 `test_rag_permission_graph.py`**（设计 §11 T3）：构造含 confidential+public 两个合成文档的手册集合（或先直接用合同库验证修复）：mock graph state role ∈ {None, admin, operator} 执行一轮 → 断言 confidential 命中与否。**修复前** admin 提问也检不到 → **修复后** admin 检到、operator 检不到。
- [ ] **步骤 5：回归现有测试**
  ```bash
  python -m pytest flex_fab_agent/test_api_ask.py flex_fab_agent/rag/test_retriever.py -q 2>&1 | tail -5
  ```
  预期：全过（role 默认 admin 不改变现状行为）。

**验证：** T3 权限回归绿 + 现有 ask/retriever 测试不破坏。

---

## T2：config 手册项 + schema v6 三表 + 迁移

**文件：**
- 修改 `flex_fab_agent/config.py`（§9 手册配置项 + 备注）
- 修改 `flex_fab_agent/schema/migrate.py`（`CURRENT_VERSION`→6 + `_MANUALS_DDL` 常量 + `_up_v6/_down_v6`）
- 修改 `flex_fab_agent/schema/schema.sql`（三表建表同步，供全新部署基线）

- [ ] **步骤 1：config 追加**：`MANUALS_DIR`/`MANUALS_INBOX_DIR`（默认 `RUNTIME_DIR/manuals` / `RUNTIME_DIR/manuals_inbox`）、`MANUAL_CHUNK_SIZE=1000`/`OVERLAP=200`、`MANUAL_OCR_MIN_CHARS=10`、`MANUAL_OCR_CONF_THRESHOLD=0.85`、`MANUAL_IMPORT_WORKER_CONCURRENCY=1`。目录名常量可建 `rag/manual_paths.py` 统一（设计 §7 引用，二选一，选一个放 config 相邻即可）。
- [ ] **步骤 2：migrate 三表 DDL**（设计 §4，一字不差照抄 design 的 4.1–4.3 三块，含 `UNIQUE KEY uk_doc_model_version`）。`_up_v6` 顺序建、`_down_v6` 逆序 DROP。
- [ ] **步骤 3：schema.sql 末尾同步**（与 _MANUALS_DDL 同文，全新部署基线走 v1 schema.sql 也能建）。
- [ ] **步骤 4：应用迁移 + 验证**（WSL MySQL，口令取 credentials.local.md）
  ```bash
  python -m flex_fab_agent.schema.migrate --up
  python -c "from flex_fab_agent.schema.migrate import status; s=status(); print(s['versions'], s['table_count'])"
  ```
  预期：versions 含 6、table_count 增加 3（manual_doc/manual_asset/manual_import_job）。
- [ ] **步骤 5：回滚可逆验证**
  ```bash
  python -m flex_fab_agent.schema.migrate --down
  python -m flex_fab_agent.schema.migrate --up   # 再 up 回 v6
  ```
  预期：down 后三表消失、up 后重建（进 T9 前确认 once）。

**验证：** migrate up/down/up 幂等可逆。

---

## T3：`core/vision_client.py`（设计 D5，mock 可测）

**文件：**
- 修改 `flex_fab_agent/config.py`（`VISION_PROVIDERS`/`PRIMARY_VISION_PROVIDER`/`available_vision_providers()`，照 `PROVIDERS` :116 同构）
- 新建 `flex_fab_agent/core/vision_client.py`
- 新建 `flex_fab_agent/core/test_vision_client.py`

- [ ] **步骤 1：config 多模态表**。条目同构 `{name,enabled,api_key,base_url,model,note}`。预置 3 条目（design D5/spec 3.1）：火山标准端点 `/api/v3`+doubao vision（复用 `VOLC_API_KEY`，默认 enabled）、海外 vision（`enabled=false` 待 key + 代理）、本地 VLM（末位兜底 false）。`PRIMARY_VISION_PROVIDER` env 提到最前（照 :143-148 现有逻辑）。
- [ ] **步骤 2：vision_client**。签名 `call_vision(prompt: str, image_paths: list[str], bypass_cache: bool = False) -> str`：
  - 图片 base64 → `user content=[{"type":"text","text":prompt},{"type":"image_url","image_url":{"url":"data:image/{ext};base64,..."}}]`
  - 独立 OpenAI client 缓存（**不复用** `core/llm_client.py` 的 `_client_cache`，见设计 D5）+ `trust_env=False` 连接池照 `llm_client._build_client` :80 模式
  - 遍历 `available_vision_providers()` 首个成功即返回；失败打日志切下一个；全失败 `raise`
  - 缓存：`RUNTIME_DIR/vision_cache.sqlite`，键 = `sha256(all base64)` + `CAPTION_PROMPT_VERSION` + provider.model；`bypass_cache=True` 跳过读与写（**L3 重生成必须绕过**，design D5）
- [ ] **步骤 3：测试 test_vision_client.py**（设计 §11 T4）：monkeypatch `available_vision_providers` 返回 mock（照 `core/test_llm_client.py:68` monkeypatch 模式）；首个抛错切次个成功；bypass 命中/未命中断言；全不可用 raise。
- [ ] **步骤 4：跑测试**
  ```bash
  python -m pytest flex_fab_agent/core/test_vision_client.py -q 2>&1 | tail -5
  ```

**验证：** mock 下回落/缓存/降级三态绿；不发起真实调用。

---

## T4：文本手册检索最小闭环（`manual_index` / `manual_retriever` / `chunker` 文本版 / `search_manual`）

**目标：** 用**合成文本**（临时 .md/.txt 当手册）跑通「建 doc → 灌 chroma → search_manual 检索 → 权限/激活过滤 → 溯源」核心闭环。此任务完成后手册检索能力立即可验证，不依赖解析库。

**文件：**
- 新建 `flex_fab_agent/rag/manual_paths.py`（目录常量，如未放 config）
- 新建 `flex_fab_agent/rag/manual_index.py`
- 新建 `flex_fab_agent/rag/manual_retriever.py`
- 新建 `flex_fab_agent/rag/chunker.py`（先实现文本切分 + metadata 构造；PDF/docx 标题树切分 T5 补）
- 修改 `flex_fab_agent/tools/registry.py`（注册 `search_manual`）
- 修改 `flex_fab_agent/auth/token_exchange.py`（T1 步骤 3 同批）
- 新建 `flex_fab_agent/rag/test_manual_retriever.py`、`test_manual_index.py`

- [ ] **步骤 1：manual_index**（design §5）：collection `kb_manuals`（`DB_DIR` 同 `knowledge_base.py:22`，同 chroma 库不同 collection）；id `doc:{doc_id}:{seq}`；`add_doc(doc, chunks)` upsert；`delete_doc(doc_id)` delete where；`get_doc_chunks(doc_id)`。
  **chunk metadata 冗余**（设计 D3 关键）：每块带 `{doc_id, device_model, version, is_active, access, page_no, section, source, chunk_type}`——access/is_active 来自 manual_doc，灌库时写入，检索直接过滤不查库。
- [ ] **步骤 2：manual_retriever**：`_manual_state`（collection+b25+chunks+metas+reranker）独立惰性单例 + `invalidate()`；`search_manual(query, top_k, token)`：role→allowed（复用 `retriever._role_from_token`/`_allowed_sources`）；两路召回预过滤（向量 `query(where={"is_active":"1","access":[...]})`，BM25 出结果按 meta 过滤，设计 D3）；融合后 rerank 前再过滤（同 `retriever.py:130` 位置）；format 带溯源 `<手册名> v<版本> 第<N>页 第<X.Y节>`（图片命中附 asset URL，T5 补）。
- [ ] **步骤 3：chunker 文本版**：`chunk_doc(doc, text, *, page_no=None, section=None)` 用 `MANUAL_CHUNK_SIZE/OVERLAP` 滑窗 + 逐块 meta（含 access/is_active）。
- [ ] **步骤 4：注册 search_manual**（design §5.2 完整代码，description 写清手册域 + 「合同用 search_knowledge_base」）。`_TOOLS_NEED_TOKEN` 加名（T1 同批）。
- [ ] **步骤 5：测试**（设计 §11 T1/T2）：
  - index：重复 upsert 同 doc 不新增（幂等）；delete_doc 后检索消失
  - retriever：灌两份同型号 v1/v2（is_active v2=1）→ 检索只命中 v2；v2 置 0/v1 置 1 后切换命中；access=confidential 手册在 role admin 命中、role operator/None 不命中
  - 测试用合成文档写进 `tmp/` 或 `RUNTIME_DIR/manuals_test/`，跑完清理，**不污染 data/chroma 正式库**（可临时设 `FLEX_FAB_AGENT_RUNTIME_DIR` 指向 tmp 目录隔离）。
- [ ] **步骤 6：端到端手验**
  ```bash
  python -c "from flex_fab_agent.rag.manual_retriever import search_manual; print(search_manual('某设备 故障恢复 步骤', top_k=3))"
  ```
  预期：返回带页码/章节溯源的中文片段；换 role token 结果相应过滤。

**验证：** 文本手册检索闭环全绿；溯源含页码章节。

---

## T5：解析 + 图片三级处理（`loaders` / `ocr` / `captioner` / chunker 完整 / asset 落库）

**依赖：T0 spike 定型号。** 此步前先 `pip install pymupdf python-docx rapidocr-onnxruntime`（T0 已验证过则直接装）。

**文件：**
- 新建 `flex_fab_agent/rag/loaders/__init__.py`、`pdf.py`、`docx.py`、`image.py`
- 新建 `flex_fab_agent/rag/ocr.py`、`captioner.py`
- 修改 `flex_fab_agent/rag/chunker.py`（补 PDF 页/标题、docx heading 层级切分）
- 修改 `flex_fab_agent/config.py`（OCR 阈值两项入 /config 白名单，`api.py:185`）
- 新建 `flex_fab_agent/rag/test_manual_ocr_caption.py`

- [ ] **步骤 1：loaders/pdf**：按页抽文本层 + toc + 内嵌图（渲染到 `{img}`）；扫描页判定（页净文本 < 阈值 → 整页渲染走 OCR）。docx：段落 + heading 样式 → 多级标题树 + inline_shapes 导出。image：单图直通 OCR 管线。
- [ ] **步骤 2：ocr**：`ocr_image(path)->(text, avg_conf)`；`needs_caption(text, conf)` 阈值判定（`MANUAL_OCR_MIN_CHARS`/`MANUAL_OCR_CONF_THRESHOLD`，读 config，运行期可 /config 改——接系统表配置读取模式，同 `config.get_routing_policy()` 风格）。
- [ ] **步骤 3：captioner**：`generate_caption(img_path, force=False)` → `call_vision`；`force=True`（L3）→ bypass 缓存。**全 provider 不可用降级**：`caption_source='none'`+`review_status='flagged'`，不抛中断整份。
- [ ] **步骤 4：chunker 完整**：PDF 按页为主 + 页内章节粗分，块 meta 带 `page_no`/`section`；docx 按 heading 层级切，`section` 用多级路径。图片块：`{text: caption, chunk_type:'image', asset_id}`。
- [ ] **步骤 5：asset 落库 + 三级流转函数**（manual_service 前置）：`manual_doc` 建行 → parse 产 pages/images → 每图 ocr → needs_caption 判定入 caption 队列 → 逐张写 `manual_asset`（caption_source 分档）。这层先用**脚本式驱动**（T6 worker 化之前，用 pytest 内嵌调用函数验证流程），asset upsert 向量按 `vector_id`。
- [ ] **步骤 6：测试 test_manual_ocr_caption.py**（设计 §11 T5）：needs_caption 阈值边界（字数/置信）；L1→ocr、L2→vlm、L3 重生成后 upsert 不产生重复向量（查 collection count 不变 + 该 asset vector_id 更新）；mock call_vision 不发真调用。

**验证：** 三级流转单测绿；一份真实 PDF 手工跑通「解析→OCR→(mock 图注)→asset 落库→向量」。

---

## T6：异步导入 worker + `/manuals/*` API + 静态图访问

**依赖：T2（表）+ T4（index/retriever）+ T5（解析管线）。**

**文件：**
- 新建 `flex_fab_agent/rag/manual_import_worker.py`
- 新建 `flex_fab_agent/rag/manual_service.py`
- 修改 `flex_fab_agent/api.py`（手册路由段插 :675 静态块前 + startup 加 worker.start）
- 修改 `flex_fab_agent/schema/migrate.py`（无新表，不改）
- 新建 `flex_fab_agent/rag/test_manual_import_worker.py`、`test_manual_api.py`

- [ ] **步骤 1：manual_service**：`list_docs/get_doc/create_from_upload/create_from_scan（hash 判重幂等）/set_active（事务：同型号其余置 0）/delete_doc/update_doc/list_assets/update_asset_caption/regen_asset_caption`。delete_doc 收尾：DB 行 + `manual_index.delete_doc` + 落盘文件 + invalidate。
- [ ] **步骤 2：worker**（design D4）：`get_import_worker()` 单例 + `start()`（先清 DB 残留 `running→failed`，再起 daemon thread）。循环：取一条 queued → running → `parse→ocr→caption→index` 阶段机 → done/failed；`stage_progress` 记 `done/total`；每阶段起时写 `failed_stage`；单图 try/except 降级。模式照 `scheduler/auto_scheduler.py` 的 Thread+Event 结构（:45-51）。
- [ ] **步骤 3：startup 挂接**：`api.py:127 _start_auto_scheduler` 内追加 `get_import_worker().start()`（try/except 不阻塞）。
- [ ] **步骤 4：API 路由段**（design §7 全部 14 端点；GET 读匿名 / 写 `Depends(require_admin)` 照 debug 段 :542 模式；multipart 用 FastAPI `UploadFile`+`python-multipart` 依赖要加）。上传端点类型白名单 + 单文件上限（读 config）。扫描端点返回 `{created, skipped}`。原图端点 `GET /manuals/assets/{id}/image` 用 `FileResponse`，路径仅限 `MANUALS_DIR`（照 :686 is_relative_to 防穿越）。**放 :675 静态块之前**（否则被 catch-all 吞）。
- [ ] **步骤 5：测试**（设计 §11 T6/T7）：worker 阶段机（注入假 parse/ocr 观察状态流转）；fail 留 failed_stage + retry 从该阶段续跑（幂等）；startup 清僵尸（预置 running 行断言转 failed）；上传/扫描 file_hash 幂等；activate 事务同型号仅一激活；鉴权 admin/匿名。
- [ ] **步骤 6：手验**：`python -m compileall flex_fab_agent` + 启动 app 后 `POST /manuals/scan`（inbox 放 1 份小 PDF）→ 轮询 `/manuals/jobs/{id}` 到 done → `GET /manuals` 可见 → 提问触发 search_manual。

**验证：** 导入端到端（scan→parse→index→列表可见）跑通。

---

## T7：前端（manual.ts + ManualView + tab「手册库」+ 路由）

**依赖：T6 API 就绪。**

**文件：**
- 新建 `web/src/api/manual.ts`
- 修改 `web/src/api/debug.ts`（`askTrace` 加 role 参数）
- 新建 `web/src/views/ManualView.vue`
- 修改 `web/src/views/PortalView.vue`（tab `name="manuals"` label「手册库」，**勿用 `manual`**——已被演示 tab 占用 :49）
- 修改 `web/src/router/index.ts`（`/portal/manuals`，避开 `/portal/manual` :21）
- 修改 `web/vite.config.ts`（proxy 加 `/manuals`）

- [ ] **步骤 1：manual.ts**：JSON 实例（照 `log.ts`）+ 上传实例（`timeout: 120_000`，multipart）。函数清单见 design §8。
- [ ] **步骤 2：ManualView 三块**（design §8）：
  1. 列表（筛选 + el-table：型号/版本·当前历史 tag/权限 tag/类型/状态 tag + 上传 el-upload + 扫描按钮 + 行操作：设为当前/编辑权限/删除）
  2. 详情抽屉：asset 列表（缩略图 `el-image → /manuals/assets/{id}/image` + caption_source/review tag），标注弹层（编辑说明 PUT / 重新生成 POST /manuals/assets/{id}/regen → 确认）
  3. 问答试验：角色下拉（admin/reviewer/operator/viewer）+ 提问 → `/ask {query, role}` → answer + 溯源链接；job 进度轮询 `GET /manuals/jobs`
- [ ] **步骤 3：挂载**：PortalView import + tab pane；router 深链；vite proxy `/manuals`。
- [ ] **步骤 4：debug.ts askTrace 加 role**（默认 admin），DebugView 提问默认不带则后端兜底 admin，可选下拉（放在 ManualView 问答区即可，Debug 保持现状，**最小改动**）。
- [ ] **步骤 5：typecheck + build**
  ```bash
  cd web && npm run typecheck 2>&1 | tail -5 && npm run build 2>&1 | tail -8
  ```
  预期：vue-tsc 无错、vite build 通过。

**验证：** 前端构建绿；dev 联调（vite 5173 + API 8000）手册列表/上传/标注/问答可用。

---

## T8：操作日志动作映射 + smoke 页面清单

- [ ] **步骤 1**：`observability/operation_log.py`——`_HTTP_MANUAL_PREFIXES` 加 `/manuals/`；`_ACTION_PREFIXES` 加映射（上传手册/扫描导入/设当前版本/手册标注/重新生成图注/删除手册，对应端点前缀）；**排除图片 GET**（`GET /manuals/assets/{id}/image` 高频率静态，前缀排除）。
- [ ] **步骤 2**：`classify_request` 单测补 /manuals 用例（test_operation_log.py 已有，追加断言）。
- [ ] **步骤 3**：`smoke_test.py` 页面清单（:510 附近）加 `("/#/manuals", "手册库")`。

**验证：** classify 单测绿；冒烟页面清单含新 tab。

---

## T9：验收清单 + 全量门禁 + 容器部署验证

**文件：**
- 新建 `docs/demo/08-test/验收清单-多模态运维手册知识库-2026-09-07.md`（按需求规格 §10.2 手动项 + §10.3 门禁落表）
- 或并入 `docs/demo/05-tasklist/todo-多模态运维手册知识库-20260907.md` 阶段 7（同目录 08-test 惯例见其它功能）

- [ ] **步骤 1**：验收清单落表（规格 §10.2 7 项手动 + §10.3 门禁 + 权限/版本回归项）。
- [ ] **步骤 2：全量测试** `python run_all_tests.py 2>&1 | tail -15`（含新增 6 组测试文件全过）。
- [ ] **步骤 3：前端 build**（T7 已跑，复跑确认）。
- [ ] **步骤 4：容器部署验证**（WSL）——HF/chroma cache 挂载沿用现有 compose；`docker compose build && docker compose up -d` → 上传 1 份真实 PDF → 轮询解析 → 提问命中带溯源；confidential 手册 role=operator 过滤、role=admin 命中；容器重启后 manual/向量仍在（RUNTIME_DIR 挂卷）。

---

## 自检记录

- **规格覆盖度**：图片三级→T5；C1 异步→T6；C2 上传+扫描→T6/T7；C3 权限+修 bug→T1；C4 版本→T4/T6；C5 分库→T4；C6 vision→T3；C7 溯源→T4/T5；C8 tab→T7；操作日志→T8；冒烟→T8；验收/门禁→T9。
- **范围外未建任务**（符合规格 4.2）：.doc/xlsx/pptx、正文编辑、多模态 embedding、跨版本 diff。
- **设计收敛已内化**：合同库 `knowledge_base.py:82`/`retriever.py:135 _rag_state` **不动**（设计 D2）；DOC_PERMISSION 保留（D3）——T1/T4 不得去改这两处。
- **待用户提供**：T0 一份真实大扫描件样本。
- **待确认（实施期配置）**：T3 火山 vision 账号额度 / 海外 key（不阻塞，mock 先行）。
- **git 红线**：全程不自动 commit/push；用户要求 dev 分支 + 功能分支，发起时再切。
