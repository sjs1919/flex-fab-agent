# 资源聚合页 + 人员模块 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。
> **拆分说明**：资源页（6 类）与人员模块**写在一起、分开实现**（用户 2026-08-26）：任务 1-2 为资源页，任务 3-4 为人员模块，任务 5 端到端验收。

**目标：** 新增资源聚合查看页（设备/客户/订单/库存/批次/前道 6 类 + 人员），后端只读端点 + 前端「资源」tab 内子 tab，列表 id 倒序、完整展示；人员含状态（上班/请假，前端可改 + 模拟器 leave 联动）。

**架构：** 后端 `GET /resources/{category}`（复用 `data.py` load_*，`{"items": [...]}` id 倒序）+ 人员 `PUT /resources/personnel/{id}/status`（R-7 admin）；前端 `PortalView.vue` 加「资源」tab → `ResourcesView.vue` 子 tab（`el-tabs lazy`，动态列 `el-table`）；人员模块：`personnel` 表 + seed + 模拟器 `_fire_leave` 从"前道工人池"抽象改造为操作具体人员。

**技术栈：** FastAPI + pymysql + pytest(TestClient)；Vue3 + TS + Vite + element-plus + axios。

**关联现状：**
- `demo/tools/data.py`：`load_machines/customers/orders/inventory/batches/preprocess_tasks` 已实现（mysql），返回 `list[dict]` id 正序
- `demo/simulator/engine.py:311` `_fire_leave`：前道工人请假/回岗（6 人池抽象，state_change_log entity="前道工人池"）——**本次改造为操作 personnel 表具体人**
- `demo/schema/*.sql`：幂等建表（CREATE TABLE IF NOT EXISTS）
- `web/src/views/PortalView.vue`：单页聚合容器（现有 5 tab + 资源 tab）
- `demo/api.py`：只读看板端点匿名可读（R-7）；写端点 `Depends(require_admin)`

---

## 文件结构

| 文件 | 动作 | 职责 |
|------|------|------|
| `demo/schema/*.sql` | 修改 | 新增 `personnel` 表（任务 3） |
| `demo/simulator/seed.py` | 修改 | 预置 6 名人员（任务 3） |
| `demo/tools/data.py` | 修改 | 新增 `load_personnel`（任务 3） |
| `demo/api.py` | 修改 | `GET /resources/{category}`（含 personnel）+ `PUT /resources/personnel/{id}/status`（任务 1/3） |
| `demo/simulator/engine.py` | 修改 | `_fire_leave` 改造为操作 personnel 具体人（任务 3） |
| `demo/test_resources.py` | 创建 | 资源/人员端点测试（任务 1/3） |
| `web/src/api/resources.ts` | 创建 | `fetchResources` + `setPersonnelStatus`（任务 2/4） |
| `web/src/views/ResourcesView.vue` | 创建 | 7 子 tab（6 类 + 人员），人员含状态切换（任务 2/4） |
| `web/src/views/PortalView.vue` | 修改 | 加「资源」tab（任务 2） |

---

## 任务 1：资源 6 类后端端点

（内容同原计划，含测试 `test_resources_list_returns_items` / `test_resources_id_descending` / `test_resources_unknown_404`，TDD 步骤不变）

- [ ] **步骤 1-4**：失败测试 → 实现 `_RESOURCE_LOADERS` + `GET /resources/{category}` → 测试通过
- [ ] **步骤 5：Commit** `feat(demo): 资源列表只读端点 /resources/{category}（6 类，id 倒序）`

---

## 任务 2：资源前端 tab（6 子 tab）

（内容同原计划：`api/resources.ts` + `ResourcesView.vue` + PortalView 加 tab + build）

- [ ] **步骤 1-4**：api 封装 → ResourcesView（6 子 tab 动态列）→ PortalView tab → `npm run build`
- [ ] **步骤 5：Commit** `feat(web): 资源聚合页（6 类子 tab 动态列展示，id 倒序）`

---

## 任务 3：人员后端（personnel 表 + seed + 端点 + 模拟器 leave 联动）

**文件：**
- 修改：`demo/schema/*.sql`、`demo/simulator/seed.py`、`demo/tools/data.py`、`demo/api.py`、`demo/simulator/engine.py`
- 测试：`demo/test_resources.py`（追加人员用例）

- [ ] **步骤 1：schema 加 personnel 表**

```sql
-- 人员（写方：seed + 模拟器 leave 事件 + 前端状态切换；读方：resources/前端）
CREATE TABLE IF NOT EXISTS personnel (
    id     VARCHAR(16) NOT NULL COMMENT '人员编号（P001...）',
    name   VARCHAR(32) NOT NULL COMMENT '姓名',
    role   VARCHAR(32) NOT NULL COMMENT '工种（前道工人/调机员/排版员）',
    status ENUM('上班','请假') NOT NULL DEFAULT '上班' COMMENT '状态（leave 联动 + 前端可改）',
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
  COMMENT='人员（所有权：seed + 模拟器 B 层 leave + 前端）';
```

- [ ] **步骤 2：seed 预置 6 名人员**

`demo/simulator/seed.py` 追加（对齐 seed 幂等风格）：
```python
def seed_personnel(cur):
    """预置 6 名前道工人（对应原"前道工人池"，改具体人员）。幂等：存在即跳过。"""
    cur.execute("SELECT COUNT(*) FROM personnel")
    if cur.fetchone()[0] > 0:
        return
    rows = [
        ("P001", "张伟", "前道工人"), ("P002", "李强", "前道工人"),
        ("P003", "王芳", "前道工人"), ("P004", "刘洋", "前道工人"),
        ("P005", "陈静", "前道工人"), ("P006", "赵磊", "前道工人"),
    ]
    for pid, name, role in rows:
        cur.execute("INSERT INTO personnel (id, name, role) VALUES (%s, %s, %s)",
                    (pid, name, role))
```
（在 seed 主流程里调用 `seed_personnel(cur)`；若 seed.py 为纯 SQL/文件式，按既有模式落地）

- [ ] **步骤 3：`data.py` 加 `load_personnel`**

```python
def load_personnel(tenant_id: str = "") -> list[dict[str, str]]:
    """加载人员（personnel 无 tenant_id，参数保留对齐签名）。"""
    return _read_rows("SELECT * FROM personnel", (), filename="")
```

- [ ] **步骤 4：`api.py` 端点（GET 复用 + PUT 状态切换）**

`_RESOURCE_LOADERS` 加 `"personnel": "load_personnel"`（GET /resources/personnel 即生效）。

新增 PUT：
```python
class PersonnelStatusRequest(BaseModel):
    status: str  # 上班 | 请假


@app.put("/resources/personnel/{pid}/status")
def set_personnel_status(pid: str, body: PersonnelStatusRequest,
                         _admin: str = Depends(require_admin)) -> dict:
    """人员状态切换（写端点，强制 admin token R-7）。status: 上班|请假。"""
    if body.status not in ("上班", "请假"):
        raise HTTPException(400, f"非法状态: {body.status}")
    from .tools.data import get_connection
    with get_connection() as conn:
        with conn.cursor() as cur:
            cur.execute("UPDATE personnel SET status=%s WHERE id=%s", (body.status, pid))
            if cur.rowcount == 0:
                raise HTTPException(404, f"人员不存在: {pid}")
            conn.commit()
    return {"ok": True, "message": f"{pid} 已切换为 {body.status}"}
```

- [ ] **步骤 5：`engine.py` `_fire_leave` 改造为操作具体人员**

```python
def _fire_leave(conn, cur, sim_time: datetime, params: dict, payload: dict) -> None:
    """前道工人请假/回岗（personnel 表具体人，原 6 人池抽象改造）：
    absence 请假（选一名上班者）+ 预排回岗（kind=back）+ 预排下一个 absence；
    back 回岗（选一名请假者恢复）。"""
    if payload.get("kind") == "back":
        cur.execute("SELECT id FROM personnel WHERE status='请假' LIMIT 1")
        row = cur.fetchone()
        if row:
            cur.execute("UPDATE personnel SET status='上班' WHERE id=%s", (row[0],))
            states.log_state_change(cur, sim_time, "personnel", row[0],
                                    "status", "请假", "上班")
        events.schedule_next(cur, "leave", sim_time, params)
        return
    cur.execute("SELECT id FROM personnel WHERE status='上班' LIMIT 1")
    row = cur.fetchone()
    if row:
        cur.execute("UPDATE personnel SET status='请假' WHERE id=%s", (row[0],))
        states.log_state_change(cur, sim_time, "personnel", row[0],
                                "status", "上班", "请假")
    return_at = sim_time + timedelta(days=random.randint(1, 3))
    cur.execute(
        "INSERT INTO sim_events (sim_time, event_type, payload_json, status) "
        "VALUES (%s, 'leave', %s, 'scheduled')",
        (return_at, json.dumps({"kind": "back"}, ensure_ascii=False)))
    events.schedule_next(cur, "leave", sim_time, params)
```
（保持 leave 事件调度链不变；`states`/`events`/`random`/`timedelta`/`json` 均已 import。**范围边界**：只做状态切换 + 日志，不重算产能/man_hours——产能联动另记后续任务）

- [ ] **步骤 6：测试（追加到 test_resources.py）**

```python
def test_personnel_list_and_status_toggle():
    """人员列表含 seed 6 人；PUT 状态切换（admin token）生效；无 token 401。"""
    from fastapi.testclient import TestClient
    from demo.api import app
    from demo.auth.token_exchange import STS
    client = TestClient(app)
    # 列表
    items = client.get("/resources/personnel").json()["items"]
    assert items and all(i["status"] in ("上班", "请假") for i in items)
    pid = items[0]["id"]
    tid = STS().issue_user_token("admin-debug", "admin")
    # 无 token -> 401
    r = client.put(f"/resources/personnel/{pid}/status", json={"status": "请假"})
    assert r.status_code == 401
    # 有 token -> 切换
    r = client.put(f"/resources/personnel/{pid}/status", json={"status": "请假"},
                   headers={"X-Admin-Token": tid})
    assert r.status_code == 200 and r.json()["ok"] is True
    again = client.get("/resources/personnel").json()["items"]
    updated = next(i for i in again if i["id"] == pid)
    assert updated["status"] == "请假"
    # 回切
    client.put(f"/resources/personnel/{pid}/status", json={"status": "上班"},
               headers={"X-Admin-Token": tid})


def test_fire_leave_updates_personnel(monkeypatch):
    """模拟器 leave 事件联动 personnel 表（请假改具体人状态 + 日志）。"""
    from datetime import datetime
    from demo.simulator import engine
    # 假 conn/cur：记录 UPDATE 与 state_change_log 调用
    class FakeCur:
        def __init__(self): self.rows = []
        def execute(self, sql, params=None): self.rows.append((sql, params))
        def fetchone(self): return ("P001",)  # 有上班者/请假者
    cur = FakeCur()
    engine._fire_leave(None, cur, datetime(2026, 9, 1, 8, 0), {}, {})
    updates = [r for r in cur.rows if r[0].startswith("UPDATE personnel")]
    assert updates, "leave 应 UPDATE personnel 状态"
```
（`test_fire_leave_updates_personnel` 为轻量断言：leave 触发时确会 UPDATE personnel；完整链路由模拟器 E2E 覆盖）

- [ ] **步骤 7：跑测试 + 构建**

```bash
.venv/bin/python -m pytest demo/test_resources.py -v   # 全部 PASS（含 3 资源 + 2 人员）
.venv/bin/python -m compileall demo/simulator/engine.py demo/api.py demo/tools/data.py
```

- [ ] **步骤 8：Commit**

```bash
git add demo/schema demo/simulator/seed.py demo/tools/data.py demo/api.py demo/simulator/engine.py demo/test_resources.py
git commit -m "feat(demo): 人员模块后端——personnel 表/seed/端点 + 模拟器 leave 联动具体人"
```

---

## 任务 4：人员前端（资源 tab 加「人员」子 tab + 状态切换）

**文件：**
- 修改：`web/src/api/resources.ts`、`web/src/views/ResourcesView.vue`

- [ ] **步骤 1：api 加 `setPersonnelStatus`**

`web/src/api/resources.ts` 追加：
```ts
export async function setPersonnelStatus(id: string, status: '上班' | '请假', token: string): Promise<{ ok: boolean; message: string }> {
  const { data } = await http.put(
    `/resources/personnel/${encodeURIComponent(id)}/status`,
    { status },
    { headers: { 'X-Admin-Token': token } },
  )
  return data
}
```

- [ ] **步骤 2：ResourcesView 加「人员」类目 + 状态切换操作列**

- `categories` 数组加 `{ name: 'personnel', label: '人员' }`；`ResourceCategory` 类型加 `'personnel'`
- 模板：`active === 'personnel'` 时，表格多一列「操作」——状态切换按钮（`上班`/`请假` 互切，`:loading`，成功后 `load()`）；token 从 `localStorage.getItem('debug_admin_token')` 取，无 token 提示填

```vue
<el-table-column v-if="active === 'personnel'" label="操作" width="120" fixed="right">
  <template #default="{ row }">
    <el-button size="small" :type="row.status === '上班' ? 'warning' : 'success'"
      :loading="togglingId === row.id"
      @click="toggleStatus(row)">
      {{ row.status === '上班' ? '请假' : '回岗' }}
    </el-button>
  </template>
</el-table-column>
```
```ts
const togglingId = ref('')
async function toggleStatus(row: Record<string, unknown>) {
  const token = localStorage.getItem('debug_admin_token') || ''
  if (!token) { ElMessage.warning('请先在调试 tab 获取 admin token'); return }
  togglingId.value = String(row.id)
  try {
    const res = await setPersonnelStatus(String(row.id), row.status === '上班' ? '请假' : '上班', token)
    if (res.ok) ElMessage.success(res.message); else ElMessage.error(res.message)
    await load()
  } finally { togglingId.value = '' }
}
```
（import `ElMessage`、`setPersonnelStatus`）

- [ ] **步骤 3：构建验证**

运行：`cd web && npm run build` — `vue-tsc` 无类型错误 + `vite build` 产出 `web/dist/`

- [ ] **步骤 4：Commit**

```bash
git add web/src/api/resources.ts web/src/views/ResourcesView.vue
git commit -m "feat(web): 人员子 tab——状态展示 + 上班/请假切换（R-7 token）"
```

---

## 任务 5：端到端验收测试

**验收标准**（来自设计 spec + 用户需求）：
- A1 资源 6 类 `GET /resources/{category}` 非空 items 且 id 倒序
- A2 人员 `GET /resources/personnel` 6 人；`PUT` 状态切换生效；无 token 401
- A3 模拟器 leave 事件联动 personnel 状态（请假/回岗改具体人 + 日志）
- A4 前端「资源」tab 7 子 tab，表格完整展示、刷新可用、人员状态切换生效
- A5 既有 5 tab + 深链无回归

- [ ] **步骤 1：后端 curl 验证**

```bash
for c in machines customers orders inventory batches preprocess personnel; do
  echo "$c: $(curl -s http://localhost:8000/resources/$c | python3 -c 'import sys,json; d=json.load(sys.stdin); print(len(d["items"]), "条, 首id", d["items"][0]["id"] if d["items"] else "-")')"
done
# 人员状态切换（token 容器内签发）
TOKEN=$(docker exec demo-api python -m demo.auth.mint admin)
curl -s -X PUT http://localhost:8000/resources/personnel/P001/status -H "X-Admin-Token: $TOKEN" -H "Content-Type: application/json" -d '{"status":"请假"}'
curl -s http://localhost:8000/resources/personnel | python3 -m json.tool | grep -A1 '"id": "P001"'
# 无 token 应 401
curl -s -o /dev/null -w "%{http_code}\n" -X PUT http://localhost:8000/resources/personnel/P001/status -H "Content-Type: application/json" -d '{"status":"请假"}'
```

- [ ] **步骤 2：模拟器 leave 联动验证**

容器内观察（或临时脚本）：`docker logs demo-api | grep -i personnel` 出现 leave 触发的 `UPDATE personnel ... status 请假/上班` + `state_change_log`（`/debug` 或 DB 直查 `SELECT * FROM state_change_log WHERE entity_type='personnel' ORDER BY id DESC LIMIT 3`）

- [ ] **步骤 3：浏览器验收**

打开 `http://localhost:8000/`：资源 tab 7 子 tab（设备/客户/订单/库存/批次/前道/人员）；各表完整字段 + id 倒序 + 刷新；人员子 tab 点「请假/回岗」状态翻转；既有 5 tab 无回归

- [ ] **步骤 4：无回归**

```bash
.venv/bin/python -m pytest demo/test_resources.py demo/test_schedule_approve.py demo/simulator/ -q
```
预期：全 PASS

---

## 自检对照

- 规格覆盖：A1（6 端点）任务 1 · A2（人员端点）任务 3 · A3（leave 联动）任务 3 · A4（前端 7 子 tab + 状态切换）任务 2/4 · A5（无回归）任务 5
- 占位符扫描：所有步骤含实际代码/命令
- 类型一致：`fetchResources(category)` ↔ `GET /resources/{category}`；`setPersonnelStatus` ↔ `PUT /resources/personnel/{id}/status`
- 人员分开实现：任务 3-4 独立于任务 1-2，写在一起、分任务落地
- 范围边界：leave 联动只做状态切换 + 日志，产能/man_hours 重算不在本次（另记后续）
- 端到端验收：任务 5 为最后一个任务，逐条列出 A1-A5 验收标准 + 验证命令
