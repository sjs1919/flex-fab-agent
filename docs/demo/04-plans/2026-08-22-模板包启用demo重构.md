# 模板包启用到 agent-training 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 把 project-standards v1.2 模板包启用到 agent-training 仓库根（CLAUDE.md 薄入口 + rules 三层 + 四门禁 hooks + docs/demo 编号目录迁移 + 回填模板包 v1.3），并完成四项启用自检。

**架构：** 规则落位 agent-training 根目录（全仓生效，job-portfolio 等轻量文档区豁免四门禁）；新增 stack-python 6 文件规则包（替换 stack-go/stack-vue3）；docs/demo 内设 5 个编号子目录承接 spec/plan/tasklist 流转；模板包本体同步回填 v1.3。

**技术栈：** 纯文档/配置变更（Markdown + shell hook + git），零代码改动；验证靠文件存在性检查 + hook 脚本直测 + `python run_all_tests.py`。

**依据 spec：** `docs/demo/02-specs/2026-08-22-模板包启用demo重构-design.md`（§1 落位 / §2 stack-python / §3 docs 映射 / §4 占位符 / §5 回填 / §6 自检）

---

## 🔐 授权清单（用户已一次性授权，执行时不再逐项询问）

| # | 授权操作 | 范围 |
|---|---------|------|
| A1 | agent-training 仓库内多次 `git add` + `git commit`（约 7 笔，见各任务） | 仅本计划列出的文件 |
| A2 | `git mv` 迁移 docs/demo 下 5 份文档到编号子目录 | 见任务 4 |
| A3 | `git config core.hooksPath .githooks` + `chmod +x` 两个 hook 脚本 | 仓库本地配置，可逆 |
| A4 | 先提交预存的 project-standards v1.2 未提交变更（pitfalls.md + README/rules-index 修改，用户 08-21 的工作） | 单独一笔 commit，不与本计划混 |
| A5 | 直测 hook 脚本（模拟 force push JSON 输入喂 guard 脚本、临时文件喂 commit-msg），**不执行真实 push** | 本地 |
| A6 | 运行 `python run_all_tests.py`（约 128 个测试，mock LLM 零成本） | 本地 |
| A7 | 修改 agent-training 根 `.gitignore`（`.claude/` 整目录忽略改为忽略缓存、放行 settings.json/hooks） | 见任务 1 |
| A8 | **不 push 远程**：全部提交留在本地，push 由用户自行发起 | - |

**工作方式**：全程串行（workspace 红线：编码阶段并行 Agent ≤ 2，本计划无需并行）。

---

## 文件结构

| 动作 | 文件 | 职责 |
|------|------|------|
| 创建 | `CLAUDE.md` | 薄入口：项目定位 + Top 红线 + 豁免区/消歧声明 + 命令速查 |
| 创建 | `AGENTS.md` | Agent 薄引用（Trae CLI 自动加载） |
| 创建 | `.claude/settings.json` | 权限白名单 + deny 兜底 + PreToolUse guard hook |
| 创建 | `.claude/hooks/guard-hard-interrupt.sh` | 拦截 force push/部署/ssh 写 |
| 创建 | `.githooks/commit-msg` | conventional commit 格式校验 |
| 修改 | `.gitignore` | `.claude/` 忽略改精确放行；补 logs/dist/coverage |
| 创建 | `rules/rules-index.md` | 触发表（含 stack-python 6 行） |
| 创建 | `rules/common/*.md` ×10 | 通用层照搬（docs-flow 头部加路径映射行） |
| 创建 | `rules/stack-python/*.md` ×6 | arch/error/logging/database/testing/ops-build |
| 不复制 | `rules/stack-go/`、`rules/stack-vue3/` | 本仓用不到（spec §1） |
| 迁移 | `docs/demo/` 5 文档 -> 01-requirements/02-specs/04-plans/09-reports | spec §3 |
| 创建 | `docs/demo/credentials.local.md.example` | 凭据占位符映射表 |
| 修改 | `docs/demo/05-tasklist/todo-模板启用与生产化评审-20260822.md` | T-3 完成标记 |
| 修改 | `docs/demo/生产化差距与部署蓝图-v1-2026-08-22.md` 等 | 迁移后链接修正 |
| 修改 | `README.md`、`docs/00_文档地图.md`、`demo/README.md` | 旧路径链接修正 |
| 回填 | `project-standards/code-repo-template/rules/stack-python/` ×6 | 模板包 v1.3 |
| 修改 | `project-standards/code-repo-template/rules/rules-index.md`、`project-standards/README.md` | 追加触发行 + 变更记录 |

---

### 任务 0：预存 project-standards v1.2 变更单独提交

**文件：**
- 提交（他人工作，不改内容）：`project-standards/README.md`、`project-standards/code-repo-template/rules/rules-index.md`、`project-standards/code-repo-template/rules/common/pitfalls.md`

- [ ] **步骤 1：确认变更内容**（防止误提交）

```bash
cd /c/workspace/projects/agent-training
git status -s
git diff project-standards/README.md project-standards/code-repo-template/rules/rules-index.md
cat project-standards/code-repo-template/rules/common/pitfalls.md
```

预期：README 含 `[1.2] - 2026-08-21` 变更记录；rules-index 含 pitfalls 触发行；pitfalls.md 为踩坑记录规则文件。若内容与预期不符，停下问用户。

- [ ] **步骤 2：单独提交**

```bash
git add project-standards/README.md project-standards/code-repo-template/rules/rules-index.md project-standards/code-repo-template/rules/common/pitfalls.md
git commit -m "chore(standards): 回填 v1.2 踩坑记录机制(pitfalls.md + 索引触发行)"
```

---

### 任务 1：模板骨架复制 + 占位符替换

**文件：**
- 创建：`CLAUDE.md`、`AGENTS.md`、`.claude/settings.json`、`.claude/hooks/guard-hard-interrupt.sh`、`.githooks/commit-msg`
- 修改：`.gitignore`

- [ ] **步骤 1：复制骨架**

```bash
cd /c/workspace/projects/agent-training
mkdir -p .claude/hooks .githooks rules
cp project-standards/code-repo-template/.claude/hooks/guard-hard-interrupt.sh .claude/hooks/
cp project-standards/code-repo-template/.githooks/commit-msg .githooks/
```

- [ ] **步骤 2：写 CLAUDE.md**（模板占位符已替换 + 本项目附加红线）

```markdown
# agent-training-demo 项目规范（薄入口 · 基于模板 v1.2）

## 项目定位

制造业排程排产多 Agent 助手（第二阶段重构）。文档仓：docs/demo/（spec / plan / todo 全部写入该目录编号子目录）。

## Top 红线

- 🚫 禁止自动 git：commit / push / pull 必须用户发起
- 🚫 禁止自主部署：编译测试通过后必须经用户批准（门禁4）
- 🚫 未测试禁止部署：python run_all_tests.py 全过 + 构建成功才可提请部署
- 🚫 敏感信息不入库：密码/密钥只留占位符，真实值在 docs/demo/credentials.local.md

## 附加红线（本项目特有）

1. **豁免区**：`job-portfolio/`、`docs/`（demo 子目录除外）为轻量文档区，不走四门禁流程；仅守 commit 格式与敏感信息两条红线。
2. **冲突消歧**：与 workspace 根 CLAUDE.md 冲突时（如并行 Agent 上限、命令速查），以本仓 `rules/rules-index.md` 及其加载文件为准。

## 编码红线（Karpathy 原则）

1. **先思考** - 明假设、曝困惑、呈权衡；不默选、敢反对
2. **简单优先** - 最少代码解决问题；不预设扩展；200 行能 50 行就重写
3. **外科手术** - 只改要求的；不动相邻代码/格式；保持现有风格；清理无用 import
4. **目标驱动** - 先定义可验证标准；多步任务列「步骤->验证」清单

## 命令速查

测试 `python run_all_tests.py` · 构建 `docker compose build`（WSL 下）· 静态检查 `python -m compileall demo`

## 规则总索引

全部规则见 `rules/rules-index.md`（按触发场景加载）。本文件不承载规则条款。
```

- [ ] **步骤 3：写 AGENTS.md**

```markdown
# agent-training-demo - Agent 指引（薄引用）

规则单一事实源：`rules/` 目录。总索引：@rules/rules-index.md

本文件不承载任何规则条款；新增/修改规则一律改 `rules/`（Trae CLI 会自动加载上行 `@` 引用的文件）。
```

- [ ] **步骤 4：写 .claude/settings.json**（占位符替换：TEST_CMD/BUILD_CMD/LINT_CMD）

```json
{
  "permissions": {
    "allow": [
      "Bash(python run_all_tests.py:*)",
      "Bash(python -m compileall demo:*)",
      "Bash(docker compose build:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git fetch:*)",
      "Bash(git branch:*)"
    ],
    "deny": [
      "Bash(docker push:*)",
      "Bash(docker rm:*)",
      "Bash(docker restart:*)",
      "Bash(scp:*)",
      "Bash(rsync:*)",
      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(git reset --hard:*)",
      "Bash(git branch -D:*)",
      "Bash(rm -rf:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "sh .claude/hooks/guard-hard-interrupt.sh" }
        ]
      }
    ]
  }
}
```

- [ ] **步骤 5：改 .gitignore**（`.claude/` 整目录忽略 -> 放行 settings/hooks；补模板缺失项）

将现有行：
```
# Claude Code 本地计划缓存（工具生成，勿提交）
.claude/
```
替换为：
```
# Claude Code 本地缓存（工具生成，勿提交；保留 settings.json 与 hooks/ 供模板启用）
.claude/*
!.claude/settings.json
!.claude/hooks/
```
并在文件末尾追加：
```
# 模板补充忽略（project-standards .gitignore 对齐）
logs/
*.log
dist/
coverage*
```

- [ ] **步骤 6：验证**

```bash
ls CLAUDE.md AGENTS.md .claude/settings.json .claude/hooks/guard-hard-interrupt.sh .githooks/commit-msg
git check-ignore .claude/settings.json .claude/hooks/guard-hard-interrupt.sh || echo "OK: settings/hooks 未被忽略"
git check-ignore .env && echo "OK: .env 仍被忽略"
```

预期：5 个文件存在；`git check-ignore` 第一条**无输出**（未忽略即正确），第二条输出 `.env`。

- [ ] **步骤 7：Commit**

```bash
git add CLAUDE.md AGENTS.md .claude .githooks .gitignore
git commit -m "chore(standards): 模板骨架落位--CLAUDE/AGENTS薄入口+权限白名单+hooks+gitignore放行.claude"
```

---

### 任务 2：rules 通用层复制 + docs-flow 路径映射

**文件：**
- 创建：`rules/common/` 10 文件（照搬，不改条款）
- 创建：`rules/rules-index.md`（基础表，stack-go/vue3 行删除，stack-python 行任务 3 追加）

- [ ] **步骤 1：复制 common 层 + 基础索引**

```bash
cd /c/workspace/projects/agent-training
cp -r project-standards/code-repo-template/rules/common rules/
cp project-standards/code-repo-template/rules/rules-index.md rules/
```

- [ ] **步骤 2：rules-index.md 三处修改**

1. 删除触发表中 3 行 stack-go 行（`| Go 测试执行 / WSL / Mock / 字段对齐 |`、`| handler / service / repository 分层 |` 到 `| 构建 / 服务重启 / 版本约束 |` 共 8 行）和 2 行 stack-vue3 行（`| 前端测试执行 / Vitest / Playwright |`、`| 前端组件 / 页面 / 状态管理 |`）
2. 删除表尾「新栈追加协议」整节（启用后的项目不需要追加协议，那是模板包的事）
3. 表头优先级行保持不变

- [ ] **步骤 3：common/docs-flow.md 头部加路径映射行**

在 `# 文档流转规范` 标题下插入：

```markdown

> **本项目路径映射**（单仓变体）：模板文档仓目录映射到 `docs/demo/` 下--01-requirements -> `docs/demo/01-requirements/`、02-specs -> `docs/demo/02-specs/`、04-plans -> `docs/demo/04-plans/`、05-tasklist -> `docs/demo/05-tasklist/`、09-reports -> `docs/demo/09-reports/`；待办登记见 `05-tasklist/todo.md`。
```

同时把 docs-flow.md 内所有 `{{DOCS_REPO}}` 替换为 `docs/demo/`。

- [ ] **步骤 4：全 common 层占位符替换**

```bash
grep -rn '{{' rules/common/ || echo "OK: 无残留占位符"
grep -rln '{{DOCS_REPO}}' rules/common/ | xargs -r sed -i 's|{{DOCS_REPO}}|docs/demo/|g'
```

预期：替换后 `grep -rn '{{' rules/common/` 无输出（`{{PROJECT_NAME}}` 等如出现也一并替换为本项目值）。

- [ ] **步骤 5：验证**

```bash
ls rules/common/ | wc -l    # 预期 10
grep -c 'stack-go\|stack-vue3' rules/rules-index.md    # 预期 0
grep -n '本项目路径映射' rules/common/docs-flow.md    # 预期命中 1 行
```

- [ ] **步骤 6：Commit**

```bash
git add rules/
git commit -m "chore(standards): rules通用层照搬+删go/vue3栈行+docs-flow单仓路径映射"
```

---

### 任务 3：stack-python 6 规则文件

**文件：**
- 创建：`rules/stack-python/arch.md`、`error.md`、`logging.md`、`database.md`、`testing.md`、`ops-build.md`
- 修改：`rules/rules-index.md`（追加 6 触发行）

- [ ] **步骤 1：写 arch.md**

```markdown
# 架构规则（stack-python / demo）

## 分层不变式

- 分层：`main.py/api.py`（入口）-> `agents/graph`（编排）-> `tools`（工具层）-> `core/rag/cache`（基座）-> `data`（数据）。禁止跨层反向依赖（如 tools 直接调 graph）。
- `auth/` 与 `observability/` 是横切层：经 registry/tracer 挂接，业务层不得绕过。

## 工具注册（唯一入口）

- 新工具**只经** `tools/registry.py:build_default_registry()` 注册，自动继承参数白名单/RBAC/sandbox 重试/tracer span 全管线；禁止散落的直调函数。
- 工具 =「函数 + docstring」：docstring 写清触发条件（LLM 选择依据），遵循现有风格。
- 写操作工具必须带 `read_only=False` 标记 + schema 注明权限要求（与 D3 治理衔接）。

## 数据源与配置

- `tools/data.py` 的 DataSource 抽象是数据层唯一入口（`DEMO_DATA_SOURCE=csv|mysql` 切换），工具函数签名返回 `list[dict]` 不变。
- `config.py` 是配置唯一来源；禁止业务代码散落 `os.getenv`。

## 求解器/模拟器边界

- `demo/scheduler/`、`demo/simulator/` 是独立模块，与 Agent 只经工具通信；模拟器不做业务决策，求解器无常驻进程。
```

- [ ] **步骤 2：写 error.md**

```markdown
# 错误处理规则（stack-python）

- 🚫 严禁「打日志然后丢弃」：异常必须向上传播（`raise ... from e` 或返回明确错误结构），上层无感知即缺陷。
- 错误信息用中文，专有名词保留英文。
- 工具返回值统一经 sandbox 三元组 `(result, success, retries)`；失败重试次数写日志并进 tracer span。
- 业务错误定义 sentinel 常量（如 `SCHEDULER_TIMEOUT`、`DB_UNAVAILABLE`），禁止裸字符串比较。
- 求解器超时不算失败：返回次优可行解 + 求解耗时（见蓝图 R-D1）。
```

- [ ] **步骤 3：写 logging.md**

```markdown
# 观测与审计规则（stack-python）

- 观测走 `observability/tracer.py` 的 span（contextmanager），禁止业务代码散落 `print`；调试输出用 tracer 或 logging。
- 新增子系统必须可被 trace_id 贯穿（LLM/工具/求解器/模拟器统一）。
- 鉴权决策、审批、重排触发、模拟器事件必须写 `auth/audit_logger.py`（JSONL，subject 标注来源）。
- 模拟器独立线程的 span 上下文必须与主线程隔离（线程局部），防止 span 串线。
- 求解器 span 必须记录：耗时、目标值、版本号、约束违反数。
```

- [ ] **步骤 4：写 database.md**

```markdown
# 数据库规则（stack-python / MySQL）

## 环境红线

- **MySQL / PostgreSQL / Chroma 向量库服务一律跑 WSL**；Windows 侧只经 `localhost` 访问（WSL2 端口转发）。🚫 禁止在 Windows 本机安装这些服务。

## 连接与事务

- 连接串统一 `config.py:MYSQL_DSN`（.env 配置）；连接池后续引入时只改 data 层。
- 短事务原则：模拟器 tick 内「批次->设备->状态日志」多写必须单事务原子提交，失败整体回滚（禁止半写脏状态）。
- 单写者原则：模拟器是运行数据唯一写者（agent 经工具写审批/排产除外）；API/agent 读侧不要求强一致。

## 数据规则

- **tenant_id 过滤强制**（R8）：所有业务查询带 `WHERE tenant_id=?`，新表新查询不得遗漏。
- schema 变更必须有回滚路径（down 脚本或备份兜底）。
- 种子数据脚本化可重建（`demo/simulator/seed.py`），禁止手改库。
- 密钥只在 `.env` / `docs/demo/credentials.local.md`；代码与文档永远留占位符。
- 基础设施存储（checkpoints.db/tokens.db/llm_cache.db/cache_db/chroma_db/audit.jsonl/cases.jsonl）不进 MySQL，边界见重构方案 §2.2。
```

- [ ] **步骤 5：写 testing.md**

```markdown
# 测试规则（stack-python）

- 测试先行（TDD）：先写失败测试，再写实现。
- 单测 mock LLM（零成本）；`run_all_tests.py` 是 CI 入口，全绿才过门禁。
- eval 回归基线 **>=7/10**（case >=0.6 判 pass）；ground_truth 与种子数据变更必须同步。
- 新工具必须有单测：正常路径 + 权限拒绝路径 + 参数白名单拒绝路径。
- 求解器测试带样例输入断言：C1-C9 约束自检 + 无可行解返回冲突清单（不静默）。
- 运行：`python run_all_tests.py`（全量）；`python -m pytest demo/path/test_x.py -v`（单个）。
```

- [ ] **步骤 6：写 ops-build.md**

```markdown
# 运维与构建规则（stack-python）

- Python **3.11 锁定**；依赖增删改 requirements-demo.txt 且不随意升级版本。
- **Docker / 前端 / 镜像构建必须在 WSL 下执行**（Windows npm/Docker 有 EPERM 文件锁与上下文爆炸教训）。
- **MySQL / PostgreSQL / Chroma 向量库服务跑 WSL**（见 database.md 环境红线）。
- 不入库：`.env`、`checkpoints.db`、`cache_db/`、`chroma_db/`、`*.log`、`demo/eval/reports/`。
- compose 必须带健康检查；`/health` 端点需报数据源与模拟器 tick 状态。
- 服务器操作读/写边界：读直接执行，写必须先问用户（rules/common/server-approval.md）。
```

- [ ] **步骤 7：rules-index.md 追加触发行**（表尾、`| 服务器操作` 行之后）

```markdown
| demo 分层 / 工具注册 / 数据源抽象 | -> `stack-python/arch.md` |
| 异常处理 / 工具返回值 | -> `stack-python/error.md` |
| 观测 / 审计 / span | -> `stack-python/logging.md` |
| MySQL / 迁移 / 种子数据 / tenant_id / WSL 数据库 | -> `stack-python/database.md` |
| Python 测试执行 / pytest / mock LLM / eval | -> `stack-python/testing.md` |
| Python 版本 / Docker / WSL 构建 | -> `stack-python/ops-build.md` |
```

- [ ] **步骤 8：验证**

```bash
ls rules/stack-python/ | wc -l    # 预期 6
grep -c 'stack-python' rules/rules-index.md    # 预期 6
```

- [ ] **步骤 9：Commit**

```bash
git add rules/stack-python rules/rules-index.md
git commit -m "feat(standards): stack-python规则包6文件--arch/error/logging/database/testing/ops-build+触发行"
```

---

### 任务 4：docs/demo 编号目录迁移 + 链接修正

**文件：**
- 迁移：5 份文档（git mv 保留历史）
- 创建：`docs/demo/credentials.local.md.example`
- 修改：`README.md`、`docs/00_文档地图.md`、`demo/README.md`、`docs/demo/05-tasklist/todo-模板启用与生产化评审-20260822.md`、`docs/demo/生产化差距与部署蓝图-v1-2026-08-22.md`、`docs/demo/02-specs/2026-08-22-模板包启用demo重构-design.md` 中指向旧路径的链接

- [ ] **步骤 1：建目录 + git mv**

```bash
cd /c/workspace/projects/agent-training/docs/demo
mkdir -p 01-requirements 02-specs 04-plans 09-reports
git mv rq-排程排产需求.md 01-requirements/
git mv 需求规格-v1-2026-08-21.md 02-specs/
git mv 排程排产需求规格-v1-2026-08-21.md 02-specs/
git mv 重构方案-v1-2026-08-21.md 04-plans/
git mv 盲区报告-JD对标与demo追赶路线-v1-2026-08-21.md 09-reports/
git mv 评测报告-RAG质量-ragas-v1-2026-08-21.md 09-reports/
```

- [ ] **步骤 2：放 credentials 示例**

```bash
cp ../../project-standards/docs-repo-template/credentials.local.md.example credentials.local.md.example
```

预期：`git check-ignore credentials.local.md` 命中（`*.local.*` 规则已有，真实值文件不会入库）。

- [ ] **步骤 3：全仓旧路径链接扫描与修正**

```bash
cd /c/workspace/projects/agent-training
grep -rn 'docs/demo/需求规格-v1\|docs/demo/重构方案-v1\|docs/demo/rq-\|docs/demo/盲区报告\|docs/demo/评测报告\|docs/demo/排程排产需求规格' --include='*.md' README.md docs/ demo/ job-portfolio/ | grep -v project-standards
```

对每个命中文件，按映射修正链接（同目录引用 `重构方案-v1-2026-08-21.md` -> `04-plans/重构方案-v1-2026-08-21.md` 等）。重点已知命中：
- `demo/README.md`（头部 3 处：需求规格/8大缺陷/重排引用，前两处指向 `../docs/demo/`）
- `docs/00_文档地图.md`（demo 节）
- `docs/demo/05-tasklist/todo-*.md` 与 `docs/demo/生产化差距与部署蓝图-v1-*.md`（本次新写的相对链接：`../重构方案-v1...` -> `../04-plans/重构方案-v1...`；`../生产化差距...` 引用不变但生产化蓝图自身在 demo 根不动）
- `docs/superpowers/specs/2026-08-22-*-design.md`（关联表两处 `../../demo/04-plans/...`、`../../demo/02-specs/...` 已是新路径，核对即可）
- `docs/demo/01-requirements/rq-排程排产需求.md`、`02-specs/*`、`04-plans/*`、`09-reports/*` 之间互链

- [ ] **步骤 4：验证链接**

```bash
grep -rn 'docs/demo/需求规格-v1\|docs/demo/重构方案-v1\|docs/demo/rq-\|docs/demo/盲区报告\|docs/demo/评测报告\|docs/demo/排程排产需求规格' --include='*.md' README.md docs/ demo/ | grep -v project-standards | grep -v '_archived' || echo "OK: 旧路径清零"
ls docs/demo/01-requirements docs/demo/02-specs docs/demo/04-plans docs/demo/09-reports docs/demo/05-tasklist
```

预期：旧路径清零；5 个编号目录内容齐全（05-tasklist 含 todo.md + 活跃任务）。

- [ ] **步骤 5：Commit**

```bash
git add -A docs/demo demo/README.md README.md docs/00_文档地图.md
git commit -m "docs(demo): 文档迁移编号目录01/02/04/09+credentials示例+全仓链接修正"
```

---

### 任务 5：hooks 启用

**文件：**
- 修改（仓库配置）：`.git/config`（hooksPath）

- [ ] **步骤 1：赋权 + 启用**

```bash
cd /c/workspace/projects/agent-training
chmod +x .claude/hooks/guard-hard-interrupt.sh .githooks/commit-msg
git config core.hooksPath .githooks
```

- [ ] **步骤 2：验证**

```bash
git config core.hooksPath    # 预期输出 .githooks
ls -l .githooks/commit-msg .claude/hooks/guard-hard-interrupt.sh | grep -c 'x'    # 预期 2（可执行位）
```

- [ ] **步骤 3：本任务无独立 commit**（配置只影响本地，不入库；已在任务 1 提交过文件本体）

---

### 任务 6：回填模板包 v1.3

**文件：**
- 创建：`project-standards/code-repo-template/rules/stack-python/` ×6（从 rules/stack-python 复制）
- 修改：`project-standards/code-repo-template/rules/rules-index.md`（模板版追加触发行）
- 修改：`project-standards/README.md`（变更记录加 [1.3]）

- [ ] **步骤 1：复制 stack-python 到模板包**

```bash
cd /c/workspace/projects/agent-training
cp -r rules/stack-python project-standards/code-repo-template/rules/
```

- [ ] **步骤 2：模板 rules-index.md 追加触发行**（与任务 3 相同 6 行，加在 stack-vue3 行之后；**保留**模板版的「新栈追加协议」节）

- [ ] **步骤 3：模板 README.md 变更记录追加**（`## 模板变更记录` 节内、`[1.2]` 之后）

```markdown
### [1.3] - 2026-08-22

#### Added

- stack-python 技术栈规则包（6 文件：arch/error/logging/database/testing/ops-build）--首个非 Go/Vue3 栈，验证「三步追加协议」可裁剪落地（九文件骨架按项目实际裁为 6）
- 来源：agent-training demo 第二阶段重构启用（落位方案见 agent-training `docs/demo/02-specs/2026-08-22-模板包启用demo重构-design.md`）
```

同时更新 README 头部 `TEMPLATE_VERSION: 1.2` -> `TEMPLATE_VERSION: 1.3`。

- [ ] **步骤 4：验证**

```bash
ls project-standards/code-repo-template/rules/stack-python/ | wc -l    # 预期 6
grep -c 'stack-python' project-standards/code-repo-template/rules/rules-index.md    # 预期 6
grep -n '\[1.3\]' project-standards/README.md    # 预期命中
```

- [ ] **步骤 5：Commit**

```bash
git add project-standards/
git commit -m "chore(standards): 模板包回填v1.3--stack-python规则包+变更记录"
```

---

### 任务 7：端到端验收（四项自检 + spec 覆盖核对）🔴 门禁

> 本任务对照 spec §6 四项自检 + §1-§5 交付物逐条验收。**全绿之前禁止宣称启用完成。**

- [ ] **步骤 1：自检① force push 拦截**（直测 guard 脚本，非真实 push）

```bash
cd /c/workspace/projects/agent-training
echo '{"tool_name":"Bash","tool_input":{"command":"git push --force origin main"}}' | sh .claude/hooks/guard-hard-interrupt.sh
echo "exit=$?"
```

预期：stderr 输出 `[guard] 已拦截：破坏性 git：force push`，`exit=2`。

- [ ] **步骤 2：自检①补充**（合法命令放行）

```bash
echo '{"tool_name":"Bash","tool_input":{"command":"python run_all_tests.py"}}' | sh .claude/hooks/guard-hard-interrupt.sh
echo "exit=$?"
```

预期：无拦截输出，`exit=0`。

- [ ] **步骤 3：自检③ 非法 commit message 拒绝**

```bash
echo "update stuff" > /tmp/commit-msg-test.txt
.githooks/commit-msg /tmp/commit-msg-test.txt
echo "exit=$?"
echo "docs(test): 合法样例" > /tmp/commit-msg-test.txt
.githooks/commit-msg /tmp/commit-msg-test.txt
echo "exit=$?"
rm /tmp/commit-msg-test.txt
```

预期：第一条 `exit=1`（格式错误提示）；第二条 `exit=0`。

- [ ] **步骤 4：自检② 测试命令运行**（mock LLM 全量）

```bash
python run_all_tests.py
```

预期：全量单测+集成 **128 passed**（或不少于现有基线；若 mock LLM 零成本路径报错，停下按 systematic-debugging 处理，禁止跳过）。

- [ ] **步骤 5：自检④ 规则可列出**

```bash
cat rules/rules-index.md
```

预期：触发表含 common 10 文件行 + stack-python 6 行，无 stack-go/vue3 行，无 `{{` 占位符残留。

- [ ] **步骤 6：spec 覆盖逐条核对**（对照 spec §1-§6）

| spec 条目 | 验证 | 预期 |
|-----------|------|------|
| §1 落位（CLAUDE/AGENTS/rules/.claude/.githooks + 删栈） | `ls CLAUDE.md AGENTS.md rules/ .claude/ .githooks/` | 齐全；无 stack-go/vue3 |
| §1 附加红线（豁免区+消歧） | `grep -n '豁免区\|冲突消歧' CLAUDE.md` | 各命中 |
| §2 stack-python 6 文件 | `ls rules/stack-python/` | 6 文件 |
| §2 数据库 WSL 红线（用户追加） | `grep -n 'WSL' rules/stack-python/database.md rules/stack-python/ops-build.md` | 两文件均含 MySQL/PG/Chroma 跑 WSL 条款 |
| §3 docs 5 目录 + credentials | `ls docs/demo/` | 01/02/04/05/09 + credentials.local.md.example |
| §4 占位符取值 | `grep -rn '{{' CLAUDE.md AGENTS.md .claude/settings.json rules/ \|\| echo OK` | 无残留 |
| §5 回填 v1.3 | `grep -n '\[1.3\]' project-standards/README.md` | 命中 |
| §6 四项自检 | 步骤 1-5 | 全过 |

- [ ] **步骤 7：收尾标记 + Commit**

```bash
# 05-tasklist 的 T-3 标记 ✅（表格该行状态 📋 -> ✅）
git add docs/demo/05-tasklist/
git commit -m "docs(tasklist): 模板启用T-3完成标记"
```

- [ ] **步骤 8：向用户报告**

格式：`spec 共 8 组验收项 -> 全部 ✅ | 自检四项 -> ①拦截✅②测试 X passed✅③格式拒绝✅④规则列出✅ | commit 共 N 笔本地（未 push） | 遗留：无/[清单]`

> **遗留说明**：`.claude/settings.json` 的 PreToolUse hook 与权限白名单只在「会话从 agent-training 目录启动」时生效（Claude Code 按启动目录加载）。下次在 agent-training 内开会话即自动加载，无需额外动作。

---

## 自检记录（writing-plans 写后验证）

1. **规格覆盖度**：spec §1 落位->任务1/2/5；§2 stack-python->任务3；§3 docs 映射->任务4；§4 占位符->任务1/2/4/7；§5 回填->任务6；§6 自检->任务7。遗漏 0。
2. **占位符扫描**：无「待定/TODO/后续实现」；规则文件与 CLAUDE.md 均给出全文。
3. **类型一致性**：目录名（01-requirements/02-specs/04-plans/05-tasklist/09-reports）、文件名、hook 路径全文一致。
4. **测试覆盖度**：本计划为文档/配置变更，单元测试等价物 = 每任务的验证步骤（文件存在性/内容断言）；端到端验收 = 任务 7（四项自检 + spec 8 组覆盖核对 + 全量 pytest 128 passed）。
5. **用户追加约束**：MySQL/PostgreSQL/Chroma 跑 WSL 已落入 database.md 与 ops-build.md（任务 3 步骤 4/6）并在任务 7 步骤 6 设核对项。
