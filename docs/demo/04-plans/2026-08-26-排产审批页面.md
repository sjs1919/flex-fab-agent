# 排产审批页面 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 新增排产版本列表 + 审批（通过/驳回）的 REST API 和前端页面，打通「排产 → 审批 → 生产 → 模拟事件」闭环；并收尾语义缓存 TTL 改造的全量验证。

**架构：** FastAPI 新增 `GET /schedule/versions`（版本列表）和 `POST /schedule/approve`（审批，R-7 `require_admin` 鉴权，复用 `approve_schedule` 业务）；前端新增独立审批页 `ScheduleView.vue` + `api/schedule.ts` 封装，vite proxy 同源访问，构建产物由 FastAPI StaticFiles 同源托管。

**技术栈：** FastAPI + pymysql + pytest(TestClient)；Vue3 + TypeScript + Vite + element-plus + axios。

**关联现状：**
- 已有 `GET /schedule/latest`、`POST /schedule/load`（R-7 鉴权）
- `approve_schedule(version_id, action, note, approver)` 已实现（scheduler_tools.py:107），action=通过|驳回，单事务更新 schedule_versions.status + batches.approval_status + approvals
- 测试已有 admin token helper：`STS().issue_user_token("admin-debug", "admin")`（test_api_debug.py:112）
- 语义缓存 TTL 改造（config/semantic_cache/manager/single_agent/runner/scheduler_tools）已完成代码，待全量验证

---

## 文件结构

| 文件 | 动作 | 职责 |
|------|------|------|
| `demo/api.py` | 修改 | 新增 `GET /schedule/versions`、`POST /schedule/approve` |
| `demo/test_schedule_approve.py` | 创建 | 审批/列表端点集成测试（TestClient + 真 MySQL） |
| `web/src/api/schedule.ts` | 创建 | 前端 API 封装（列表 + 审批） |
| `web/src/views/ScheduleView.vue` | 创建 | 审批页面：版本列表 + 通过/驳回按钮 + admin token 输入 |
| `web/src/router/index.ts` | 修改 | 新增 `/portal/schedule` 路由 |
| `web/vite.config.ts` | 修改 | proxy 增加 `/schedule` |
| `demo/api.py`（静态托管） | 修改 | FastAPI 挂载 `web/dist` 静态目录（同源服务前端） |
| `docker-compose.yml` | 修改 | 挂载 `web/dist` 进容器 |

---

## 任务 1：REST 端点（列表 + 审批）

**文件：**
- 修改：`demo/api.py`（在 `schedule_load` 后新增端点）
- 测试：`demo/test_schedule_approve.py`

- [ ] **步骤 1：编写失败测试**

```python
"""审批/排产列表端点集成测试（TestClient + 真 MySQL + STS admin token）。"""
from fastapi.testclient import TestClient

from demo.api import app
from demo.auth.token_exchange import STS


def _admin_token() -> str:
    return STS().issue_user_token("admin-debug", "admin")


def test_versions_list_returns_versions():
    """GET /schedule/versions 返回版本列表（含 id/status/batch_count）。"""
    client = TestClient(app)
    r = client.get("/schedule/versions")
    assert r.status_code == 200
    data = r.json()
    assert isinstance(data["versions"], list)
    assert data["versions"], "至少有一个已生成的排产版本"


def test_approve_rejects_without_token():
    """POST /schedule/approve 无 admin token -> 401。"""
    client = TestClient(app)
    r = client.post("/schedule/approve", json={"version_id": 1, "action": "通过"})
    assert r.status_code == 401


def test_approve_flow():
    """审批链路：取最新待审核版本 -> 通过 -> 状态变已审核。"""
    client = TestClient(app)
    tid = _admin_token()
    versions = client.get("/schedule/versions").json()["versions"]
    pending = [v for v in versions if v["status"] == "待审核"]
    assert pending, "应有待审核版本"
    vid = pending[0]["id"]
    r = client.post("/schedule/approve", json={"version_id": vid, "action": "通过"},
                    headers={"X-Admin-Token": tid})
    assert r.status_code == 200
    assert r.json()["ok"] is True
    # 重新拉列表确认状态
    again = client.get("/schedule/versions").json()["versions"]
    updated = next(v for v in again if v["id"] == vid)
    assert updated["status"] == "已审核"
```

- [ ] **步骤 2：运行测试确认失败**

运行：`python -m pytest demo/test_schedule_approve.py -v`
预期：`test_approve_rejects_without_token` 因端点 404 失败（GET /schedule/versions 同理）

- [ ] **步骤 3：实现端点**

在 `demo/api.py` 的 `schedule_load` 后追加：

```python
class ScheduleVersionList(BaseModel):
    id: int
    created_at: str
    triggered_by: str
    status: str
    batch_count: int


@app.get("/schedule/versions")
def schedule_versions() -> dict:
    """排产版本列表（只读）：id/时间/触发/状态/批次数，供审批页展示。"""
    from .tools.data import get_connection

    with get_connection() as conn:
        with conn.cursor() as cur:
            cur.execute(
                "SELECT v.id, v.created_at, v.triggered_by, v.status, "
                "(SELECT COUNT(*) FROM batches b WHERE b.schedule_version_id=v.id) "
                "FROM schedule_versions v ORDER BY v.id DESC")
            versions = [
                {"id": r[0], "created_at": str(r[1]), "triggered_by": r[2],
                 "status": r[3], "batch_count": r[4]}
                for r in cur.fetchall()
            ]
    return {"versions": versions}


@app.post("/schedule/approve")
def schedule_approve(body: ScheduleApproveRequest,
                     _admin: str = Depends(require_admin)) -> dict:
    """审批排产版本（写端点，强制 admin token，R-7）。action: 通过|驳回。"""
    from .tools.scheduler_tools import approve_schedule
    result = approve_schedule(body.version_id, body.action, body.note)
    return {"ok": result.startswith("✅"), "message": result}
```

（`ScheduleApproveRequest` model 已在本会话早前加过，若无则在 `ScheduleApproveRequest` 处补：`version_id: int`、`action: str`、`note: str = ""`）

- [ ] **步骤 4：运行测试验证通过**

运行：`python -m pytest demo/test_schedule_approve.py -v`
预期：3 个测试全 PASS

- [ ] **步骤 5：Commit**

```bash
git add demo/api.py demo/test_schedule_approve.py
git commit -m "feat(demo): 排产版本列表 + 审批 REST 端点（R-7）"
```

---

## 任务 2：前端审批页面

**文件：**
- 创建：`web/src/api/schedule.ts`
- 创建：`web/src/views/ScheduleView.vue`
- 修改：`web/src/router/index.ts`、`web/vite.config.ts`

- [ ] **步骤 1：创建 API 封装 `web/src/api/schedule.ts`**

```ts
import axios from 'axios'

const http = axios.create({ baseURL: '/', timeout: 15_000 })

export interface ScheduleVersion {
  id: number
  created_at: string
  triggered_by: string
  status: string
  batch_count: number
}

export async function fetchVersions(): Promise<ScheduleVersion[]> {
  const { data } = await http.get<{ versions: ScheduleVersion[] }>('/schedule/versions')
  return data.versions
}

export async function approveSchedule(versionId: number, action: '通过' | '驳回', token: string): Promise<{ ok: boolean; message: string }> {
  const { data } = await http.post(
    '/schedule/approve',
    { version_id: versionId, action },
    { headers: { 'X-Admin-Token': token } },
  )
  return data
}
```

- [ ] **步骤 2：创建页面 `web/src/views/ScheduleView.vue`**

```vue
<script setup lang="ts">
import { onMounted, ref } from 'vue'
import { ElMessage } from 'element-plus'
import { fetchVersions, approveSchedule, type ScheduleVersion } from '../api/schedule'

const versions = ref<ScheduleVersion[]>([])
const loading = ref(false)
const adminToken = ref(localStorage.getItem('admin_token') || '')

async function load() {
  loading.value = true
  try {
    versions.value = await fetchVersions()
  } finally {
    loading.value = false
  }
}

function saveToken() {
  localStorage.setItem('admin_token', adminToken.value.trim())
  ElMessage.success('admin token 已保存')
}

async function act(v: ScheduleVersion, action: '通过' | '驳回') {
  const token = adminToken.value.trim()
  if (!token) {
    ElMessage.warning('请先填写并保存 admin token')
    return
  }
  const res = await approveSchedule(v.id, action, token)
  if (res.ok) ElMessage.success(res.message)
  else ElMessage.error(res.message)
  await load()
}

onMounted(load)
</script>

<template>
  <div class="schedule-page">
    <h2>排产版本审批</h2>
    <div class="toolbar">
      <el-input v-model="adminToken" placeholder="admin token（R-7 写端点鉴权）" style="width: 320px" />
      <el-button type="primary" @click="saveToken">保存 token</el-button>
      <el-button @click="load" :loading="loading">刷新</el-button>
    </div>
    <el-table :data="versions" v-loading="loading" border stripe>
      <el-table-column prop="id" label="版本" width="80" />
      <el-table-column prop="created_at" label="生成时间" />
      <el-table-column prop="triggered_by" label="触发方式" width="120" />
      <el-table-column prop="status" label="状态" width="120" />
      <el-table-column prop="batch_count" label="批次数" width="100" />
      <el-table-column label="操作" width="180">
        <template #default="{ row }">
          <el-button size="small" type="success" :disabled="row.status !== '待审核'" @click="act(row, '通过')">通过</el-button>
          <el-button size="small" type="danger" :disabled="row.status !== '待审核'" @click="act(row, '驳回')">驳回</el-button>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>

<style scoped>
.schedule-page { padding: 16px; }
.toolbar { display: flex; gap: 8px; margin-bottom: 12px; }
</style>
```

- [ ] **步骤 3：路由 + 代理**

`web/src/router/index.ts` 追加 import 和路由：

```ts
import ScheduleView from '../views/ScheduleView.vue'
// routes 数组追加：
{ path: '/portal/schedule', name: 'schedule', component: ScheduleView },
```

`web/vite.config.ts` 的 proxy 增加：`'/schedule': apiTarget,`

- [ ] **步骤 4：构建验证**

运行（WSL 下）：`cd web && npm install && npm run build`
预期：`vue-tsc` 无类型错误 + `vite build` 产出 `web/dist/`

- [ ] **步骤 5：Commit**

```bash
git add web/src/api/schedule.ts web/src/views/ScheduleView.vue web/src/router/index.ts web/vite.config.ts
git commit -m "feat(web): 排产审批页面（版本列表 + 通过/驳回）"
```

---

## 任务 3：前端部署（FastAPI 同源托管）

**文件：**
- 修改：`demo/api.py`（StaticFiles 挂载）
- 修改：`docker-compose.yml`（挂载 `web/dist`）

- [ ] **步骤 1：确认构建产物存在**

运行：`ls web/dist/index.html`
预期：文件存在（任务 2 步骤 4 产出）

- [ ] **步骤 2：FastAPI 挂载静态目录**

`demo/api.py` 顶部 import 加：`from fastapi.staticfiles import StaticFiles`
文件末尾（路由定义之后）追加：

```python
# 前端同源托管：构建产物在 web/dist，经 /assets 与根路径返回（需存在目录）
_DIST = Path(__file__).resolve().parent.parent / "web" / "dist"
if _DIST.exists():
    app.mount("/assets", StaticFiles(directory=_DIST / "assets"), name="assets")
    from fastapi.responses import FileResponse

    @app.get("/{full_path:path}", include_in_schema=False)
    def _spa(full_path: str):
        candidate = _DIST / full_path
        if full_path and candidate.is_file():
            return FileResponse(candidate)
        return FileResponse(_DIST / "index.html")
```

（`api.py` 顶部需 `from pathlib import Path`，若已有则复用）

- [ ] **步骤 3：compose 挂载 dist**

`docker-compose.yml` volumes 追加：`- ./web/dist:/app/web/dist`

- [ ] **步骤 4：重建容器 + 验证前端可达**

运行：
```bash
docker compose build --build-arg HTTP_PROXY=... --build-arg HTTPS_PROXY=... --build-arg NO_PROXY=pypi.tuna.tsinghua.edu.cn,127.0.0.1,localhost
docker compose up -d
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8000/portal/schedule
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8000/schedule/versions
```
预期：两者均 200

- [ ] **步骤 5：Commit**

```bash
git add demo/api.py docker-compose.yml
git commit -m "feat(demo): FastAPI 同源托管前端 dist + 静态挂载"
```

---

## 任务 4：端到端验收测试

**验收标准**（来自本会话讨论的闭环 + spec 相关条目）：
- A1 审批 API 可用：`GET /schedule/versions` 返回列表，`POST /schedule/approve`（带 admin token）将「待审核」改为「已审核」；无 token 401
- A2 前端审批页：`/portal/schedule` 显示版本列表，点「通过」按钮后状态刷新为已审核
- A3 审批后生产闭环：已审核版本对应批次 `approval_status=通过`，模拟器 tick 产生业务事件（sim_events 增长）
- A4 语义缓存 TTL 回归：`pytest` 全量通过（含 TTL/状态类清理用例），冒烟测试全绿
- A5 其余冒烟用例无回归（S0-S10）

- [ ] **步骤 1：审批链路 curl 验证**

运行（WSL 下脚本，token 用 `STS().issue_user_token("admin-debug","admin")` 生成）：
```bash
# 1. 列表
curl -s http://localhost:8000/schedule/versions | python3 -m json.tool | head -20
# 2. 取最新待审核版本 id，审批通过
# 3. 再查确认 status=已审核
```
预期：版本 status 从「待审核」→「已审核」

- [ ] **步骤 2：前端页面验证**

运行：浏览器打开 `http://localhost:8000/portal/schedule`，填入 admin token → 列表显示 → 点最新待审核版本「通过」
预期：按钮可用（status=待审核）、点后 ElMessage 成功、列表状态刷新为已审核

- [ ] **步骤 3：生产闭环验证**

运行（临时脚本查 DB）：
```sql
SELECT approval_status, COUNT(*) FROM batches WHERE schedule_version_id=<已审核版本> GROUP BY approval_status;
SELECT event_type, COUNT(*) FROM sim_events GROUP BY event_type;
```
预期：已审核版本批次 `approval_status=通过`；sim_events 有事件且随时间增长

- [ ] **步骤 4：TTL 全量测试 + 冒烟**

运行：
```bash
# 全量 pytest（WSL venv；semantic 用例需模型，若 WSL 无缓存则用容器内或配 HF 代理）
python -m pytest demo/cache/test_cache.py -v
# 冒烟（HTTP 模式，全跑含 S10）
python demo/smoke_test.py --base-url http://localhost:8000
```
预期：cache 用例全 PASS（含 3 个新 TTL 用例）；冒烟 26+ PASS / 0 FAIL（S9.2 设计性跳过除外）

- [ ] **步骤 5：Commit（若有测试/文档改动）**

```bash
git add -A
git commit -m "test(demo): 审批端到端 + TTL 回归验证"
```

---

## 自检对照

- 规格覆盖：审批闭环（A1-A3）、TTL 回归（A4）、无回归（A5）均落到任务 1-4
- 占位符扫描：所有步骤含实际代码/命令，无「后续实现」类占位
- 类型一致：`approveSchedule(v.id, action, token)` ↔ `ScheduleApproveRequest{version_id, action}` 对齐；`fetchVersions()` ↔ `GET /schedule/versions` 对齐
- 端到端验收：任务 4 为最后一个任务，逐条列出 A1-A5 验收标准 + 验证命令
