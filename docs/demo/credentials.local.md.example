# 凭据映射表（示例文件）

> ⚠️ 警示：复制本文件为 `credentials.local.md` 后填入真实值。真实文件已被 .gitignore 忽略，**任何情况下不得提交**。代码/文档中只允许出现左列占位符。
>
> **敏感分级**（行业实践）：
> - 🔴 L1 高危：主密钥、数据库 root 口令、生产 API key（泄露 = 直接经济损失）
> - 🟡 L2 中危：业务库用户口令、Redis 口令、JWT secret（泄露 = 单系统沦陷）
> - 🟢 L3 低危：非生产测试 key、只读账号口令（泄露 = 信息泄漏但无操作权限）
>
> **填充步骤**（部署时执行一次）：
> 1. `cp docs/demo/credentials.local.md.example docs/demo/credentials.local.md`
> 2. 按表格第三列填入真实值（L1/L2 必须填，L3 不用就留「（未用）」）
> 3. 验证：`chmod 600 credentials.local.md`（WSL/Linux），确保只有本人可读
> 4. 确认不入库：`git status` 不应出现 `credentials.local.md`

| 占位符 | 用途 | 真实值 | 级别 |
|--------|------|--------|------|
| `{{MYSQL_HOST}}` | MySQL 主机（默认 127.0.0.1） | 127.0.0.1 | 🟢 L3 |
| `{{MYSQL_PORT}}` | MySQL 端口（默认 3306） | 3306 | 🟢 L3 |
| `{{MYSQL_DB}}` | MySQL 库名（默认 demo_scheduling） | demo_scheduling | 🟢 L3 |
| `{{MYSQL_USER}}` | MySQL 业务用户名（默认 demo_sched） | demo_sched | 🟢 L3 |
| `{{MYSQL_PASSWORD}}` | MySQL 业务用户口令 | （填入） | 🟡 L2 |
| `{{MYSQL_ROOT_PASSWORD}}` | MySQL root 口令（仅部署初始化用，业务代码不读） | （填入） | 🔴 L1 |
| `{{REDIS_HOST}}` | Redis 主机（默认 127.0.0.1） | 127.0.0.1 | 🟢 L3 |
| `{{REDIS_PORT}}` | Redis 端口（默认 6379） | 6379 | 🟢 L3 |
| `{{REDIS_PASSWORD}}` | Redis AUTH 口令（无口令留「（未用）」） | （未用） | 🟡 L2 |
| `{{REDIS_DB}}` | Redis 库编号（默认 0） | 0 | 🟢 L3 |
| `{{VOLC_API_KEY}}` | 火山豆包 coding plan API Key（主 provider） | （填入） | 🔴 L1 |
| `{{DEEPSEEK_API_KEY}}` | DeepSeek API Key（备 provider） | （填入） | 🔴 L1 |
| `{{KIMI_API_KEY}}` | Kimi coding plan API Key（备2，未启用） | （未用） | 🔴 L1 |
| `{{JWT_SECRET}}` | HTTP 层 JWT 签名密钥（v2-06 启用后使用） | （填入） | 🔴 L1 |
| `{{SMTP_AUTH_CODE}}` | QQ 邮箱 SMTP 授权码（发激活邮件用） | （填入） | 🟡 L2 |
| `{{ADMIN_TOKEN_SEED}}` | 写端点 admin token 种子（STS 签发用） | （填入） | 🟡 L2 |

---

## 轮换策略（建议）

| 级别 | 轮换周期 | 触发即时轮换的场景 |
|------|---------|-------------------|
| L1 🔴 | 90 天 | 人员离职 / 怀疑泄露 / 安全事件 |
| L2 🟡 | 180 天 | 版本大升级 / 主机迁移 |
| L3 🟢 | 365 天 | 常规审计时一并换 |

## 校验清单（部署完成后自查）

- [ ] `git status` 无 `credentials.local.md`（确认 .gitignore 生效）
- [ ] `.env` 中无明文 L1/L2 口令（全部为占位符或空值）
- [ ] `chmod 600 credentials.local.md`（WSL/Linux）
- [ ] 备份：加密存储在密码管理器中，不落明文盘
- [ ] 冒烟测试包含「敏感信息泄露扫描」步骤（`python demo/smoke_test.py --scan-secrets`）
