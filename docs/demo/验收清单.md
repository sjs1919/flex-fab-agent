# demo 验收清单（Phase 2 重构执行表）

> **作用**：重构实施前拆出的验收基线表（需求规格 v1 §7 预告）。**门禁2（代码验收）以此为核对源头**，逐里程碑打勾；全部 ✅ 才算 Phase 2 完成。
> **规则**：M 里程碑完成时勾选对应组；回填项（R-D1/D2/D3、前端方案 A）已并入对应组。回归项任一失败则整组回退重验。
> **来源**：[排程排产需求规格 §10](02-specs/排程排产需求规格-v1-2026-08-21.md) · [重构方案 §8](04-plans/重构方案-v1-2026-08-21.md) · [demo/README.md §验收清单](../../demo/README.md)（Phase 1 基线）

## 0. 现状基线（Phase 1，已全绿，回归不得劣化）

- [x] `python -m demo.main --check` 地基自检通过
- [x] 单 Agent：订单问题多工具调用 + 综合回答；RAG 命中合同（rerank 分 >0.9）
- [x] 多 Agent：Supervisor 分派 + RBAC 拒绝越权 + 审计报告
- [x] R1-R8 + #1-#7 工程化改造全部验收（沙箱/护栏/评估/租户/缓存/持久化/观测/成本/审计/LLM 连接池/MCP）
- [x] `python run_all_tests.py` 全量 **139 passed**（Phase 1 基线；Phase 2 只增不减）
- [x] eval 回归基线 **≥7/10**（case ≥0.6 判 pass）
- [x] ragas 四指标基线：context_precision 0.60 / faithfulness 0.80 / answer_relevancy 0.80 / context_recall 0.90

## M1 数据层（MySQL + WSL）

- [ ] MySQL 跑 WSL，Windows 侧经 localhost 访问；`DEMO_DATA_SOURCE=mysql` 缺连接串时报错提示、csv 兜底
- [ ] schema + 种子数据（7 台设备/orders 带金额加急/parts）脚本化可重建（`simulator/seed.py`），禁止手改库
- [ ] `python -m demo.main --check` 数据加载来自 MySQL；数据层 pytest 通过
- [ ] 工具函数签名不变（仍返回 list[dict]，工具层零改动）；R8 租户过滤保留（WHERE tenant_id=?）
- [ ] **R-D3 回填验收**：PooledDB 连接池（池 5-10，三并发入口共用），业务代码无裸 `pymysql.connect`
- [ ] **R-D3 回填验收**：模拟器 tick 多写单事务原子提交，失败整体回滚（构造失败用例验证无半写）

## M2 求解器（CP-SAT）

- [ ] 输入样例订单集合 -> 输出排产表（每个 part -> 批次 -> 设备 -> 开工/完成）
- [ ] 硬约束 C1-C9 全部满足（`verify.py` 程序化校验）
- [ ] 超尺寸零件（max>600）直接预警进清单，不静默忽略
- [ ] 装舱判定正确：Σ投影面积×0.7、件数上限、Σ件重≤承重
- [ ] 目标函数正确：加权迟到（金额×违约金日费率×延期天数）、交期->priority 排序、同订单同舱偏好（软惩罚）
- [ ] 指标输出：准交率 / 舱利用率 / 延期订单清单
- [ ] 拆批场景正确（整批优先、超舱可拆、记录来源）
- [ ] 无可行解给出冲突订单清单（不静默空结果）
- [ ] 10% 产能预留生效：常规排产占用 ≤90% 设备产能，应急池可用
- [ ] demo 规模求解时间秒级
- [ ] **R-D1 回填验收**：`max_time_in_seconds` 预算（默认 60s，system_config 可调）生效
- [ ] **R-D1 回填验收**：registry 工具级超时覆盖（solver 工具 120s > 默认 TOOL_TIMEOUT=10s），求解器不被沙箱杀掉重试
- [ ] **R-D1 回填验收**：预算耗尽返回当前最优可行解 + `timed_out=true` 标记 + 求解耗时，不报错；solver span 记录耗时/目标值/timed_out（单测构造：`max_time_in_seconds=1` 小预算 + 大订单集触发）

## M3 模拟器（A 时钟 + B 事件）

- [ ] `--sim` 跑 10 分钟：批次完成 / 故障 / 插单事件可见
- [ ] sim_clock / 事件（scheduled->fired）/ state_change_log 正确落库；审计 subject="simulator"
- [ ] A 层口径：1 tick = 1 sim 小时；批次进度 -> 静置 -> 设备释放占用；前道人效累积；班次/换班（30min 无产出）
- [ ] B 层：MTBF/MTTR 指数分布 + 到达率预排，到点触发
- [ ] 硬性不可行检测：在跑批次设备故障且修不回交期 -> 生成强"需重排"告警，agent 经 `query_sim_events` 可见
- [ ] 模拟器线程与 MySQL 并发：串行 tick + 短事务 + 连接池（R-D3），无锁问题、无脏状态

## M4 集成（工具注册 + RBAC + 观测）

- [ ] `--check` 报工具数 = 7+11；11 个排产工具全部经 `build_default_registry()` 注册，继承全管线
- [ ] 排版人审批流程：待审核 -> 通过下达上机 / 驳回重新排产；审批动作入审计
- [ ] CTP 计算：新单不影响排队/在途前提下算出最短可交付时间
- [ ] 订单预测：按历史输出未来 N 天分材料预测订单量/产能占用（统计方法）
- [ ] 产能负载评估：在途/排队分布 + 各订单预计完成时间；满负荷超期预警清单正确；T 窗口消化判断（剩余产能/缺机器数）正确
- [ ] 三区制判定正确（绿/黄/红：需求 vs 90%、100% 可用）
- [ ] 前道任务正确排产：前道完成 ≤ 打印开始；人效/班制/换班口径生效，前道不堵打印
- [ ] 订单状态跟踪：在途预计完成；排队预计开打/完成 + 前面单据清单（按交期->priority 队列）
- [ ] 模型路由（B3）：简单走快模型、复杂走强模型；路由策略可配；成本统计随调用落库
- [ ] RAG 权限检索（B4）：不同角色同问题返回不同文档集；越权角色查不到受限条款
- [ ] 工具治理（B5）：写工具无 token 或越权被拒；只读工具无写副作用；写操作入审计
- [ ] **R-D2 回填验收**：写工具配额生效--同角色 5min 内超 3 次重排被拒，审计记 `quota_exceeded`，tracer span 记录
- [ ] Prompt/工作流版本化（B6）：配置加载 + `--rollback` 回滚旧版本；版本变更/回滚入审计
- [ ] trace 含 solver span（耗时/目标值/约束违反数）；reviewer 审批权限生效（多 Agent 模式）

## M5 打磨（解释 + 预测校准 + 看板）

- [ ] LLM 可解释排产表（"哪些订单会延期、为什么"），引用排产表数据
- [ ] 大单承诺期（预测校准 CTP）：按"现有占用 + 预测未来占用 + 可用产能"给承诺日期，不偏乐观
- [ ] KPI 实时看板（B1）：`query_kpi` 实时算准交率/延期金额/舱利用率/良率/前道瓶颈；tick 后数值变化正确
- [ ] 良率/坏件闭环（B7）：按设备/批次/材料下钻；坏件根因关联；LLM 改善建议
- [ ] **前端方案 A 回填验收**：B8 Dashboard 为 Vue 视图 + ECharts 消费只读 API（KPI 曲线/成本分模型/trace 摘要，口径与 query_kpi/cost 一致）；静态 HTML 生成路径保留为离线兜底
- [ ] ragas 四指标回归不劣化基线（§0 末行）
- [ ] eval 回归 ≥7/10；backtest 5 个历史延期复盘场景跑通

## M6 调试台 + 前端

- [ ] **前端方案 A 回填验收**：G5 主页为 Vue3+Vite+TS+Element Plus 应用（结构基准 web-portal）；构建全在 WSL 下执行
- [ ] 主页输入 -> 两级链路展示（入参/出参/耗时/成功）；执行后 judge 打分
- [ ] case 自动落盘 cases.jsonl（trace_id 主键、normal/chitchat/empty 类型、good/bad 仅 normal）；人工标注优先
- [ ] 按 trace_id 回放 / 重跑 / 批量 bad->good 转化率统计；生产开关生效

## 通用门禁（每个 M 完成时必过）

- [ ] `python run_all_tests.py` 全绿（≥ 前一里程碑通过数）
- [ ] `python -m compileall demo` 静态检查通过
- [ ] 门禁2：逐条勾选本表对应组，无遗漏才进下一 M
- [ ] 门禁3：CR 批判式走读通过（无「打日志丢弃」/越权/吞错）

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-08-22 | v1 拆出：需求规格 §7 预告落地；Phase 1 基线 + M1-M6 分组 + R-D1/D2/D3 与前端方案 A 回填验收项并入 |
