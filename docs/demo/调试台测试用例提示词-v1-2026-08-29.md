# 调试台测试用例提示词（v1 · 2026-08-29）

> **用途**：一份可直接粘贴到「排产调试台」提问框的自然语言测试用例集，覆盖当前 demo 支持的全部可测能力（查订单 / 查设备 / 查员工 / 查批次 / 排产下单 / 订单预测 / 审批 / KPI / 良率 / RAG / 仿真 / 调试台自身）。
>
> **前置**：后端已 `DEMO_DATA_SOURCE=mysql` 启动（见 `demo/README.md` / `部署指南-v1`）；写操作（排产/审批/judge/重跑/标注）需 admin token（调试台自动签发，1h 有效）。

---

## 一、调试台能测什么（摘要）

### 1. 入口与页面

| 入口 | 路由 | 能力 |
|------|------|------|
| **排产调试台** | `/portal/debug` | 自然语言提问 → 触发工具 → 答案 + 两级链路树（入参/出参/耗时/成功）+ judge 打分 |
| **Case 台** | `/portal/cases` | case 列表 / 人工标注 good·bad / 回放 / 重跑 / bad→good 转化率 |
| **配置页** | `/portal/config` | 读写数据源、仿真时钟速率、求解器预算、调试台三开关 |
| **排产控制台** | `/portal/schedule` | 排产版本审批、批次排布查看 |
| **聚合主页** | `/` | 看板 / 调试 / 审批 / 案例 / 配置 tab 聚合 |

### 2. 18 个可测工具（自然语言提问触发）

| 域 | 工具 | 一句话作用 | 写操作 |
|----|------|-----------|--------|
| 订单 | `query_orders` | 查订单列表（多字段 AND 组合筛选 + 排序） | |
| 订单 | `get_order_detail` | 查单订单完整信息 | |
| 订单 | `get_production_status` | 查订单当前生产环节/状态 | |
| 订单 | `query_order_tracking` | 查订单所处环节 + 关联批次 + 前面单据队列 | |
| 资源 | `query_inventory` | 查材料库存（按材料/类别/低于阈值） | |
| 资源 | `query_machine_load` | 查设备负载（运行/空闲/预计释放） | |
| 资源 | `query_customer` | 查客户（等级/信用分/延期率/行业） | |
| RAG | `search_knowledge_base` | 合同知识库混合检索 + 重排 | |
| 排产 | `run_scheduling` | 触发排产求解落库（产出待审核版本） | ✅ |
| 排产 | `query_schedule` | 查排产表（最新/指定版本批次排布） | |
| 排产 | `query_sim_events` | 查模拟器事件流（插单/故障/请假/坏件/告警） | |
| 排产 | `approve_schedule` | 审批排产版本：通过 / 驳回 | ✅ |
| 排产 | `query_ctp` | 查最短可交付时间（交期承诺 CTP） | |
| 排产 | `query_load_assessment` | 产能负载评估（四段报告 + 三区制） | |
| 排产 | `query_preprocess_load` | 前道人池负载（排队/在途/瓶颈） | |
| 排产 | `query_kpi` | 排产 KPI（准交率/延期金额/舱利用率/良率/瓶颈） | |
| 排产 | `query_forecast` | 订单量统计预测（分材料件数/机时） | |
| 排产 | `query_yield` | 打印良率（总览→设备→批次→材料 + 改善建议） | |

> **员工/人员** 无独立 LLM 工具，经 `query_preprocess_load`（前道人员池）/ `query_load_assessment`（前道人池）+ HTTP `GET /resources/personnel`、`PUT /resources/personnel/{pid}/status` 覆盖。
> **下单/插单** 无 `create_order` 工具，由模拟器 `new_order` 事件触发（`/sim/start` 后随机插 1~10 单进「待排队」）。

### 3. 调试台自身的测试功能（M6 交付）

- **两级链路树**：每次提问后可见 span 树，节点含工具名 / 入参 / 出参 / 耗时 / 成功标记，可定位「LLM 传参对不对、工具是否执行失败」。
- **judge 打分**：对答案语义相关度打分（`answer_relevancy`），点「judge 打分」按钮（需 token）。
- **case 治理**：每次 `/ask` 自动落盘 `cases.jsonl`（分类 normal/chitchat/empty），可人工标注 good/bad、按 trace_id 回放/重跑、统计 bad→good 转化率。
- **配置读写**：数据源 / `SIM_TICK_SECONDS` / 求解器预算 / 调试台三开关。

---

## 二、测试用例提示词（按域分组，可直接粘贴）

> 用法：把「粘贴提问」逐条复制到调试台输入框，点「执行提问」，对照「期望触发工具」与「期望看点」验收。写操作会提示需要 admin token。

### A. 查订单

| # | 粘贴提问 | 期望触发工具 | 期望看点 |
|---|---------|-------------|---------|
| A1 | 帮我查一下所有还在排队的订单 | `query_orders(status=待排队)` | 返回待排队订单列表（含订单号/客户/交期/状态） |
| A2 | 交期在 9 月 10 号之前、A 级客户的 3D 打印订单有哪些，按优先级排序 | `query_orders(due_before, customer_level=A, process=3D打印, sort_by=priority)` | 命中组合筛选，数量正确、按优先级降序 |
| A3 | 哪些订单最紧急，排前 5 个 | `query_orders(sort_by=priority, limit=5)` | 只返回 5 条 |
| A4 | 查一下订单 ORD001 的完整信息 | `get_order_detail(ORD001)` | 完整字段（客户/金额/交期/优先级/零件） |
| A5 | ORD001 现在生产到哪个环节了 | `get_production_status(ORD001)` | 当前环节 + 状态 |
| A6 | 帮我跟踪 ORD001，它前面还有多少单在等，预计什么时候能打 | `query_order_tracking(ORD001)` | 所处环节 + 关联批次 + 前面单据清单（交期升序） |

### B. 查设备

| # | 粘贴提问 | 期望触发工具 | 期望看点 |
|---|---------|-------------|---------|
| B1 | 现在哪些设备在运行、哪些空闲、预计什么时候释放 | `query_machine_load()` | 设备清单 + 运行/空闲状态 + 预计释放时间 |
| B2 | 帮我检查今天的设备负载风险，看看有没有订单可能延期 | `query_machine_load()` + `query_load_assessment()` | 负载 + 满负荷超期预警 |

### C. 查员工 / 前道人员

| # | 方式 | 内容 | 期望看点 |
|---|------|------|---------|
| C1 | 提问 | 前道人员池现在什么情况，排队多少，会不会成瓶颈 | `query_preprocess_load()`：池占用率 / 排队与在途 / 预计清空 / 是否瓶颈 |
| C2 | 提问 | 现在整体产能负载怎么样，前道人员够不够 | `query_load_assessment()`：含前道人池负载段 |
| C3 | HTTP | `GET /resources/personnel` | 人员列表（id/姓名/状态/班次等） |
| C4 | HTTP（admin） | `PUT /resources/personnel/{pid}/status` `{"status":"请假"}` | 人员状态切换 上班↔请假（幂等） |

### D. 查库存 / 材料

| # | 粘贴提问 | 期望触发工具 | 期望看点 |
|---|---------|-------------|---------|
| D1 | 钛合金材料库存还剩多少 | `query_inventory(material_name=钛合金)` | 命中材料库存量 |
| D2 | 哪些材料库存告急（低于 100） | `query_inventory(stock_below=100, sort_by=stock_asc)` | 低库存材料按从少到多 |

### E. 查客户

| # | 粘贴提问 | 期望触发工具 | 期望看点 |
|---|---------|-------------|---------|
| E1 | 查一下 C001 这个客户的信息 | `query_customer(customer_id=C001)` | 等级 / 信用分 / 延期率 / 行业 |
| E2 | S 级客户都有哪些 | `query_customer(min_level=S)` | S 级客户清单 |

### F. 查批次 / 排产表

| # | 方式 | 内容 | 期望看点 |
|---|------|------|---------|
| F1 | 提问 | 最新排产版本的批次排布是什么 | `query_schedule(version_id=0)`：批次（设备/工序/起止时间/审批状态） |
| F2 | 提问 | 看一下版本 5 的历史排产 | `query_schedule(version_id=5)` |
| F3 | HTTP | `GET /schedule/latest` | 最新版本 + 批次（含 approval_status） |
| F4 | HTTP | `GET /resources/batches` | 批次列表（id 倒序） |

### G. 排产（写） + 下单/插单

| # | 方式 | 内容 | 期望看点 |
|---|------|------|---------|
| G1 | 提问（写） | 帮我跑一轮排产 | `run_scheduling()`：返回新版本号 + 关键指标，状态「待审核」 |
| G2 | 提问 | 插单之后帮我重新排一次产 | `run_scheduling()`（重排兜底） |
| G3 | HTTP | `POST /sim/start` → 等待 → `query_sim_events(event_type=new_order)` | 模拟器插 1~10 单（`SIM*` 订单进「待排队」） |
| G4 | 提问 | 最近有没有新订单插进来 | `query_sim_events(event_type=new_order)`：SIM 订单事件 |
| G5 | 提问 | 查一下有没有设备故障事件 | `query_sim_events(event_type=machine_failure)` |

### H. 订单预测 / 交期承诺

| # | 粘贴提问 | 期望触发工具 | 期望看点 |
|---|---------|-------------|---------|
| H1 | 未来 5 天各材料的订单量预测是多少 | `query_forecast(days=5)`：逐日分材料件数/机时 |
| H2 | 客户要 SLA 工艺 10 件、零件高 100mm，最快什么时候能交付 | `query_ctp(material=SLA, quantity=10, height_mm=100)`：承诺交期 + 校准承诺期 |
| H3 | 这笔 SLA 10 件能赶在 9 月 5 号前交吗 | `query_ctp(..., due_date=2026-09-05)`：能否按期判断 |
| H4 | 客户要下 8 万的大单，SLA 20 件，报个交期 | `query_ctp(..., amount=80000)`：大单提示按校准承诺期 |

### I. 审批（写）

| # | 方式 | 内容 | 期望看点 |
|---|------|------|---------|
| I1 | 提问（写） | 审批通过版本 3 | `approve_schedule(version_id=3, action=通过)`：版本→已审核，批次→通过 |
| I2 | 提问（写） | 驳回版本 4，理由：交期太赶 | `approve_schedule(version_id=4, action=驳回)`：版本→已驳回，批次→驳回，订单回退待排队 |
| I3 | HTTP | `GET /schedule/versions` | 版本列表（含待审核项） |

### J. 负载评估 / KPI / 良率

| # | 粘贴提问 | 期望触发工具 | 期望看点 |
|---|---------|-------------|---------|
| J1 | 帮我评估一下当前产能负载 | `query_load_assessment()`：在途/排队分布 + 预计完成 + 超期预警 + T 窗口消化 + 三区制 |
| J2 | 当前排产的准交率、延期金额、良率是多少 | `query_kpi()`：准交率/延期金额/舱利用率/良率/前道瓶颈 |
| J3 | 最近打印良率怎么样，哪台设备最低 | `query_yield()`：总览→设备（含 MTBF）→批次→材料 + 改善建议 |
| J4 | HTTP | `GET /kpi` / `GET /dashboard/kpi-history` | KPI 快照 + 历史 |

### K. 知识库（RAG）

| # | 粘贴提问 | 期望触发工具 | 期望看点 |
|---|---------|-------------|---------|
| K1 | 广州航天合同有什么特殊条款 | `search_knowledge_base(广州航天合同特殊条款)`：检索片段 |
| K2 | 深圳精密的延期记录有没有 | `search_knowledge_base(...)` |

### L. 仿真控制 + 调试台自身

| # | 方式 | 内容 | 期望看点 |
|---|------|------|---------|
| L1 | HTTP | `POST /sim/start` / `GET /sim/status` / `POST /sim/stop` | 模拟器心跳启停、sim 时间推进 |
| L2 | UI | 提问后点「judge 打分」 | `answer_relevancy` 分数卡片 |
| L3 | UI | Cases 页标注某 case 为 bad → 重跑 → 看转化率 | case 落盘 / label / rerun / bad→good 转化率 |
| L4 | UI | Config 页读写开关 | 数据源 / SIM_TICK_SECONDS / 求解器预算 / 调试台三开关 |
| L5 | HTTP | `GET /health` | 18 工具注册 / provider / 缓存 / checkpointer / sim 状态 |
| L6 | HTTP | `GET /debug/cases` / `GET /debug/trace/{id}` / `GET /debug/stats` | case 列表 / 回放 / 转化率统计 |

---

## 三、使用注意事项

1. **清缓存**：改代码/数据后必须删 `demo/data/cache_db` 并重启，否则语义缓存命中旧答案（换 embedding 模型尤其要清）。
2. **数据源**：确认后端 `DEMO_DATA_SOURCE=mysql`（种子 20 单），csv 模式字段不全会导致「查订单无内容」。
3. **admin token**：写操作（`run_scheduling`/`approve_schedule`）、judge/重跑/标注均需 token；调试台打开时自动签发，失效可手输。
4. **审批门禁**：新排产版本默认「待审核」，`query_schedule`/模拟器推进只认 `approval_status=通过` 的批次——G1 跑完排产后要先 I1 审批，后续负载/KPI 才有数据。
5. **状态枚举**：统一用新枚举（待排队/已审核/打印中/静置中/完成/已驳回）；口吻词（排队/排队中/排产中）会被 `normalize_order_status` 归一，提问用自然语言即可。

---

## 四、来源

- 工具注册表：`demo/tools/registry.py`（`build_default_registry`，18 工具）
- 端点：`demo/api.py`（`/ask`、`/debug/*`、`/config`、`/sim/*`、`/schedule/*`、`/resources/*`、`/dashboard/*`、`/kpi`）
- 前端：`web/src/views/DebugView.vue` / `CasesView.vue` / `ConfigView.vue` / `ScheduleView.vue` + `web/src/api/debug.ts`
- M6 任务与验收：`docs/demo/05-tasklist/todo-M6-20260824.md`、`docs/demo/验收清单.md`

---

## 五、正式测试执行清单 v1（2026-08-29 生成）

> **用途**：把第二节用例固化为「可直接复制粘贴的正式测试提示词 + 已核对数值」，正式测试 / 现场演示直接照做即可。
> **数据基准**：MySQL 数据源（`DEMO_DATA_SOURCE=mysql`，`python -m demo.simulator.seed --reset` 种子 20 单）。关键值已按 `demo/simulator/seed.py` 核对：
> - 订单 ORD001~ORD020 **全部「待排队」**（未跑排产）；客户 C001 深圳精密五金(A级·信用92) / C002 东莞模具厂(B) / C003 广州航天精工(S) / C004 惠州医疗器械(C) / C005 佛山智能家居(B)
> - 材料 MAT001~MAT010：「钛合金」= MAT003「钛合金粉末」25kg（子串匹配，问「钛合金」能命中）；低于 50 的有 MAT003(25)/MAT006(35)/MAT008(15)/MAT009(8)/MAT010(12)
> - 设备 M0001~M0007：SLA600 / SLA450×2 / MJS600 / MJS450×2 / SLM600；工艺枚举 SLA/MJS/SLM（**不是「3D打印」**）
> - 排产版本需先跑 G1 才有；审批版本号以 `GET /schedule/versions` 实际返回为准
>
> **时间记录**：清单生成于 **2026-08-29**。「预计耗时」为演示节奏预估；「实际执行时间/结果」待正式测试时回填（增量落盘约定）。
> **演示顺序**：⚠️ 有依赖——先 **G1 跑排产 → I1 审批通过**，再查 F（批次）/ J（KPI）才有数据；否则 F1/F2/J1/J2 返回空。

### 5.1 查订单（A）

| 用例 | 正式测试提示词（复制即用） | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| A1 | 帮我查一下所有还在排队的订单 | `query_orders(status=待排队)` → 种子 20 单全待排队，应返 20 条 | ~5s | 待回填 |
| A2 | 交期在 2026-09-10 之前、A 级客户、SLA 工艺的订单有哪些，按优先级排序 | `query_orders(due_before=2026-09-10, customer_level=A, process=SLA, sort_by=priority)`。⚠️ process 匹配 part.material（SLA/MJS/SLM），**不能用「3D打印」** | ~5s | 待回填 |
| A2′ | （备选·不依赖工艺，更稳）交期在 2026-09-10 之前、A 级客户的订单有哪些，按优先级排序 | `query_orders(due_before=2026-09-10, customer_level=A, sort_by=priority)` | ~5s | 待回填 |
| A3 | 哪些订单最紧急，排前 5 个 | `query_orders(sort_by=priority, limit=5)` → 只返 5 条 | ~5s | 待回填 |
| A4 | 查一下订单 ORD001 的完整信息 | `get_order_detail(ORD001)` | ~5s | 待回填 |
| A5 | ORD001 现在生产到哪个环节了 | `get_production_status(ORD001)` → 未排产，状态=待排队 | ~5s | 待回填 |
| A6 | 帮我跟踪 ORD001，它前面还有多少单在等，预计什么时候能打 | `query_order_tracking(ORD001)` | ~5s | 待回填 |

### 5.2 查设备（B）

| 用例 | 正式测试提示词 | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| B1 | 现在哪些设备在运行、哪些空闲、预计什么时候释放 | `query_machine_load()` → 7 台设备清单 | ~5s | 待回填 |
| B2 | 帮我检查今天的设备负载风险，看看有没有订单可能延期 | `query_machine_load()` + `query_load_assessment()`（多工具链） | ~8s | 待回填 |

### 5.3 前道人员 / 员工（C）

> ⚠️ 人员**无独立 LLM 提问工具**（18 工具不含 personnel），调试台提问入口只覆盖 C1/C2（前道人池负载）；人员列表/状态切换在 Resources 页展示，非调试台提问触发（本清单不列 HTTP）。
| 用例 | 正式测试提示词（复制即用） | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| C1 | 前道人员池现在什么情况，排队多少，会不会成瓶颈 | `query_preprocess_load()`（池占用率/排队/在途/瓶颈） | ~5s | 待回填 |
| C2 | 现在整体产能负载怎么样，前道人员够不够 | `query_load_assessment()`（含前道人池负载段） | ~5s | 待回填 |

### 5.4 查库存 / 材料（D）

| 用例 | 正式测试提示词 | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| D1 | 钛合金材料库存还剩多少 | `query_inventory(material_name=钛合金)` → 命中 MAT003 钛合金粉末 25kg（子串匹配） | ~5s | 待回填 |
| D2 | 哪些材料库存告急（低于 50） | `query_inventory(stock_below=50, sort_by=stock_asc)` → 5 条（MAT003/006/008/009/010） | ~5s | 待回填 |

### 5.5 查客户（E）

| 用例 | 正式测试提示词 | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| E1 | 查一下 C001 这个客户的信息 | `query_customer(customer_id=C001)` → 深圳精密五金 A级 信用92 折扣0.85 消费电子 | ~5s | 待回填 |
| E2 | S 级客户都有哪些 | `query_customer(min_level=S)` → 仅 C003 广州航天精工 | ~5s | 待回填 |

### 5.6 查批次 / 排产表（F）

| 用例 | 正式测试提示词（复制即用） | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| F1 | 最新排产版本的批次排布是什么 | `query_schedule(version_id=0)`（0=最新）。⚠️ **需先 G1 跑排产**，否则空 | ~5s | 待回填 |
| F2 | 看一下版本 5 的历史排产 | `query_schedule(version_id=5)`。⚠️ 版本号以实际存在为准 | ~5s | 待回填 |

> 版本/批次详情在「排产控制台」页面展示，非调试台提问入口（本清单不列 HTTP）。

### 5.7 排产（写）+ 下单/插单（G）

| 用例 | 正式测试提示词（复制即用） | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| G1 | 帮我跑一轮排产 | `run_scheduling()` → 新版本「待审核」（写操作需 admin token，调试台自动签发） | ~15-30s（CP-SAT 求解） | 待回填 |
| G2 | 插单之后帮我重新排一次产 | `run_scheduling()`（重排兜底） | ~15-30s | 待回填 |
| G4 | 最近有没有新订单插进来 | `query_sim_events(event_type=new_order)` → SIM 订单事件 | ~5s | 待回填 |
| G5 | 查一下有没有设备故障事件 | `query_sim_events(event_type=machine_failure)` | ~5s | 待回填 |

> 模拟器启动/停止无 LLM 提问工具（后台端点，非调试台提问入口），演示前由运维先起模拟器，再用 G4/G5 提问查事件。

### 5.8 订单预测 / 交期承诺（H）

| 用例 | 正式测试提示词 | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| H1 | 未来 5 天各材料的订单量预测是多少 | `query_forecast(days=5)`（分材料件数/机时，指数平滑） | ~8s | 待回填 |
| H2 | 客户要 SLA 工艺 10 件、零件高 100mm，最快什么时候能交付 | `query_ctp(material=SLA, quantity=10, height_mm=100)` → 承诺交期 + 校准承诺期 | ~8s | 待回填 |
| H3 | 这笔 SLA 10 件能赶在 9 月 5 号前交吗 | `query_ctp(material=SLA, quantity=10, height_mm=100, due_date=2026-09-05)` → 能否按期判断 | ~8s | 待回填 |
| H4 | 客户要下 8 万的大单，SLA 20 件，报个交期 | `query_ctp(material=SLA, quantity=20, height_mm=100, amount=80000)` → 8万>阈值5万，提示按校准承诺期 | ~8s | 待回填 |

### 5.9 审批（写）（I）

| 用例 | 正式测试提示词（复制即用） | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| I1 | 审批通过最新排产版本 | `approve_schedule(version_id=<G1 返回的版本号>, action=通过)` → 版本→已审核，批次→通过。⚠️ **版本号勿写死**，先 G1 跑完看返回 | ~5s | 待回填 |
| I2 | 驳回最新版本，理由：交期太赶 | `approve_schedule(version_id=<当前最新>, action=驳回)` → 版本→已驳回，订单回退待排队 | ~5s | 待回填 |

> 版本列表在「排产控制台」页面展示，非调试台提问入口（本清单不列 HTTP）。

### 5.10 负载评估 / KPI / 良率（J）

| 用例 | 正式测试提示词（复制即用） | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| J1 | 帮我评估一下当前产能负载 | `query_load_assessment()`（在途/排队/超期预警/三区制）。⚠️ 建议先 I1 审批后有数据 | ~5s | 待回填 |
| J2 | 当前排产的准交率、延期金额、良率是多少 | `query_kpi()`。⚠️ 需已审批版本才有数据 | ~5s | 待回填 |
| J3 | 最近打印良率怎么样，哪台设备最低 | `query_yield()`（总览→设备含MTBF→批次→材料） | ~10s | 待回填 |

> KPI 快照/历史在 Dashboard 页展示，非调试台提问入口（本清单不列 HTTP）。

### 5.11 知识库（RAG）（K）

| 用例 | 正式测试提示词 | 期望工具 + 入参数值 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|
| K1 | 广州航天精工的合同有什么特殊条款 | `search_knowledge_base(广州航天精工 合同特殊条款)` → 命中 `contracts/广州航天精工_合同特殊条款.txt` | ~10-15s（检索+重排） | 待回填 |
| K2 | 深圳精密五金的延期记录有没有 | `search_knowledge_base(深圳精密五金 延期记录)` → 命中 `历史延期记录.txt` | ~10-15s | 待回填 |

### 5.12 调试台自身（judge / Case / Config）（L）

> ⚠️ 仿真启停 / 健康探针 / debug 回放接口均属后台能力（无调试台提问入口），本清单聚焦**调试台页面可操作**项（提问 + judge + Case/Config 页）。
| 用例 | 操作位置 | 操作内容 | 期望看点 | 预计耗时 | 实际执行时间/结果 |
|---|---|---|---|---|---|
| L2 | 调试台·提问结果卡片 | 点「judge 打分」按钮 | `answer_relevancy` 分数卡片 | ~10s | 待回填 |
| L3 | Case 台（/portal/cases） | 标注某 case 为 bad → 重跑 → 看转化率 | case 落盘 / label / rerun / bad→good 转化率 | ~15s | 待回填 |
| L4 | 配置页（/portal/config） | 读写开关 | 数据源 / SIM_TICK_SECONDS / 求解器预算 / 调试台三开关 | ~10s | 待回填 |

### 5.13 执行前 Checklist（正式测试 / 演示前必做）

1. **清缓存**：改代码/数据后 `rm -rf demo/data/cache_db demo/data/chroma_db demo/data/llm_cache.db`（换 embedding 模型必清）；
2. **种子**：`DEMO_DATA_SOURCE=mysql` 下 `python -m demo.simulator.seed --reset`（幂等，20 单全待排队）；
3. **起后端**：`uvicorn demo.api:app --host 0.0.0.0 --port 8000`（.env 设 `DEMO_DATA_SOURCE=mysql`）；
4. **冒烟**：`python demo/smoke_test.py` 退出码 0；
5. **写操作**：调试台打开自动签发 admin token（1h 有效），G/I 写操作与 L2/L3 需 token；
6. **演示主线（推荐 15 分钟脚本，全程调试台提问框触发）**：A1→A4→B1→D1→E1→C1→H2→G1→I1→F1→J1→K1→L2，中间穿插写操作演示审批与插单。

### 5.14 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-08-29 | v1 | 首次生成：在第二节用例后追加正式测试提示词 + 已核对数值（对照 seed.py）+ 预计耗时；修正 A2 工艺筛选为 SLA（process 匹配 part.material，非「3D打印」）；审批版本号改为「以实际返回为准」；时间列留待回填 |
| 2026-08-29 | v1.1 | 按用户要求：全部用例改为**调试台提问框触发**（自然语言输入），移除 HTTP 直接调用项（C3/C4/F3/F4/G3/I3/J4/L1/L5/L6 移入备注「非调试台提问入口」）；保留调试台页面操作（L2 judge / L3 Case 台 / L4 配置页） |
