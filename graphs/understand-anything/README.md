# understand-anything 图谱（说明占位）

`/understand` 流水线产物默认写在 **flex-fab-agent 项目根**：`../.understand-anything/`
（`knowledge-graph.json`、`config.json`、`fingerprints.json`、`meta.json`、`.understandignore`）。

本目录已留档一份：`knowledge-graph.json`（702 节点 / 1,569 边 / 10 层 / 9 步巡览，中文，2026-09-03 生成）。

打开方式：flex-fab-agent 里 `claude` 会话执行 `/understand-dashboard`（交互看板）、`/understand-chat <问题>` 提问；改动代码后 `/understand --no-auto-update` 增量刷新。
